# Base media for guide entries (real GIF/PNG files, not base64).
# Served by GET /api/apps/guide/media/{key}; an internal overlay's media/ dir
# takes precedence. Empty until the data lane exports real media.
