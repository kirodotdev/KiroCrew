# Last-synced upstream tips (one SHA per tracked branch/repo).
# Updated at the END of every sync run (Step 1/7). The meshclaw-sync skill bounds
# the candidate set by these SHAs — see skills/meshclaw-sync/SKILL.md Step 1.
#
# This file records the sync BOUNDARY (what has been triaged). For the per-commit
# record of what was deliberately NOT ported and why — including the open
# human-decision (DEFER/flagged) items — see skills/meshclaw-sync/left-out.md.
# Format: "<branch-or-repo> <sha>  # <date> <note>"
#
# TWO upstream repos are tracked (the fork bundles both into one package):
#   - backend  = MeshClaw          (package mesh_claw  → src/kiro_crew/)
#   - frontend = MeshClawWebsite    (the React/Vite SPA → website/src/)
# Boundary semantics differ per repo (see SKILL.md Step 1):
#   - backend  : SHA-range incremental (the fork shares the backend's content lineage).
#   - frontend : the fork's website/ is a DIVERGED partial content-snapshot (~2026-06-02),
#                so frontend sync is by CONTENT within the post-snapshot window, not a
#                clean SHA range. This SHA records the upstream tip last triaged.

# --- Batch-49 (dual-repo beta sync 2026-07-20) --------------------------------
# Branch sync/beta-2026-07-20-batch49 off origin/main b0c52e68 (PR #46 "39 ported"
# MERGED, so batches 42-48 + governance-seam PR #54 + in-process embeddings are on
# main). Backend window 42ad7707..bb3901c8, frontend window f20f5fada..cfa63fc,
# all decided by DIFF against fork content. Tally: 8 ported (5 backend + 3
# frontend) + 1 DEFER + 2 ALREADY_PRESENT + 3 SKIP_INTERNAL. One fork commit per
# keeper (oldest upstream first). Verdicts (one line each):
# 5db73730 [backend] KEEP fix(knowledge): oversized-file guard + off-loop
#   chunking — knowledge.max_ingest_file_mb (default 100.0; message's "20" is
#   stale, code ships 100.0) enforced before read via FileTooLargeError,
#   chunk dispatch moved to asyncio.to_thread (ingest_file + ingest_text),
#   defense-in-depth is_sensitive_path refusal + SEL denial, FolderWatcher +
#   single-file watcher error-then-recover. is_sensitive_path/sel() already
#   generic fork infra (no EXTRACT). +test_knowledge_ingest_guard.py (12 tests).
#   config-baseline.json regenerated (parity test passes). Fork commit 0d6d829e.
# b9120339 [backend] PARTIAL feat(publish): offer installable-but-not-yet-
#   installed providers in the picker — PublishProvider.installable() ABC no-op
#   (default False, declarative sibling of ensure_ready) + api_artifact_publish_
#   providers avail/installable filter + `available` row flag. Extends the
#   already-present CPP PublishRegistry seam; byte-identical inert in the public
#   edition (registry empty -> list_providers()==[]). DROPPED the 5 concrete/
#   self-install files (aim_update/markbin_client/markbin_provider/chorus_provider/
#   artifactory_provider — absent, companion-side) + their test classes; ported
#   only the ABC-default assertion + generic picker test. Fork commit ec2efa17.
# 4d279fd0 [backend] PARTIAL fix(config): parse orchestrator + watchdog in
#   load() — _safe_float helper + guarded section locals + orchestrator=/
#   watchdog= kwargs (defaults byte-match, config.json was silently ignored).
#   DROPPED the dashboard-theme hunk (theme_mode/theme_color/onboarded already
#   parsed fork-side since rename #2). +TestOrchestratorWatchdogThemeAreParsed
#   (5 methods). Fork commit 5742f2e1.
# 6e74cb1f [backend] KEEP feat(skills): rubber-duck adversarial review skill —
#   single top-level skills/rubber-duck/SKILL.md (153 lines), Amazon-neutral;
#   one symbol-map edit (injected MeshClaw context -> KiroCrew). Sits beside
#   llm-council/grill (unmanifested runtime project-dir tree). Fork commit 449c974f.
# 84359f45 [backend] KEEP feat(terminal): per-tab cwd, persistent PTYs, live
#   titles, session cap — _resolve_cwd(?cwd=), _MAX_SESSIONS 3->12,
#   _ORPHAN_TIMEOUT_S 5->15min, poll_terminal_titles ({type:'title'} frames off
#   the loop via subprocess_executor), per-session send_lock. 5 files + 83 tests.
#   FORK-GATE: the 4 new POSIX-PTY controller-fd references carry an inline
#   wokeignore directive (upstream-verbatim load-bearing field predating the
#   fork woke gate; renaming would diverge). Pairs frontend 311afea. Fork commit 04f4be6b.
# 614aa3c [frontend] KEEP feat(publish): hint install-on-first-publish — FE half
#   of b9120339; PublishProviderDescriptor optional available?: boolean + share-
#   panel hint when selDesc?.available === false. Standalone-inert (endpoint never
#   emits `available`). Fork commit 53faec1c.
# 311afea [frontend] KEEP feat(activity-bar): chat-specific terminal tabs +
#   tab/panel persistence — 16 touched files (2 deleted: useTerminalWs.ts,
#   terminalSlice.ts). usePanelTabs module store (mc-panel-tabs:) + openTerminal +
#   MAX_TERMINALS_PER_CHAT; terminalRegistry persistent WS + title store; CliPanel
#   thin embedded view; chatSlice per-chat activityOpen (mc-activity-open:);
#   ChatPage mc:run-in-terminal + cold-tab hydration + AnimatePresence
#   initial={false}; App.tsx retires the floating terminal + capsule pulse. Fork's
#   'artifacts' ViewKind + no-secretary kept; mc-* literals verbatim. Pairs backend
#   84359f45. Fork commit ef1941c8.
# cfa63fc [frontend] KEEP fix(channels): guard IME composition Enter in channel
#   MentionInput — useImeGuard {...composition} + !isComposing(e) on both Enter
#   branches + reset() on Escape. Channels hide mechanism untouched (KEEP, not
#   SKIP_FORKUX — repairs a real bug in shipped code). +ChannelPage.ime.test.tsx
#   (5 tests, agent_name->kirocrew, Mesh-2693 verbatim). Fork commit 4f1e4d73.
# LEFT OUT (backend window non-keepers):
#   820b1b01 SKIP_INTERNAL (app-registry pipeline-health — fork registry is [],
#     internal KiroClawApp-PipelineHealth GitFarm app).
#   f123cc3f ALREADY_PRESENT by architecture (blob-heuristic redacting internal
#     Amazon URLs — the fork's exfil logic diverged to a companion-supplied
#     exact-host exemption with NO _SAFE_DOMAIN_SUFFIXES branch, and already skips
#     the blob/length heuristics for exempt hosts; nothing to fix).
#   8fb38539 DEFER (reap probe process groups + positively ID escaped aim trees —
#     substantial generic-security PARTIAL touching mcp_discovery/session_pid/
#     constants/sandbox + companion-side claude_code/mcp_gateway/aim hunks; needs
#     its own careful PARTIAL triage, not a tail-of-batch port).
#   cbd594fa ALREADY_PRESENT (tool_search built-in in config/defaults.json — the
#     commit itself says "Mirrors the KiroCrew fix"; fork line 17 already has it).
#   a39bffa5 + bb3901c8 SKIP_INTERNAL (v3.3.0 CHANGELOG folds — fork is
#     independently versioned 0.1.0).
# Boundary: beta -> bb3901c8 (backend tip; covers all 5 keepers + the DEFER/
#   SKIPs), frontend-beta -> cfa63fc (beta-braveheart tip; all 3 window commits
#   ported). mainline unchanged (45dcb603).

# --- Batch-48 (backend-only sync 2026-07-20) ----------------------------------
# Branch sync/frontend-ux-2026-07-19 (continues open PR #46 after batch-47; the
# PR #65 main-merge is committed at a823fb16). Backend window 38cb05ef..42ad7707
# (6 commits triaged this batch), all decided by DIFF against fork content at
# HEAD a823fb16. Frontend: ZERO frontend commits triaged this batch, so
# frontend-beta is UNCHANGED (one new upstream FE commit 614aa3c exists but
# pairs the untriaged backend straggler b9120339 — deferred to the next run).
# 836644ff [backend] PARTIAL: schema-gate the 6 workflow_* tools in
#   MCP_CORE_SCHEMAS (workflow_author/run/status/result/cancel/rerun_subtree) —
#   they validate args internally but were unregistered, so a missing arg raised
#   ValidationError out of the unguarded inner dispatch. DROPPED the 9 artifact
#   remote/share tools (absent subsystem: no schema constant, no handler) and the
#   5 comment tools (ALREADY registered via PR #54). +test_mcp_core_arg_crash.py.
# c9600704 [backend] KEEP: threading.RLock on SimpleDiGraph (knowledge/store.py)
#   so a cross-thread search (mc-embed executor) can't hit "dictionary changed
#   size during iteration" while ingest/dedup mutates the same graph on the loop
#   thread; iterating readers snapshot under the lock. +cross-thread test.
# 7d89725f [backend] KEEP: carry image_temp_paths through the Slack queue kwargs
#   on all 3 enqueue branches + defer _cleanup_image_temps to _dispatch_queued
#   (single shared finally wrapping BOTH the fork's transport-return and native
#   dispatch paths, not upstream's native-only finally) so queued messages don't
#   silently lose their image attachments. +TestQueuedMessageImagePaths.
# 23bf26bb [backend] KEEP: wire mcp_gateway read_buffer_limit_bytes /
#   response_spill_threshold_bytes config keys to the runtime resolvers in
#   pool.py (were dead — env-only); precedence env->config->default. Test methods
#   appended to the existing test_mcp_gateway_oversize.py::TestConfigOverrides.
# 4a621332 [backend] KEEP: finish the 90e3cccc ~/.kiro/agents hardening — add an
#   isinstance(data, dict) guard in agent.py _enforce_denied_commands (non-object
#   JSON root would AttributeError out of the loop, aborting deniedCommands
#   enforcement) + add UnicodeDecodeError to slack/events.py _get_agent_names
#   (non-UTF-8 ._foo.json killed the /kirocrew channels handler). +2 regression
#   tests. ~/.kiro paths kept VERBATIM (kiro-cli dir, not .kirocrew).
# 42ad7707 [backend] PARTIAL: guard non-string tags at the discover.py handler
#   (a non-list/None/non-string tag from any provider could TypeError and 500 the
#   whole /api/skills/-/discover response). Layer-1 SkillsShProvider.search
#   coercion is ALREADY_PRESENT (batch-38 _s() hardening) — skipped.
#   +TestSkillsShTagsCoercion.
# Untriaged backend stragglers ABOVE 42ad7707 (next run): b9120339 (publish
#   self-install markbin-mcp), 5db73730 (knowledge oversized-file guard),
#   f123cc3f (security blob-heuristic Amazon-URL redaction), 820b1b01 (apps
#   pipeline-health registry). SEVERITY drift-note for 836644ff recorded in
#   left-out.md (POSIX worker-thread backstop already converts the raise to an
#   Error string; the literal server-crash framing is Windows/non-kiro-host only).
#
# --- Batch-47 (dual-repo sync 2026-07-20) -------------------------------------
# Branch sync/frontend-ux-2026-07-19 (continues open PR #46 after batch-46,
# HEAD 2c9e4a80 after the embeddings port). Window backend 808f7f74..38cb05ef
# (3 commits: 85ff0a87 KEEP user-directed, 1f4ea490 KEEP, 38cb05ef PARTIAL) +
# frontend 4e520bc..f20f5fa (2 commits: 5b93d06 KEEP pairs 85ff0a87, f20f5fa
# KEEP), all decided by DIFF against fork content.
# 85ff0a87 [backend] KEEP (USER-DIRECTED, overrides the fork's old OSS-Ollama
#   default): in-process embeddings via vendored llama-cpp-python 0.3.34 (MIT,
#   kiro_crew/_vendor), always-on coercion (legacy "ollama"/"none" ->
#   "llama_cpp"), EmbeddingBackend ABC swap seam, background daemon-thread
#   model download, LRU cache. THE ONE STRUCTURAL ADAPTATION: model transport
#   swapped from upstream's internal Gitfarm git+LFS fallback (git.amazon.com +
#   mwinit — forbidden coupling) to HTTPS-CDN-ONLY: KIROCREW_EMBED_MODEL_URL
#   env > memory.embed_model_url config knob > _DEFAULT_MODEL_URL on the
#   KiroCrew CloudFront CDN (ops must upload the GGUF under models/). Plain
#   urllib — no boto3/AWS SDK. All transport-agnostic robustness kept
#   upstream-faithful: sha256 pin, retry ladder with backoff, daemon-thread
#   download, atomic os.replace into ~/.kirocrew/models/,
#   KIROCREW_SKIP_MODEL_DOWNLOAD=1 escape hatch (test conftest sets it),
#   Ollama-blob salvage, graceful keyword degrade + lazy rebind. Doctor
#   git/git-lfs checks replaced with an HTTPS-reachability probe. Dropped:
#   Brazil Config, Gitfarm fallback + its tests, absent-in-fork test/doc
#   files. Fork commit 3ace4a98.
# 5b93d06 [frontend] KEEP: always-on embeddings dashboard (pairs 85ff0a87) —
#   Disable button dropped (backend 410), Ollama UI references removed,
#   download_step/bytes_* progress fields adopted. Fork adaptations: typed
#   EmbeddingStatus kept, vectorDisableEmbeddings kept in api/client.ts.
#   Fork commit 2c9e4a80.
# 1f4ea490 [backend] KEEP: evaluate the raw shell command in the tool security
#   gate, not the LLM-authored title — AcpEvent.shell_command property
#   (raw_tool_params OR tool_input JSON), on_tool_call kw-only command=/
#   is_shell= with deny-by-default for unrecoverable shell commands,
#   security_targets loop (sensitive-path/bash/exfil vs title AND command),
#   deny list reconciled through the fork's PolicyAuthority (not bare
#   security.is_denied); six upstream call sites + THREE fork-only enforcing
#   gates (slack/telegram/wechat transport_dispatch _tool_gate) threaded;
#   Slack EVENT_TOOL_CALL stays informational (no deny-by-default,
#   flagged_unenforceable + title redaction); chat_runner deny-title
#   redaction was ALREADY_PRESENT. Tests TestToolCallEvaluatesRawCommand +
#   TestShellCommandProperty; security.md spec same commit. Fork-only
#   raw_params keystone + write-protected-config blocks kept outside the
#   per-target loop. Fork commit 938b76f5.
# 38cb05ef [backend] PARTIAL: release-cut commit — DROPPED the 3.2.3->3.3.0
#   version bump (fork independently 0.1.0) and the internal v3.3.0
#   CHANGELOG entry. PORTED the three folded-in generic fixes: cli_doctor
#   3-way embeddings runtime check (unsupported platform = designed
#   degradation, not an issue; folds onto the 85ff0a87 port) + cli.md spec +
#   test_cli.py hermetic fixtures/platform_supported kwarg/new unsupported-
#   platform test; test_sandbox_argv.py RLIM_INFINITY branch (macOS
#   unlimited-hard hosts); test_sandbox_backend_cache.py 8 hermetic
#   sb.sys platform="linux" pins. test_steer_requeue.py isort hunks
#   ALREADY_PRESENT by content. Fork commit 13de89a1.
# f20f5fa [frontend] KEEP: show cancel errors on the Logs tab instead of
#   failing silently — JobLogsView cancelError prop + render anchor under the
#   running banner, SchedulePage threads panelError + clears it on each
#   cancel attempt, new JobLogsView.cancelError.test.tsx (fork adaptation:
#   unused waitFor import dropped for the eslint warning cap; agents={agents}
#   divergence left alone). Fork commit c86836fe.
# Backend boundary advances to the beta tip 38cb05ef; frontend to f20f5fa.
# Mainline: unchanged (45dcb603).

# --- Batch-46 (dual-repo sync 2026-07-20) -------------------------------------
# Branch sync/frontend-ux-2026-07-19 (continues open PR #46 after batch-45,
# HEAD 1d32e11c). Window backend d22bc6a0..808f7f74 (3 commits, all KEEP, one
# fork commit each) + frontend b6561a90..4e520bc (1 commit, KEEP), all decided
# by DIFF against fork content. 808f7f74 was EXPLICITLY USER-REQUESTED.
# af42492f [backend] KEEP sandbox: CPUWeight=50 on agent scopes, opt-in
#   CPUQuota, oom_score_adj=1000 on children — generic Linux resource
#   hardening: _CGROUP_DEFAULT_CPU_WEIGHT=50 + _cpu_controller_delegated()
#   (cached user-slice cgroup.controllers read), _cgroup_limits_from_config
#   widened to a 4-tuple (cpu_weight 1..10000, max_cpu_percent opt-in),
#   cgroup_scope_argv emits CPUWeight (+ CPUQuota when configured) only when
#   the cpu controller is delegated; security.py _bias_child_oom_score()
#   (Linux-gated /proc/self/oom_score_adj=1000, async-signal-safe) called at
#   the end of the RLIMIT preexec. Fork slice name kirocrew-agents.slice kept;
#   grok-build/OpenClaw comparison prose kept verbatim. Tests: 3 new CPU tests
#   pure-mock + 4 oom tests (real-spawn one Linux-skipif); the real-systemd
#   cgroup test keeps its probe->pytest.skip self-gating (CI-ENV safe). docs/
#   resource-protection.md CPUWeight bullet + oom paragraph. No config-schema
#   impact (resource_limits is a raw-config block). Fork commit e72f1099.
# b8f82ffd [backend] KEEP feat: enable reasoning effort for GPT models on kiro
#   backend — effort.py gpt in the capability allowlist + effort_settings_key()
#   (output_config for Claude, reasoning for GPT); providers/acp.py overlay
#   write under the family-correct key + stale-other-family removal, clear/read
#   sweep both keys. Fork adaptations: acp-only test parametrizes kept, no
#   claude_code hunks re-added; providers.md rewritten onto the fork's merged
#   kiro-only paragraph (no "both backends"/CLAUDE_CODE_EFFORT_LEVEL).
#   Fork commit 5becef1a.
# 808f7f74 [backend] KEEP (USER-REQUESTED) feat(session): SessionWatchdog
#   container + RSS-threshold recycle — new watchdog.py (CleanupHook +
#   SessionWatchdog dumb dispatcher); session.py wires 4 hooks (idle_expiry/
#   orphan_mcp/denied_commands/rss_threshold), cleanup loop calls
#   _watchdog.tick(), inline idle+MCP+deniedCommands blocks removed (orphan-PID
#   /session-root/sandbox sweeps stay inline per CR1); reset() gains
#   expect_session/skip_if_busy guards + bool return (fork-only CC
#   _proc/_active_proc PID fallbacks preserved); _RecycleCallback +
#   set_recycle_callback/_fire_recycle_callback; session_pid.py /proc BFS RSS
#   helpers (_PAGE_SIZE/_read_rss_pages/_build_child_map/_rss_mb_from_tree/
#   get_session_rss_mb — raw /proc is the faithful port; platform_compat
#   proc_rss_bytes is RUSAGE_SELF-only); SessionConfig.watchdog_rss_max_mb=0
#   (ships dark) + config-baseline.json REGENERATED via
#   scripts/generate_config_baseline.py (the fork's equivalent of upstream's
#   baseline hunk — nothing dropped); dashboard state.py
#   _SESSION_RECYCLED_NOTICE + wire_session_recycle_callback (recycling-symbol
#   emoji kept verbatim — backend chat-notice parity; no-emoji rule is
#   frontend-only) wired at BOTH server.py start paths; tests test_watchdog.py
#   (102 lines) + test_session_rss.py (403 lines) regenerated from post-image
#   (fake proc_root/tmp_path — no NEWNS/cgroup, CI-safe); specs session.md +
#   config.md same commit; AGENTS.md New Modules gains watchdog.py.
#   PROVENANCE NOTE: the task prompt cited CR-290401003 but the commit's own
#   cr: trailer is CR-284479397 — both recorded in the PR provenance.
#   Fork commit 03a07d26.
# 4e520bc [frontend] KEEP feat: show reasoning-effort dropdown for GPT models —
#   frontend pair of b8f82ffd (same CR-290404954): lib/effort.ts
#   modelSupportsEffort gains || m.includes('gpt') + allowlist doc comments;
#   test it-block renamed + GPT capable / minimax+glm incapable asserts.
#   Fork adaptations: REASONING_EFFORT_PROVIDERS stays new Set(['acp'])
#   (upstream shows claude_code only as context — NOT re-added, fork acp-only
#   provider test untouched). Fork commit bfc8f546.
# Backend boundary advances to 808f7f74 (NOT the beta tip): 85ff0a87
#   (in-process embeddings via vendored llama-cpp-python) and its frontend pair
#   5b93d06 (embeddings dashboard) remain UNTRIAGED for the next run.
# Mainline: unchanged (45dcb603, re-confirmed batch-40).

# --- Batch-45 (backend-only sync 2026-07-20) ----------------------------------
# Branch sync/frontend-ux-2026-07-19 (continues open PR #46 after batch-44,
# HEAD 88d59843 + Codex-notes commit c217c81f). Window backend
# efd8988b..d22bc6a0 (2 commits, both KEEP, one fork commit each, decided by
# DIFF against fork content):
# 873d1130 [backend] KEEP Close the Mesh-2829 otel degrade gap; add pip-deps
#   AST guard test — the fork had the exact pre-fix bug (provider.py imported
#   local_exporter before the try/except; recorder.py had an unguarded runtime
#   `from opentelemetry.metrics import ...` that is AST-verified
#   annotation-only). recorder.py import moved under TYPE_CHECKING (upstream
#   comment verbatim); local_exporter import moved inside the guard +
#   JsonlMetricExporter=None in the except arm; fork's rewritten guard
#   paragraph KEPT, upstream's new second paragraph appended. New
#   test/test_pip_deps_consistency.py regenerated from post-image with 3 fork
#   adaptations: (a) Brazil-Config sentence de-Amazoned, (b) "amzn" _EXEMPT
#   entry dropped (zero `import amzn` in fork), (c) "deploy/skills/" added to
#   _EXCLUDED_SUBTREES (fork-only reaper-Lambda payload; boto3/botocore come
#   from the AWS Lambda runtime); unused pytest import dropped (fork F401).
#   Existing flag-patch test test/metrics/test_provider.py::
#   test_degrades_to_noop_when_otel_missing is complementary — left in place.
# d22bc6a0 [backend] KEEP fix(steer): requeue unconsumed mid-turn steers when
#   the turn dies — _ChatSlot._pending_steers (state.py __slots__ + init +
#   10-line comment), chat_handlers steer path registers BEFORE the await with
#   an unwind on failure (zedmor/CR-290015501 comment verbatim) + force-stop
#   clears pending steers alongside _queue, chat_runner gains
#   _settle_consumed_steers (count-aware <user_message> equality settle) +
#   _requeue_unconsumed_steers (requeue at queue HEAD + queue_push broadcast)
#   wired at EVENT_STEER_CONSUMED and in _run_chat's finally before the queue
#   drain. test/test_steer_requeue.py regenerated from post-image (16 tests,
#   incl. TestProductionWiring source-order asserts). All 4 source hunks
#   applied; nothing dropped; no brand tokens in source hunks.
# Frontend: ZERO new frontend commits at triage time — frontend-beta boundary
#   UNCHANGED at b6561a90. Two commits (4e520bc reasoning-effort dropdown for
#   GPT models, 5b93d06 always-on in-process embeddings dashboard) landed
#   post-triage on 2026-07-20 05:38/05:51 UTC and remain UNTRIAGED for the
#   next run (they pair with backend b8f82ffd/85ff0a87, also untriaged).
# Mainline: unchanged (45dcb603, re-confirmed batch-40).

# --- Batch-44 (dual-repo sync 2026-07-20) -------------------------------------
# Branch sync/frontend-ux-2026-07-19 (continues open PR #46 after batches 42+43,
# HEAD 4e3fe25d). Window backend 53214b50..efd8988b, frontend
# 2e67ddb0..b6561a90. 4 keepers ported, one fork commit each, all decided by
# DIFF against fork content:
# f2661c8a [backend] KEEP fix(sandbox): stop caching transient backend-probe
#   failures (1h fail-closed outage) — transient errno classification
#   (EAGAIN/ENOMEM/... never cached), prewarm_backend() boot hook in
#   slack/gateway.py + mcp_gateway/gatewayd.py, never-block-on-loop probe,
#   probe reason in the fail-closed RuntimeError + SEL denial. Port stops
#   before userns_available() (fork lacks jail.py, 5d99a8d4 SKIP_INTERNAL);
#   21 tests in test_sandbox_backend_cache.py (upstream msg says 20); two
#   unused test locals dropped for the fork's stricter bare flake8 (F841).
# efd8988b [backend] KEEP fix(mcp-gateway): ping-gated wedge detection
#   (two-condition rule + 2100s hard ceiling + warn-once slow log) +
#   notifications/cancelled receiver + ping answered while tool in-flight +
#   wait tool cooperative cancel. Fork adaptations: caller-less 2-arg
#   call_tool_fn kept, KIROCREW_SESSION_KEY, excluded-tools branch stays
#   synchronous, worker-thread interleave gated on platform_compat.IS_POSIX
#   (select() cannot poll stdin on Windows — recorded in left-out.md);
#   12 tests in test_mcp_gateway_wedge_ping_gate.py.
# 109b0d3e [frontend] KEEP fix(dashboard): allowlist-based HTML sanitize
#   (ALLOWED_TAGS + escaped discrete-text-node reconstruction +
#   [unsupported:] collapse for executable tags; strictly strengthens the
#   denylist rehypeSanitize it replaces — React #290 white-screen fix) +
#   MessageErrorBoundary wrapping every chat MarkdownRenderer (ChatMessageList,
#   ChatPage renderUserContent + inject bubble, AssistantMessage — fork prop
#   lists kept). 16 sanitize tests; existing XSS tests stay green.
# b6561a90 [frontend] KEEP fix(chat): composer text no longer lands in the
#   wrong session under lag (Mesh-2908) — createSlot.fulfilled switched-away
#   guard via fulfillWithValue meta, send() routed through
#   uiSlot=activeSlotRef, composerSlotRef keying the three persist effects
#   with a declared-last advance effect. Fork adaptations: 7-arg
#   createChatSlot kept, no clearComposer opt, 3-reducer test makeStore.
# No SKIP/DEFER this window. efd8988b and b6561a90 are the current
# beta-braveheart tips of their repos.

# --- Batch-43 (dual-repo sync 2026-07-19, second window of the day) ----------
# Branch sync/frontend-ux-2026-07-19 (continues open PR #46 after batch-42,
# HEAD f0ef4ade). Window backend 3f83fa15..53214b50 (f2661c8a backend-probe
# cache tip left for the next run), frontend b1ffd248..2e67ddb0. 5 keepers
# ported, one fork commit each, all decided by DIFF against fork content:
# 32225cc2 [backend] PARTIAL fix(config): to_dict() omitted knowledge/heartbeat/
#   watchdog so save() (incl. the load() migration write-back) silently deleted
#   hand-written sections. Ported 3 of 4 hunk lines + the full
#   TestSaveRoundTripPreservesAllSections class; the snapshot_dir hunk line was
#   DROPPED as already present fork-side (regression test keeps it in the
#   4-key guard tuple). Verified new tests fail with the loader edit reverted.
# c28619b5 [backend] KEEP fix(auto-research): _cycle_finding_files() tolerant
#   cycle-filename matcher (near-miss names, number-sort, dedup-by-cycle)
#   replacing the 3 strict glob sites + worker-prompt RECOVERY clause and
#   HARD-contract filename warning + 7 TestCycleFileMatching tests (incl. the
#   security invariant that near-miss findings still route through
#   _redact_finding). 148 -> 155 passed.
# c3732ac0 [backend] KEEP fix(dashboard): immutable Cache-Control under
#   /assets/ now gated on status in (200, 206, 304) — a cold-start 404/503
#   was cached immutable for a year (permanent black screen). +2 tests.
# 2b3c722d [backend] KEEP feat(sandbox): seccomp kill(-1) broadcast guard +
#   KIROCREW_HOST_PID export + host-pid resolver shortcuts (mcp_core/
#   mcp_caller/mcp_shared) + gatewayd server-side SO_PEERCRED peer-identity
#   resolution with SEL audits + claim-noop loud ack + spawn_run orphan
#   warning. Fork adaptations: _FROM_ENV_CACHE flow, inline mcp_shared walk,
#   2-arg _call_tool, inline CI-env gating (sys.platform + _probe_unshare
#   fails closed on GH Actions NEWNS EPERM, stderr skip covers both unshare
#   messages), new test/test_mcp_caller.py with cache reset. Partially
#   mitigates inherited finding 56fbb774 (left-out.md row updated: HIGH->MED,
#   claim frames still uid-socket-trusted, stays open upstream-first).
# 53214b50 [backend] KEEP fix: session_host_preexec() raises NOFILE to the
#   inherited hard limit for the two trusted ACP session-host spawn sites
#   (acp/client.py, acp/runtime.py) — the 1024 tool-cap caused EMFILE crashes.
#   All other spawns keep resource_limit_preexec; _PREEXEC_TOKENS tuple in
#   test_spawn_audit; +3 TestSessionHostPreexec tests; resource-protection.md
#   updated (fork spec rule).
# 2e67ddb0 [frontend] KEEP fix(sw): shell-only SW cache (no more caching
#   502/503s or returning undefined from respondWith), skip /apps//vendor/
#   /fonts//sprites/, stable build-time cache version via new swVersionPlugin
#   (%%SW_BUILD_HASH%% -> version+git-sha; verified injected in dist/sw.js).
# No SKIP/DEFER this window. f2661c8a (backend-probe cache, the current beta
# tip) postdates this batch's resolved set — boundary stays at the newest
# RESOLVED sha 53214b50 per convention; f2661c8a re-scans next run.

# --- Batch-42 (frontend-UX defer rescue + dual-repo sync 2026-07-19) ----------
# Branch sync/frontend-ux-2026-07-19 off origin/main c817dbaa (batch-41 merged as
# PR #42; PR #18 batches 38-40c and PR #14 artifacts-mirror already in main).
# 12 port commits, one per upstream keeper, all decided by DIFF against fork
# content. This batch EXECUTED THE USER-AUTHORIZED FRONTEND-UX DEFER RESCUE:
# the 08e28b8a Electron shell redesign (unblocked by merged fork PR #14) plus
# its chained riders, and the PR #14-adjacent artifact-comment DEFERs, alongside
# the normal boundary window (backend cd1cc045..3f83fa15, frontend
# 082f45d5..b1ffd248). Verdicts (one line each; window commits AND rescues):
# 3afa65a5 [backend] PARTIAL fix(acp): classify kiro stream errors so transient
#   5xx retries fire — both AcpSessionHandle raise sites (_wait_for_response,
#   _dispatch_events) now pass transient=_is_transient_raw_error(msg.error) so
#   the chat_runner/llm_helpers retry ladder fires on mid-stream 5xx; 3
#   regression tests ported. The test_cron_cancel.py hunk DROPPED as
#   already-present (fork already patches wrap_argv/cgroup_scope_argv to
#   identity). CR-290307657.
# 337b2fcc [backend] PARTIAL test(identity): shared ProcessTopology fixture +
#   pid-view strict-xfail archive + session_pid call-site registry guard
#   (test-only; archives the 24c320f6 pid-ns incident the fork also
#   ported-then-reverted in ab96394b). Fork adaptations: _FROM_ENV_CACHE reset
#   in _wire_common (XPASS guard under xfail_strict), _CURRENT_CALLER wrapper
#   dropped (no contextvar in fork mcp_core), walk copy 3 rewritten against the
#   inline mcp_shared._resolve_excluded_tools walk, 2 sync/* registry entries
#   dropped (dir absent). CR-290279554.
# 22fa7042 [backend] KEEP fix(session): verify pool-discard kills and reap
#   surviving providers — _discard_pool_provider at all 4 pool discard sites
#   (bounded shutdown + pid_exists verification + hard-kill fallback),
#   AcpRuntime.kill() reaps after SIGKILL and only untracks a dead PID,
#   _sync_kill_provider refuses non-int/pid<=1 targets; 12 new tests. Fork-only
#   unregister_protected_pid moved into the process-dead branch. CR-290312797.
# 3f158792 [backend] KEEP sandbox: cgroup TasksMax default 1024 -> 8192
#   (pids.max counts tasks/threads; 1024 starved JVM build trees). security.py
#   hunk is comment-only — keystone path policy untouched. CR-290112454.
# 3f83fa15 [backend] KEEP fix(file-explorer): granular .kirocrew sensitive
#   policy — blanket SENSITIVE_DIRS block replaced by _KIROCREW_SAFE_SUBDIRS
#   deny-by-default (workspace/uploads/skills/... reachable; keys/tokens/DB
#   still blocked). KEYSTONE: "profiles" deliberately NOT in the safe set;
#   fork-only tests pin security_policy.json / profiles/ /
#   admission_policy.json at 403. CR-289887448.
# b1ffd248 [frontend] KEEP fix(models): AcpAdapter.fetchAvailableModels static
#   registry fallback — try/catch + displayModels() on error/non-array/empty so
#   a gateway-restart /api/models timeout no longer strands the picker on
#   'auto'; providers/modelRegistry.ts recreated SLIM (displayModels only; the
#   'claude_code' literal is the model_registry.json namespace key, not a
#   selectable provider). CR-290350714.
# 08e28b8a [frontend] PARTIAL (RESCUE — batch-40 DEFER, unblocked by merged
#   PR #14) Electron shell redesign — de-tabbed BaseWindow shell, tabbed
#   SidePanel (usePanelTabs/usePersistedBool), unified top-bar capsule, and the
#   BaseWindow reload/zoom/DevTools repairs (role: items no-op on BaseWindow;
#   explicit focusedDashboardWC handlers). Folds rider 23f5a314 (SidePanel root
#   shrink-0). DROPPED: mwinit capsule segment (Midway stubbed), usageMode/
#   costData Claude-cost branch (SKIP_NONKIROACP), Artifactory Share menu
#   restyle (absent subsystem). FORK-UX intact (SHOW_ARTIFACT_ITERATE /
#   Channels hide / Board removal / Piper untouched). CR-289605811 (rider
#   CR-290332522).
# 082f45d5 [frontend] KEEP (RESCUE — ported LAST, on the 08e28b8a inset stack)
#   flush header logo when instance tab bar is topmost on macOS —
#   visibleInstanceTabs() export shared with App.tsx, mac-instancebar-inset
#   root class + 2 index.css rules; supersedes batch-41's "no anchor" N/A.
#   CR-290355334.
# cd6730f [frontend] ALREADY_PRESENT (re-audit of the batch-18 DEFER) masonry
#   artifact gallery — the fork is AHEAD via merged PR #14 (+ ef98477/edb11ea/
#   a5609d4/20f1881/4ab933e follow-ups already present); the absent
#   publishedOnly filter + CopyLinkBox renders are the recorded PR #14
#   publish-behind-seam strips, not missed generic hunks. DEFER closed, nothing
#   to port. CR-281375038.
# 0000f561 [frontend] PARTIAL — prod hunks (startOffset anchor + bestScore/
#   bestDist tiebreak) ALREADY_PRESENT via PR #14; ported the missing
#   rangeForAnchor occurrence-matching specs. CR-286616018.
# f746d60 [frontend] PARTIAL — prod hunk (collapse empty comment sidebar +
#   sidebarUserToggledRef nav reset) ALREADY_PRESENT via PR #14; ported the 3
#   sidebar regression tests + the artifactComments mock-leak reset;
#   RemoteArtifactDetailPage hunks stay dropped (Harmony/Chorus remote surface
#   stripped). CR-288179503.
# 86315dc [frontend] PARTIAL — prod hunks (orphaned-anchor warn + comment
#   activity events) ALREADY_PRESENT via PR #14; ported the new
#   CommentsSidebar.test.tsx orphaned-anchors describe (Mesh-2752) + the
#   activity-timeline comment-events spec. CR-288819773.
# 475146ca [backend] PARTIAL — store/HTTP halves ALREADY_PRESENT via PR #14
#   (with fork-side hardening kept: _run_off_loop + server-side reason
#   redaction); ported the agent-facing surface: 5 comment MCP tools in
#   mcp_core (folding the a84eabc4 base four + de64d07b full-body/anchor
#   formatting by content — their old "subsystem absent" skip reasons went
#   stale post-PR #14), validation schemas, the comment-triage rubric in
#   skills/artifacts/SKILL.md, and the spec's Comments & Lifecycle section.
#   artifact_update_comment (a5979e42) deliberately NOT ported — separate
#   un-ported row. CR-288819773.
# Sweep note (outside this batch's verdict set): one still-open frontend DEFER
#   was surfaced — 4964a3e (usage popup real credit total/overage, batch-30,
#   recorded only in the batch-30 block below) is now largely ALREADY_PRESENT
#   by content (fork-native PR #20 kiro_usage_api emits credits_overage +
#   percentage; App.tsx already computes overage); flagged for a re-audit/close
#   next batch, with at most a one-line adapter preference for the backend
#   percentage.
# Boundary: beta -> 3f83fa15, frontend-beta -> b1ffd248, mainline unchanged.
# Verify: per-commit gates green (pytest touched files via .venv311; CI-parity
#   bare flake8 + isort 6.0.0 + mypy 1.14.1; frontend tsc -b + touched vitest;
#   woke on added lines: no findings).
#
# ── Prior batch ──────────────────────────────────────────────────────────────
# --- Batch-41 (2026-07-19) -----------------------------------------------------
# Frontend-only batch, one fork commit per keeper, triaged by content. Intended
# for the standing branch sync/beta-2026-07-18 (PR #18), but PR #18 MERGED at
# 2026-07-19T04:52Z before this batch could push — shipped instead as a small
# follow-up PR off main carrying only the batch-41 delta. Backend had 0 new
# candidates this run (beta unchanged at cd1cc045; mainline unchanged). Tally:
# 1 frontend PORTED + 1 ALREADY_PRESENT + 1 DEFER (no anchor — chained onto the
# 08e28b8a left-out.md entry, now UNBLOCKED by merged fork PR #14). Verdicts:
# c64e203a KEEP fix(chat): don't freeze a still-streaming reply when switching
#   back to a running slot — switchSlot.fulfilled's re-attach branch now gates
#   the streaming->assistant coercion on !running so a mid-flight background
#   reply resumes into the same bubble instead of splitting across two; fork
#   pre-image was byte-identical, verbatim port + the upstream regression test
#   (fails pre-fix, passes post-fix). CR-290351637.
# 23f5a31 ALREADY_PRESENT (by architecture) fix(chat): pin activity panel width
#   — the fork has no pages/chat/SidePanel.tsx (tabbed side panel never ported);
#   its activity panel renders via components/DetailPanel.tsx whose outer motion
#   wrapper AND inner panel div already carry shrink-0 (no min-w-0 shrinkable
#   pre-image), so the mid-animation reflow bug cannot occur. CR-290332522.
# 082f45d DEFER (no anchor) fix(instances): flush header logo when instance tab
#   bar is topmost on macOS — relocates the 84px macOS traffic-light clearance,
#   but the fork has NO traffic-light inset stack at all (no mac-electron /
#   mac-fullscreen CSS, no isMacElectron/onFullScreenChanged wiring, no 84px
#   inset in index.css): that stack rides with the DEFERred Electron shell
#   redesign 08e28b8a. Fork PR #14 (ArtifactPanel.tsx) has MERGED, so 08e28b8a
#   is now UNBLOCKED — 082f45d5 is chained onto that left-out.md entry to be
#   ported together in the dedicated 08e28b8a batch (082f45d5 last). Until then
#   its App.tsx half cannot compile and it fixes a bug the fork does not have
#   (header already flush, no inset). The fork's InstanceTabBar keeps its inline
#   tabInstances filter (the visibleInstanceTabs export only exists to share the
#   rule with the absent inset logic). CR-290355334.
# Boundary: frontend-beta -> 082f45d (beta-braveheart tip; all 3 window commits
#   resolved). Verify: tsc -b clean; chatSlice.test.ts 165 pass (new test fails
#   on pre-image); woke on added lines: no findings.
#
# ── Prior batch ──────────────────────────────────────────────────────────────
# --- Batch-40c (2026-07-19) ----------------------------------------------------
# Frontend-only addendum onto the SAME branch sync/beta-2026-07-18 (open PR #18),
# one fork commit per upstream commit, triaged by content. Tally: 3 frontend PORTED
# + a beta boundary correction. Verdicts (one line each):
# 507715f0 KEEP fix(a11y): keyboard access for session rows + focus-reveal for
#   action groups — role=button/tabIndex/aria + Enter/Space onKeyDown on session
#   and history rows (non-activation keys forward to dnd-kit's listener),
#   group-focus-within/focus-within reveal on IconButtonGroup + both folder
#   headers. Fork adaptations: folder headers get only the two focus tokens (the
#   Radix folder-menu rework with has-[[data-state=open]] was never ported);
#   removed 2 fork-added jsx-a11y eslint-disables the keyboard handlers obsolete;
#   history render block carries the fork pre-image (no key prop). CR-289595943.
# 2a33062f KEEP chore(chat): remove progress track indicator from chat gutter —
#   deleted ChatProgressTrack.tsx + ChatProgressTrack.outsideClose.test.tsx (the
#   only ProgressTrack test the fork had; upstream's other two never reached it),
#   removed the ChatPage scaffolding (import, currentSectionIdx, updateTopmostIdx
#   section loop, chatNavSectionsRef, scrollToNavSection, wrapper div + render
#   block). useChatNavigation KEPT (ActivityViewer consumes links/navResolving).
#   CR-290189317.
# 4aec096f KEEP feat(dashboard): split-button suggested-response pills (Mesh-2676)
#   — FollowUpBar Zap send-now split segment + sendSegmentClassName, suppressed in
#   the quickSend instant-send state; verbatim port (fork pre-image byte-identical
#   to the upstream parent) + the 6-test send-now describe block. CR-290049767.
# Boundary: frontend-beta -> 507715f0. The batch task nominated 4aec096f, but
#   2a33062f and 507715f0 are its DESCENDANTS and are resolved in this same batch
#   — the boundary sits at the newest resolved SHA per the documented convention,
#   else the next scan re-triages two already-ported commits. Only 23f5a31
#   (pin activity panel width) follows on beta-braveheart and stays untriaged for
#   the next run. Also corrected beta -> cd1cc045: batch-40b triaged cd1cc045
#   (PARTIAL, AF_UNIX test hunk) + 1fd785bc (ALREADY_PRESENT) but left the
#   boundary at 6f6d5bdf; advancing past resolved commits is the convention.
# Verify: tsc -b clean per commit; ChatSidebar board/moveToFolder specs 14 pass;
#   FollowUpBar 25 pass (6 new); full src/test vitest after the ChatPage removal:
#   271 files / 3159 pass, 3 skip; eslint on touched files: no new warnings
#   (4 pre-existing); woke on added lines: no findings.
#
# ── Prior batch ──────────────────────────────────────────────────────────────
# --- Batch-40b addendum (2026-07-19) ------------------------------------------
# Small addendum onto the SAME branch sync/beta-2026-07-18 (open PR #18), one
# fork commit per upstream commit, triaged by content. Tally: 2 PORTED (the
# ghost-cards pair) + 1 PARTIAL + 1 ALREADY_PRESENT.
# PORTED: 6f6d5bdf backend native kiro-cli subagent ghost cards — empty-task
#   skip in _native_subagent_sync, 120s staleness auto-close for cards that
#   disappeared from list_update, state._native_cards registry so DELETE
#   /api/spawn/{native:*} cancels the card (+ cancel_native_subagent SEL audit),
#   SubagentManager.spawn() empty-task guard (rejected_empty_task SEL), and the
#   _native_card_feed redaction chokepoint at all three subagent_done result
#   sites. 19 new tests in test_native_subagent_spawn.py + TestSpawnEmptyTaskGuard
#   in test_subagent.py. 7851f2af frontend half of the same bug (CR-290070213):
#   chatSlice sseSubagentSpawn copies the authoritative task onto pending cards,
#   sseSubagentDone gains a cross-slot by-id fallback + task backfill, and
#   useWebSocket spawn approvals require an explicit data.slot (no activeSlot
#   fallback); 3 regression tests + the property-test isDroppedPlaceholder
#   filter (fork's '\n[⚠' filter string preserved, upstream's '\n[WARNING:'
#   NOT copied).
# PARTIAL: cd1cc045 — ported ONLY the generic TestAbortAckLogging AF_UNIX
#   sun_path hunk into test_stop_kill_cancel.py (tmp_path → short /tmp dir,
#   macOS 104-char limit under xdist); the headline toolbox-bundler arch-gate
#   (Rosetta interpreter refusal) + its docs are internal-only — the fork's
#   packaging/build-desktop.sh already hard-fails on arch mismatch.
# ALREADY_PRESENT: 1fd785bc (spawn queue on bare-constructed managers in
#   conservative-shutdown tests) — batch-40's stop/kill port pre-resolved it;
#   fork adaptation at test_stop_kill_cancel.py:441/502 already provides
#   mgr._queue = [].
# Verify: per-commit — test_native_subagent_spawn + test_subagent (120 pass),
#   test_stop_kill_cancel (21 pass); CI-parity flake8 (bare) + isort + mypy
#   clean; frontend tsc -b clean + chatSlice/chatSlice.property vitest green
#   (169 pass). Boundary advances: beta → 6f6d5bdf, frontend-beta → 7851f2af,
#   mainline unchanged.
#
# ── Prior batch ──────────────────────────────────────────────────────────────
# --- Batch-40 (dual-repo beta sync 2026-07-18) --------------------------------
# Branch sync/beta-2026-07-18 (same open PR #18), one fix per commit, all
# decided by DIFF against fork content (not SHA). Tally: 11 backend PORTED
# (076a7a43 PARTIAL among them) + test-only PARTIAL f93e2f07 + test-hygiene
# PARTIAL 3eaa7df1 + 7 frontend PORTED; 1 SKIP_NONKIROACP; 2
# ALREADY_PRESENT; 1 DEFER; plus a fork-side review-fix commit.
# BOTH upstream REVERTS in the window were processed:
# 14fb9442 (byte-clean revert of 24c320f6 pid-namespace isolation) PORTED —
# the fork had ported the feature in batch-39, so the revert had to land or
# the fork would ship a feature upstream pulled; ce65b67a (revert of the
# 40965431 mcp-gateway Midway blue-green cutover) ALREADY_PRESENT/no-op —
# the fork never ported 40965431 (batch-39 SKIP_INTERNAL), so fork state
# already equals the post-revert state. The two batch-39 stragglers got
# explicit verdicts: 9f685806 (Skill browser modal, FE half of 31b8a10b)
# KEEP/PORTED; 08e28b8a (Electron shell redesign) DEFER — blocked on open
# fork PR #14 (SidePanel hard-imports its ArtifactPanel), recorded in
# left-out.md's human-decision table.
#
# Backend PORTED (11): 1a18a5d7 vector_memory episodic dedup skips
# tombstoned ghost vectors (silent write drop); 325fb1b9 security IMDS deny
# closes inet_aton 2-/3-part short-form IP encodings (SSRF bypass);
# e39edcb4 cron _execute no longer masks callback-reported failures as ok;
# a1933a4b subagent queue drain preserves spawn params + _force_reap drains
# the queue; 38f9ce84 mcp_core delete_message schema-gated (missing arg no
# longer kills the stdio server); efb0110b dashboard cron batch-delete runs
# remove_job on the event loop (off-loop create_task crash); 63b22b3a
# dashboard-auth reports real session_exp (numeric claim) + refresh stops
# churning link nonces (adapted: fork validates via use_session_exp=True);
# a5cf36ff cron auto-pause persisted across restarts (auto_paused field +
# record_failure/record_success + SEL); 14fb9442 revert of pid-namespace
# isolation (see above; cron-cancel work untouched); 19fa9ee8 knowledge
# namespace validate+fold in add_source + non-dict-properties 400 +
# TestWatchdog sandbox-passthrough deflake; 076a7a43 PARTIAL stop/kill
# reaches pooled MCP backends (abort.py + quarantine + runtime_info +
# stop_turn abort push; DROPPED the ClaudeCodeProvider _send_abort fallback
# hunk — provider deleted — and the absent test_mcp_gateway_pool_race.py
# hunk). Test-only PARTIAL: f93e2f07 prose-push regression tests ported
# (fork's diverged verb-anchored _git_push_args already immune — production
# hunk N/A). 3eaa7df1 PARTIAL test hygiene: base.py configurable timeout,
# _SHUTDOWN_TIMEOUT_SECS patch target fix, TestCleanupLoop sweep stubs,
# Darwin kern.maxprocperuid fold, spawn-audit scan cache (DROPPED the
# brazil-build/AGENTS.md/setup.py invariant-check + _isolate_aim_cli hunks
# — internal build machinery absent/stubbed in fork).
#
# Frontend PORTED (7): 38dfc2e9 useWebSocket dispatches
# MC_NOTIFICATION_EVENT so notification sounds play on WS frames; 964d92c9
# useSmoothStream snaps emit cursors on non-streaming content growth
# (post-completion truncation); 52a6c57b batched frames no longer fabricate
# missed-chunk markers in applyNonActiveFrame; 7ddd1b5e steer echo
# reconcile scans optimistic bubbles (duplicate steer card mid-turn);
# 5147f023 SourcesList sends uploadNamespace for local_folder; 5a51c71c
# stop-button force-stop affordance + 15s killing-state escape hatch
# (adapted: dropped the secretarySlice import — subsystem absent); 9f685806
# Skill browser modal + discover API client methods (batch-38 straggler,
# pairs with the ported skill_providers backend).
#
# SKIP (4): d80b1430 SKIP_NONKIROACP (BedrockProvider user-first history
# trim — provider deleted, no shared chokepoint touched); 4fe1de2b
# ALREADY_PRESENT (slack events session_key bound function-level at fork
# events.py:2167; regression covered by test_channel_activation.py);
# ce65b67a ALREADY_PRESENT (blue-green revert — never ported, see above);
# 08e28b8a DEFER (Electron shell redesign — blocked on PR #14, see above).
#
# Fork-side (not an upstream port): fix(review) commit addressing PR #18
# review findings — sandbox.py cleanup sweep os.kill(pid,0) →
# platform_compat.pid_exists (fork Windows platform rule); AutoNudgePopover
# text-white → text-accent-fg (restores upstream's own 15b1f9c token,
# regressed in 729e09d); TagManagerList inline-rename aria-label. Rationale
# recorded in left-out.md.
#
# Verify: per-commit — touched pytest files green (sandbox 80, cron, memory,
# subagent, mcp_core, auth, knowledge, security suites); CI-parity flake8
# (bare 7.1.0) + isort (6.0.0) + mypy (1.14.1) clean; frontend tsc -b clean
# + touched vitest specs green. Boundary advances: beta → 3eaa7df1 (the
# current beta-braveheart tip), frontend-beta → 5a51c71c (newest PORTED;
# 9f685806/08e28b8a triaged by content), mainline unchanged.
#
# ── Prior batch ──────────────────────────────────────────────────────────────
# --- Batch-39 (2026-07-18 follow-up) -----------------------------------------
# The batch-38 DEFER set (commits that landed AFTER batch-38's target set was
# fixed) triaged and ported onto the SAME branch sync/beta-2026-07-18 (PR #18),
# one fix per commit, all decided by DIFF against fork content.
#
# Backend PORTED (4): 4075a006 hyphenated destructive AWS CLI deny in is_denied
# (security.py — both underscore + hyphen forms interleaved); 9d4360d7
# knowledge thread-local sqlite connections in KnowledgeStore (Mesh-2776, fixes
# HTTP 500 on search-for-context); 24c320f6 sandbox PID-namespace isolation
# contains signal broadcasts from agent subprocesses (unshare(NEWPID)+mini-init
# + agent.sandbox_pid_namespace kill-switch + gatewayd SO_PEERCRED server-side
# identity resolution — ported ONLY the pid_namespace hunks; dropped every diff
# context line referencing the fork-absent overlay_agents_dir /
# overlay_settings_mcp_json / gateway_socket params); ed2f65b0 cron cancel
# running executions (CronService.cancel + cron_script running-proc registry +
# Popen(start_new_session) + platform_compat broadcast guard + POST
# /api/crons/{id}/cancel).
#
# Frontend PORTED (1): ec0c619 cron-cancel UI (Cancel button on Schedule page +
# detail panel + logs banner, cancelled status chip; DROPPED upstream's "3s live
# polling" interval — the fork reloads via refreshTrigger/websocket).
#
# SKIP_INTERNAL (1): 40965431 (mcp-gateway Midway blue-green cutover — Midway is
# stubbed in the fork per the de-Amazon plan; the blue-green session-migration
# glue has no public analogue). Boundary advances PAST it to the newest PORTED
# SHA 4075a006.
#
# Verify: per-commit — touched pytest files green (security 21, knowledge 5,
# sandbox+pidns 89, cron cancel/script 103 + regressions); flake8 + isort (6.0.0)
# + mypy (1.14.1) all clean; frontend tsc -b clean + SchedulePage/cron vitest
# specs green (6 + 42).
#
# ── Prior batch ──────────────────────────────────────────────────────────────
# --- Batch-38 (dual-repo beta sync 2026-07-18) -------------------------------
# Branch sync/beta-2026-07-18 off origin/main (6814cab2). 31 target commits
# PORTED IN CHRONOLOGICAL ORDER, one fix per commit, all decided by DIFF against
# fork content (not SHA). Tally: 19 backend + 12 frontend.
#
# Backend PORTED (19): 4765e2d3 tail-only fork; 85aacc60 TurnUsage nesting
# (PARTIAL, dropped CC hunk); 43243ab4 taskrunner run-scoped AcpRuntime +
# workspace_dir + parallel-cap; 363c57ab stop/interrupt idempotent (PARTIAL,
# defect-3 only, CC defects 1&2 dropped); 93e8efd7 sandbox launcher →
# ~/.kirocrew/run + cleanup sweep; aaca64c1 Knowledge LLMPool idle-TTL;
# 0492da73 lean /goal nudge; 7de4f830 mcp_gateway oversize (PARTIAL, additive
# Layer-2 spill only, kept fork's diverged Layer-1); 7aae5273 steer persist to
# JSONL; 0e5f2a00 /assets immutable cache; c906883f launcher-chmod test
# (PARTIAL, test-only — code already present); edb8c188 image-authoring skill +
# primitives; bde1a556 metrics no-op when otel missing; e6c35eb6 auth secrets →
# is_sensitive_path; 78d4af44 /api/models 503 (dropped CC branch); 31b8a10b
# skills.sh browser (dropped ALREADY_PRESENT otel setup.cfg hunk); 50eae442
# --slack-only auth parity; 90e3cccc utf-8 non-crash agent.py; 670922ac exfil
# URL no-path-query fix.
#
# Frontend PORTED (12): 2c46e44 tail-fork UI toggle; 729e09d 4 dropdowns →
# shadcn (fork-adapted: pre-cut-mode TrustDropdown, richer integration mocks);
# bad099f commentDrafts → slotDraftStore factory; 397d6a4 paste→optimizer;
# add4817 DetailPanel row-width cap (PARTIAL, dropped absent ArtifactPanel);
# 82df28c session tags in list view; 115e024 steer_push→appendSlotMessage
# (prereq); c326758 recency-tint cap 50; 3448707 render-sent + slot-switch
# reply preserve; 62bfa8e Task Runner rename + emoji→icon (PARTIAL, dropped
# workspace selector + Export-YAML — backend surfaces absent/entangled);
# 0f6cbc4 Recent session filter; fdd8734 flat view.
#
# DEFER (landed in the window AFTER this batch's target set was planned; NOT yet
# triaged — the next sync run re-scans them, so the boundary is set to the
# newest PORTED SHA, not the current beta tip):
#   Backend (beta 670922ac..4075a006): 4075a006 (hyphenated destructive AWS CLI
#   deny — likely KEEP generic security), 24c320f6 (PID-ns signal containment),
#   40965431 (mcp-gateway blue-green cutover), 9d4360d7 (knowledge thread-local
#   sqlite), ed2f65b0 (cron cancel running executions), 1d8f9a4c (toolbox-shim
#   bypass — likely SKIP_INTERNAL/toolbox), 25cfdce3 (code-review-sage test mypy
#   — SKIP, app ABSENT), 8c8d085d (system-monitor registry removal — N/A, app
#   ABSENT from fork), 081c7b47 (docs sync sweep — triage by content next run).
#   Frontend (frontend-beta ec0c619 & 9f68580 & 08e28b8): 9f68580 (Skill browser
#   modal — the FE half of 31b8a10b, port next run), ec0c619 (cron-cancel UI —
#   pairs with backend ed2f65b0), 08e28b8 (Electron shell redesign — triage).
#
# ── Prior batch ──────────────────────────────────────────────────────────────
# --- Governance-seam re-triage (2026-07-18) ----------------------------------
# NOT a sync boundary advance. This pass re-triaged 16 already-in-window upstream
# commit GROUPS against the CPP platform/ extension-point strategy; it did NOT
# triage any new upstream window, so the beta / frontend-beta / mainline boundary
# SHAs below are INTENTIONALLY UNCHANGED. Full per-SHA verdict tables (implemented
# ports/extracts, rejected recommendations, and the 16 group summaries) live in
# left-out.md -> "Governance-seam re-triage (2026-07-18)". Two prior boundary-note
# classifications are corrected here:
#   * 40965431 (mcp-gateway blue-green pooled-backend drain on credential rotation)
#     — RECLASSIFIED SKIP_INTERNAL -> EXTRACT (item G6). The drain machinery
#     (pool.py drain_all_to_bluegreen + credwatch.py content-digest watcher) is
#     Amazon-free generic core; the credential watch PATH is supplied by the
#     companion via the new IdentityProvider.credential_watch_paths() seam (Default
#     returns [] = no watcher, run_gatewayd byte-identical). Landed on branch
#     feat/governance-seam-retriage (consolidated after PR #18 merged; the
#     drain was first built on the stacked feat/govseam-post-pr18 whose pool.py
#     offsets differ and whose sync boundary advanced past 40965431).
#   * 4964a3e (credit-usage popup FE half of ebad8b6c) — the batch-30 DEFER below
#     ("reads credits_overage/percentage the fork backend does not yet emit") is now
#     OBSOLETE: the fork backend emits the canonical shape, and the same FE fix was
#     independently merged as fork PR #20 (382c9bd5 "show true Kiro credit total via
#     usage API"). Recorded as ALREADY_PRESENT (via PR #20); the govseam item P4 was
#     DROPPED during rebase (superseded).

# ── Prior batch ──────────────────────────────────────────────────────────────
# --- Artifacts-page exact-mirror batch (2026-07-17) --------------------------
# Branch sync/artifacts-mirror-2026-07-17 (built in an isolated git worktree).
# This is a TARGETED CONTENT PORT of the MeshClaw artifacts page, NOT an
# incremental boundary advance — so the beta/frontend-beta boundary SHAs below
# are intentionally LEFT UNCHANGED. Upstream tips have since advanced
# (backend 5d1961ef->50eae442, frontend 7665cc8e->82df28c); those windows are
# the NEXT incremental sync's input and were NOT triaged here. This batch mirrored
# the artifacts page by content on top of the batch-37 boundary: durable LOCAL
# comments (BE ArtifactComment store + FE CommentsSidebar/FileArtifactComments/
# useCommentBridge/comment-bridge widgetSrcdoc), masonry ArtifactsPage
# (@virtuoso.dev/masonry), ArtifactBody/ArtifactPanel + clickable artifact refs,
# and a vendor-neutral PUBLISH subsystem behind a new CPP PublishRegistry seam +
# capabilities.publish governance scope + publish.allowed_destinations config
# (public edition registers NO provider -> publishing unavailable). EXCLUDED
# (carve-outs): the Iterate button stays hidden (SHOW_ARTIFACT_ITERATE=false);
# Harmony Artifactory + Chorus concrete providers, remote comment sync
# (merge_remote_comments/RemoteComment), remote browse (RemoteArtifactCard/
# RemoteArtifactDetailPage), and UpstreamSyncBanner stay absent (internal infra).
# See left-out.md "Artifacts-page exact-mirror batch (2026-07-17)".

# ── Prior batch ──────────────────────────────────────────────────────────────
# --- Batch-37 (dual-repo beta sync 2026-07-17) -------------------------------
# Branch sync/beta-2026-07-17 off origin/main (b111ee0). Backend window
# f9b37d8c..5d1961ef, frontend window e7c1de9..7665cc8e, triaged by content.
# 20 target commits PORTED IN CHRONOLOGICAL ORDER (+1 prereq), one fix per commit,
# ZERO left out this batch. Tally: 16 backend (15 target + 90f5c3f1 prereq) + 5
# frontend = 21 real commits. All decided by DIFF against fork content, not SHA.
# LATE ADD (2026-07-17): 3 keeper commits — 1fee24ce (backend cgroup/ulimit,
# KEEP), b493aa40 (backend PARTIAL — generic drain helper) and 597a0697 (frontend
# cron model selector, KEEP) — were initially DROPPED because their triage-skeptic
# agents hit API errors (the pipeline nulled them); they were re-triaged by diff
# and ported on top of the batch (inserted before this boundary chore), so the
# counts above include them. 2 of the 15 backend targets are PARTIAL (e7619f5e,
# bcc97453) plus b493aa40 => 3 PARTIAL total.
# CODE-REVIEW FIXES (2026-07-17, commit 4ea586f before this chore): /code-review
# max on the branch found 3 defects, fixed fork-side: (1) artifact DELETE
# pre-fetch caught only ArtifactNotFoundError -> a bad slug leaked HTTP 500 (was
# 400); broadened to ArtifactError. (2) cron per-job model override validated
# against available_models("claude_code") (5 ids) while the fork's dropdown comes
# from the live kiro-cli --list-models (~21) -> 11/21 models 400'd; dropped the
# membership gate at all 4 sites to match the chat model path (format regex +
# auto sentinel kept). (3) test_acp_client backend-selection tests failed on a
# cgroup-delegated host (our cgroup_scope_argv wiring adds a systemd-run prefix);
# a class-scoped autouse fixture neutralizes the probe for those tests.
# BACKEND PORTED (15 target + 1 prereq):
#   5d1961ef offload SEL prune + memory maintenance off the event loop
#     (run_in_executor via maintenance_executor; SEL.prune streaming os.replace
#     under self._lock); 3b93b1f3 ctx.phase() returns a no-op context manager
#     (_PhaseContextManager/_NoOpContextManager + validate.py visit_With/AsyncWith
#     guards) — fixes AttributeError('__enter__'); a636d32c watchdog crash-dump
#     discoverability (new crash_dump_store.py + loop_watchdog dump_file +
#     server startup replay + `kirocrew doctor` section); e7619f5e artifact-bound
#     companion sessions + artifact_update WS event (PARTIAL — dropped the
#     pull-latest/relocate emits, absent Artifactory handlers; delete path fetches
#     art before delete to capture version); 192b5c5c per-job cron model override
#     (CronJob.model round-trip + MCP/dashboard validation + gateway
#     _acquire_with_model_fallback/_annotate_model_downgrade; the upstream
#     test_taskrunner de-flake was not in this commit's tree — nothing to port);
#     902d22cb drain in-flight turns before stale-asset watchdog shutdown
#     (_drain_in_flight + _count_in_flight_work); 90f5c3f1 (PREREQUISITE) surface
#     live {{MAX_SUBAGENTS}} cap (context resolver + mcp_core hints); 2e4bf165
#     extend cap guidance to orchestrator + trim prompt.md; 99d5947f grill skill
#     one-question-per-turn (regenerated from post-image, no brand tokens);
#     bcc97453 detect expansion-produced git-publish program names
#     (_GIT_PUBLISH_SUBST_PROGRAM_RE; PARTIAL — kept the fork's stricter
#     _AMBIGUOUS_EXPANSION_RE fail-closed, inverted the feature-allow test);
#     d3aeb079 configurable knowledge embedder timeout + content budget
#     (KnowledgeConfig.embed_timeout_secs/embed_content_budget; fork's
#     embed_signature has no base_url, dropped upstream's socket-timeout hunks);
#     a093ccdf teach session-history resolver the cron JSONL naming (Mesh-2546);
#     46bf6b96 /goal command (v0 self-verdict loop over AutoNudgeService);
#     75d2be54 subagent-registry test isolation fixture in conftest + remove the
#     flaky test_global_timeout;
#     1fee24ce wire per-process + cgroup resource limits into agent spawns
#     (LATE ADD; HEADLINE SECURITY, Talos bdf0d7e5 / CR-289825986) — rewrote
#     security.apply_resource_limits (_RLIMIT_DEFAULTS; NOFILE default-on 1024,
#     NPROC/CPU/AS opt-in 0, clamp-to-hard on soft+hard), added
#     sandbox.resource_limit_preexec + cgroup_scope_argv (transient
#     systemd-run --user --scope under kirocrew-agents.slice, TasksMax + MemoryMax,
#     host-proportional 65% RAM / 8192 MB fallback, no-op+warn where no cgroup v2
#     delegation), wired preexec_fn/cgroup into every fork routed-spawn site
#     (acp/client+runtime, apps/backend+registry+routes, deploy_web, file_explorer,
#     cron_script, dashboard handlers agents/memory/sessions, git_coord, hooks,
#     mcp_discovery, task_executor, voice_reply + fork-only cloud/aws & knowledge/
#     llm_pool), 2 new test_spawn_audit tripwires + TestApplyResourceLimits /
#     TestResourceLimitPreexec / TestCgroupScopeArgv + non-Linux /proc skip guards.
#     DROPPED the upstream claude_code/taskkeeper/code_reviewer wiring (absent) and
#     the black-only conftest/security churn;
#     b493aa40 extract shared pending-context drain helper (LATE ADD; PARTIAL,
#     CR-289037039 / Mesh-2837) — ported ONLY the generic ungated half
#     (chat_runner.drain_pending_context + shared state._MAX_PENDING_CONTEXT across
#     the 2 eviction sites; test_thread_backfill drain-contract + PersistMapReconnect).
#     DROPPED the headline slack thread-backfill (_backfill_thread_context) — it
#     fires only on the challenge-redirect flow this fork deliberately removes; its
#     chat_slack hunk + auth/security/guard tests dropped with it.
# FRONTEND PORTED (5): 7665cc8e 429-aware retry ladder + friendly-429 mapper
#   (Usage page behind a throttling edge); fe9137b5 steer-by-default split send
#   button + steer carries attachments; abfe9692 Schedule Running badge +
#   refreshTrigger auto-reload + running-first sort (switched tests to
#   renderWithProviders, kept the fork's batch-select divergence); 08e6d08b
#   deflake ChatSidebar folder-create Escape-cancel;
#   597a0697 cron model selector in job form + list (LATE ADD; KEEP,
#   CR-289627904) — CronJob.model + JobForm useQuery(['models']) selector (both
#   layouts) + CronTab Model column (colSpan 10->11) + SchedulePage inline model;
#   the UI counterpart to the already-ported backend per-job model override
#   192b5c5c. Generic model override over /api/models (the single acp backend),
#   NOT a provider selector.
# KiroACP invariant HELD (enum ["acp"], no cc_agent/Bedrock/ClaudeCodeProvider
#   re-added; the to_provider_id("claude_code") uses are the canonical model-id
#   namespace key, not a selectable provider). SHOW_ARTIFACT_ITERATE untouched
#   (no ArtifactDetailPage in the diff). De-Amazon audit: 0 LIVE couplings on
#   added lines (only inert provenance comments + the KiroClaw/Talos/CR ids in
#   the git-publish provenance comment). Verify: per-commit pytest (touched
#   files, python3.11 venv) + flake8 all green; frontend tsc -b clean + touched
#   vitest green. NOTE: config-baseline.json regenerated (+2 knowledge fields).
#   Pre-existing unrelated fail: tests/test_context.py::test_pointer_is_short
#   (WIDGET_BLOCK prose >300 chars) fails identically on origin/main — untouched.
#
# --- Batch-36 addendum (folded into PR #92 / branch sync/beta-2026-07-15-batch35) ---
# After batch-35's ports landed, a third upstream wave arrived (backend
# 3cae0aad..f9b37d8c = 9 commits; frontend 340cfa9..e7c1de9 = 3). Folded into the
# SAME PR. Tally: 7 backend PORTED + 2 backend SKIP_INTERNAL; 1 frontend PORTED +
# 2 frontend ALREADY_PRESENT/no-op.
# BACKEND PORTED (7):
#   ff921945 isolate per-app spawn failures at gateway boot (try/except so one
#     app's fail-closed sandbox error doesn't crash boot); ca356e53 fold playwright
#     e2e suite (de-Amazoned harness, dropped @amzn/chromium/brazil machinery);
#     a6c1680e [SECURITY] sandbox agent-influenced spawns (Talos 92e24570) —
#     sandboxed_spawn_argv chokepoint (wrap_argv + scrub_env) routing
#     mcp_discovery.probe_server/git_coord/task_executor; 8993ac32 grill skill
#     facts-vs-decisions (dropped _GRILL_CHALLENGE_PROMPT, absent); 689fe139
#     orphan-sweep spawn-grace + dead-runtime respawn (dropped doc-links-gated
#     chat_handlers/knowledge hunks); f9b37d8c macOS-26 sandbox-exec — REMOVES the
#     >=26 version gate again (empirical probe trusted), superseding batch-35's
#     interim ef4f05b gate re-add.
#   549fd384 (zero-CLI Slack settings API + subagent approval-slot routing) was
#     FULLY ALREADY_PRESENT (fork PRs #65/#68) — nothing ported, boundary advances.
# BACKEND SKIP_INTERNAL (2):
#   88e703ab (register Dev Fleet in app-registry) — internal MeshClawApp-DevFleet
#     GitFarm app; fork registry is []. 38b4fb97 (auto-ingest Chorus/Pippin/Quip
#     doc links) — entirely internal doc sources (chorus.aws.dev/pippin.amazon.dev/
#     quip-amazon.com) via the stubbed ReadInternalWebsites bridge.
# FRONTEND: 273cb0e playwright load-stability PORTED (re-anchored to fork IA);
#   c0fb568 (Slack panel + conversation-map close-on-click-away) ALREADY_PRESENT
#   (fork PR #65/#84); e7c1de9 registry regen a no-op.
# Codex findings from this + batch-35 remain tracked as inherited fix-in-both below;
# a6c1680e/ff921945/f9b37d8c RESOLVE several of them (wrap_argv-raises boot crash,
# macOS-26 hard-fail, unscrubbed agent-spawn env).
#
# --- Batch-35 (dual-repo beta sync 2026-07-15, SECURITY-FIRST round) ---
# Branch sync/beta-2026-07-15-batch35 off origin/main. Backend window
# a7736388..3cae0aad (5 commits), frontend window 4b9d6255..340cfa9 (4 commits).
# User directive this round: make sure ALL security commits are synced; two
# mid-round upstream waves were folded into the SAME PR. Tally: 9 PORTED =
# 5 backend + 4 frontend. ZERO SKIP — every candidate landed. SECURITY
# COVERAGE: no hardening silently dropped.
# BACKEND 5th commit (folded after the first 4):
#   3cae0aad -> "fix(sandbox): restore macOS-26 sandbox-exec gate + macOS test
#     fixes" — upstream fixup to THIS batch's platform_compat/Talos-sandbox work:
#     re-adds the platform.mac_ver() macOS-26 (Darwin 25/Tahoe) short-circuit that
#     383eae45 had dropped (+ restores module-level `import platform` and the
#     test_sandbox_probe macOS-26 assertions), plus macOS-only test hardening
#     (tmp_path->/tmp, passthrough fixtures, PEERCRED mocks, numpy skipif).
#     session_pid.get_ppid / acp isort were already at parity. Upstream-CR
#     CR-289598418.
# FRONTEND 4 (window 4b9d6255..340cfa9, all KEEP):
#   3c290bb command-palette Settings provider (codegen registry + deep-link +
#     useSettingHighlight; adapted to the fork's KiroACP-only tab set, dropped
#     absent Provider/Secretary/Sync/TaskKeeper panels); 8345970 sidebar
#     folder drag-move (collectFolderSubtreeIds cycle-safe reparent; dropped the
#     Radix submenu affordance the fork's inline-toolbar sidebar lacks); d29d798
#     Semantic Memory table render cap (100 rows + filter); 340cfa9 store
#     per-slot cache eviction on sseSlots + 200-notification ring cap.
# PORTED (4 backend, chronological / parent-first):
#   eaf62582 -> "feat: native Windows support via platform_compat shim" — the
#     fork ADOPTS Windows support (user reversed the prior macOS+Linux-only stance).
#     NEW src/kiro_crew/platform_compat.py (cross-platform file-lock/process/signal/
#     chmod/resource/strftime/console shim) + routing ~50 live modules through it;
#     replaces the dangerous os.kill(pid,0) liveness probe with pid_exists. PARTIAL:
#     dropped hunks for fork-absent modules (providers/claude_code.py, cc_agent.py,
#     aim_cli.py, secretary.py, knowledge/connectors/quip.py). Docs flipped to
#     "macOS, Linux, and Windows" (CLAUDE.md/AGENTS.md/README/INSTALL/FEATURES +
#     cli/session/acp-client specs) + new docs/WINDOWS_INSTALL.md. macOS/Linux
#     behavior byte-for-byte unchanged (Windows branches added, not substituted).
#     Task Mesh-2329.
#   383eae45 -> "fix(security): remediate 10 Talos AppSec pentest findings" —
#     [SECURITY] sandbox launcher hardening (CAP_SYS_ADMIN drop + NO_NEW_PRIVS,
#     seccomp-BPF, hardlink inode scan), fail-closed unsandboxed-exec gate
#     (agent.sandbox_allow_unsandboxed_exec opt-in), quote-evasion-resistant
#     git-publish detection, IMDS/env-credential access detection, command-path
#     normalization, apply_resource_limits, SEL 0o600, snapshot sel_hmac.key
#     exclusion, orphaned-session-root sweep. Kept the fork's STRICTER fail-closed
#     git-push gate (did not graft upstream's looser quoted-feature-branch allow).
#     Keystone floor preserved.
#   e99840c3 -> "fix(vector-memory): guard FAISS dedup against writes without
#     embeddings" — correctness guard (emoji stripped from subject).
#   1b0585bb -> "fix(security): port Talos OSR AppSec (app admission, HMAC, host)"
#     — [SECURITY] NEW apps/admission.py App-Kit admission gate (banned/approved,
#     optional-HMAC signature, fail-closed), backend.entryPoint path-containment,
#     third-party load gate (apps_allow_third_party opt-in), body-bound App-Kit
#     HMAC, added ~/.kirocrew/app_admission.json to the sensitive floor. DISTINCT
#     from + does NOT weaken the CPP platform/admission.py plugin gate
#     (CONTRACT_VERSION stays 1). Dropped the publish_sync.py hunk (internal
#     Artifactory engine, absent from fork).
# INHERITED-UPSTREAM findings from PR #88 remain fix-in-both (see left-out.md);
# NOT re-addressed here. KiroACP invariant HELD; de-Amazon audit clean.
#
# --- Batch-34 addendum (folded into PR #88 / branch sync/beta-2026-07-15-batch33) ---
# After the batch-33 ports landed, three more backend commits appeared upstream
# (d757fd9d..a7736388) plus the batch-33 DEFER (ed984a05) was resolved. Triaged
# by content and folded into the SAME PR (not a new one), advancing beta to the
# new tip a7736388. Frontend tip unchanged (no new MeshClawWebsite commits).
# PORTED (2 backend, generic-core KEEP):
#   ed984a05 -> "fix(security): remove legacy HTML dashboard entirely" — was the
#     batch-33 DEFER; the fork still carried static/dashboard.html + js/dashboard.js
#     + purify.min.js + css/dashboard.css + cli-mode.css + _HTML_PATH wiring + the
#     legacy-XSS test, so this is a genuine generic-core XSS-surface reduction.
#     index() now serves dist/index.html only (guidance page on missing bundle);
#     /static route, theme assets, kirocrew-logo.png, and _BASE_CSP preserved.
#     Upstream-CR CR-289374220 / Talos V2285871874.
#   a7736388 -> "feat(context): scale injected-memory budget to model context
#     window" — the fork had the fixed _CONTEXT_BUDGET_BASE=165k this refactors to
#     scale linearly with the active model's window (1M reference = factor 1.0,
#     default deployment byte-for-byte unchanged, pinned by test). Generic core.
# SKIP:
#   889f87b0 (register 6 chapmang apps in app-registry) — SKIP_INTERNAL: all six
#     are internal MeshClawApp-* GitFarm gateway apps (incl. taskei-agent); the
#     fork's src/kiro_crew/apps/app-registry.json is deliberately [] and these
#     point at nonexistent internal repos. Upstream-CR CR-288078910.
#
# --- Batch-33 (dual-repo beta sync 2026-07-15) ---
# Branch sync/beta-2026-07-15-batch33 off origin/main 327be9d. Backend window
# 84d8abf3..d757fd9d, frontend window ab0d8d2c..4b9d6255, triaged by content.
# 6 PORTED = 5 backend + 1 frontend, one fix per commit, in chronological order.
# BACKEND (5, all generic-core KEEP): 36527cd3 Heimdall pentest security.py hardening
#   (CWD-bypass absolutize in _path_in_home_dirs, _CRED_CLASS quote/query delimiters,
#   _PEM_HOLD_RE streaming hold-back, empty-user DB-URI, relative-traversal verb-agnostic
#   block); 7cc8217d consolidated Talos redaction/SSRF/XPIA follow-ups (PEM round-3,
#   JWT/JWE+JSON-Bearer union + StreamRedactor fail-closed, entropy glued-secret, SSRF
#   trailing-dot, XPIA round-2) — built on 36527cd3, ported first; 82560fb7 exfil-URL
#   path/raw-IP scan (_HARD_CREDENTIAL_RE full-path + _URL_RE raw-IPv4/IPv6) + bash-egress
#   DENY gate (_BASH_EXFIL_PATTERNS/audit_bash_exfiltration wired into hooks.on_tool_call
#   above the governance block + mcp_cron governance-routed OR chain); 56fbb774 mcp-gateway
#   claim-push caller identity (new claim.py, _StubConn/_CONN_INDEX/_apply_claim in gatewayd,
#   ancestor_pids + backoff in stub, schedule_claim on both rekey paths; AcpClient socket
#   read defensively via getattr); 0917c9aa queue chat during sub-agent runs (is_system_injection
#   + _dequeue_next_system_message drain filter, serialize_slots shared serializer +
#   subagents_running, api_chat idle-case hold, chat_runner drain honors hold, debounced
#   slots push on subagent_spawn/done). FRONTEND (1): 4b9d6255 reflect sub-agent runs in
#   composer busy state (selectComposerBusy/selectSlotSubagentsActive + subagents_running
#   type, both ChatPage + ChatPane gated on the shared selector).
# ALREADY_PRESENT: 791c0cb5 (backend Board removal / Channels hide) + c64af69 (frontend Board
#   removal) — the removal is fully done on the fork via merged PR #78 (Board dir/tests absent,
#   Channels hidden flag + AppsPage filter live). SKIP: cdb2d135 (CC deny union-merge —
#   SKIP_NONKIROACP, confined to the deleted cc_agent.py / _CC_DENY_PATTERNS), d757fd9d
#   (brazil ws-create timeout — SKIP_INTERNAL, patches _brazil_build_app_locked which is
#   absent from the fork; the fork's timeout=30 is an unrelated public git-pull step).
# DEFER: ed984a05 (Talos V2285871874 remove legacy HTML dashboard — KEEP-worthy generic-core
#   XSS-surface reduction that applies to the fork, but a 16-file −2409-line behavior-changing
#   removal that landed in the beta window AFTER the batch verdicts were adversarially verified;
#   held for the next verified batch, recorded in left-out.md "Still open").
# KiroACP invariant HELD (no provider glue re-added; _is_claude seam intact, 19 refs).
#   De-Amazon audit: 0 LIVE couplings on added backend/doc/frontend lines (only inert Talos
#   IDs + Mesh-NNNN/CR-NNNNNN provenance refs). Codex-flagged StreamRedactor.feed cross-chunk
#   credential-drop gap in 7cc8217d is inherited VERBATIM from upstream (not a regression) —
#   joint fork+upstream follow-up, NOT fixed here. Verify: per-commit pytest (touched files)
#   + flake8 + isort + mypy all green (test_security, test_hooks, test_mcp_cron, test_mcp_gateway_claim,
#   test_queue_during_subagents, test_slack_gateway); frontend tsc -b clean + chatSlice vitest green.

# --- Batch-31 (dual-repo beta sync 2026-07-15) ---
# Branch sync/beta-2026-07-15 off origin/main 2567734. Backend window 5a62fdd7..84d8abf3,
# frontend window 2bf7ede..ab0d8d2c, triaged by content. 21 commits ported IN CHRONOLOGICAL
# ORDER (oldest first), one fix per commit, ZERO left out (no SKIP / DEFER this batch).
# Tally: 21 PORTED = 15 backend + 6 frontend, incl. 3 PARTIAL (cec9758e electron single-
#   instance-lock rescue, a5e914cb slash-desc fallback drop /workflows, 834470ac backend
#   slash-desc drop /workflows assertion).
# BACKEND (15, security-heavy — all generic-core KEEP per the "SSRF/redaction/XPIA/SEL =
#   generic core" rubric): af5306f5 conftest AIM-skills isolation; 1d9968b0 episodic-memory
#   injection scan (XPIA persistence); 30e126ca JWT/Bearer redaction; 6068f9db bare 40-char
#   AWS-secret redaction; 4e5ad699 SSRF base guard on remote embedding URL; 1ebb2b20 remove
#   flaky test_discovery_runs_off_event_loop; 2159a709 SEL HMAC-key + audit-log sensitive-path;
#   834470ac backend slash-command descriptions; 975cc548 full PEM private-key body redaction;
#   220fad51 SSRF alternate-IPv4-encoding block (follow-up to 4e5ad699, ported same wave);
#   b19e6e1b Slack thread-parent/meta XPIA screen (moves _INJECTION_PATTERNS ->
#   vector_memory_constants, re-exported); c035795f taskrunner TOOL_AUTO_APPROVE (re-applied on
#   top of the fork governance kwargs); 0a010ec2 bound background MCP probe fan-out; 58be644c
#   folder-watcher build-output-dir prune; 84d8abf3 webbrowser.open offload to subprocess_executor.
# FRONTEND (6): cec9758e electron single-instance lock (PARTIAL — the ONLY generic hunk of an
#   unsupported-Windows commit; all Windows scaffolding dropped, electron stays ^33); a5e914cb
#   slash-command autocomplete descriptions (PARTIAL — dropped /workflows, absent in the fork);
#   291660b0 pin/unpin current-session palette action; 77eaf255 changelog viewer in Settings >
#   About (kept fork's GitHub tooltips, NOT code.amazon.com); 71a5d059 findBestOccurrence empty-
#   anchor guard + V-shaped early-exit; ab0d8d2c inline blinking streaming caret.
# KiroACP invariant HELD (no provider glue re-added; the 834470ac claude_code branch is the kept
#   dormant seam, filled via the shared map). De-Amazon audit: 0 LIVE couplings (inert Talos IDs
#   + builder-mcp test-fixture string only). Verify: per-commit pytest (touched files) + flake8 +
#   isort all green; frontend tsc -b clean + touched vitest green.

beta bb3901c82fe74c62d12cdd76369ebaa3d1b6e53a      # 2026-07-20 MeshClaw backend triaged/ported tip (batch-49, branch sync/beta-2026-07-20-batch49 off origin/main b0c52e68). Window 42ad7707..bb3901c8 triaged by content: 5db73730 KEEP (knowledge oversized-file guard + off-loop chunking, default 100.0), b9120339 PARTIAL (publish picker installable() ABC no-op + `available` flag; 5 concrete provider/self-install files DROPPED companion-side), 4d279fd0 PARTIAL (config load() parses orchestrator+watchdog; dashboard-theme hunk ALREADY_PRESENT), 6e74cb1f KEEP (rubber-duck skill), 84359f45 KEEP (terminal per-tab cwd/persistent PTYs/live titles/cap 12; 4 new POSIX-PTY controller-fd refs carry an inline wokeignore directive). LEFT OUT: 820b1b01 SKIP_INTERNAL (app-registry pipeline-health), f123cc3f ALREADY_PRESENT-by-arch (fork exfil logic diverged to exact-host exemption, no _SAFE_DOMAIN_SUFFIXES branch), 8fb38539 DEFER (probe process-group reap + escaped-aim ID — substantial generic-security PARTIAL, own triage), cbd594fa ALREADY_PRESENT (tool_search built-in already in defaults.json), a39bffa5+bb3901c8 SKIP_INTERNAL (v3.3.0 CHANGELOG folds). See batch-49 note above. PRIOR: 42ad7707 2026-07-20 MeshClaw backend triaged/ported tip (batch-48, branch sync/frontend-ux-2026-07-19). Window 38cb05ef..42ad7707 (6 commits) triaged by content at HEAD a823fb16: 836644ff PARTIAL (schema-gate the 6 workflow_* tools; 9 artifact remote/share DROPPED absent, 5 comment tools ALREADY_PRESENT PR #54), c9600704 KEEP (SimpleDiGraph RLock — cross-thread knowledge search vs ingest/dedup), 7d89725f KEEP (carry image_temp_paths through Slack queue + defer cleanup to _dispatch_queued's shared finally), 23bf26bb KEEP (wire mcp_gateway read-buffer/spill config keys to runtime), 4a621332 KEEP (finish 90e3cccc ~/.kiro/agents hardening — non-dict guard + UnicodeDecodeError), 42ad7707 PARTIAL (discover.py non-string-tag guard; layer-1 skillsh coercion ALREADY_PRESENT batch-38). See batch-48 note above. Backend stragglers ABOVE 42ad7707 left UNTRIAGED for the next run: b9120339, 5db73730, f123cc3f, 820b1b01. PRIOR: 38cb05ef 2026-07-20 MeshClaw backend triaged/ported tip (batch-47, branch sync/frontend-ux-2026-07-19, = the beta-braveheart tip). Window 808f7f74..38cb05ef triaged by content: 85ff0a87 KEEP USER-DIRECTED (in-process embeddings via vendored llama-cpp-python, always-on; model transport swapped Gitfarm->HTTPS CDN — the one structural adaptation), 1f4ea490 KEEP (raw shell command evaluated in the tool security gate, not the LLM-authored title; + 3 fork-only transport_dispatch gates threaded), 38cb05ef PARTIAL (version bump + internal changelog DROPPED; generic doctor 3-way runtime check + sandbox test fixes PORTED). See batch-47 note above. PRIOR: 808f7f74 2026-07-20 MeshClaw backend triaged/ported tip (batch-46, branch sync/frontend-ux-2026-07-19). Window d22bc6a0..808f7f74 triaged by content: af42492f KEEP (sandbox CPUWeight/opt-in CPUQuota/oom_score_adj bias), b8f82ffd KEEP (GPT reasoning effort on kiro backend), 808f7f74 KEEP (SessionWatchdog + RSS-threshold recycle — USER-REQUESTED). See batch-46 note above. Beta tip 85ff0a87 (in-process embeddings) remains UNTRIAGED for the next run. PRIOR: d22bc6a0 2026-07-20 MeshClaw backend triaged/ported tip (batch-45, branch sync/frontend-ux-2026-07-19). Window efd8988b..d22bc6a0 triaged by content: 873d1130 KEEP (Mesh-2829 otel degrade gap closed — recorder.py OTel import moved under TYPE_CHECKING, local_exporter import moved inside provider.py's guard, new test_pip_deps_consistency.py AST guard; fork adaptations: de-Amazoned docstring, amzn _EXEMPT entry dropped, deploy/skills/ added to _EXCLUDED_SUBTREES), d22bc6a0 KEEP (steer-loss fix — _ChatSlot._pending_steers tracking, register-before-await + failure unwind in chat_handlers, hard-kill clear, EVENT_STEER_CONSUMED count-aware settle + finally requeue-at-head in chat_runner, test_steer_requeue.py). Boundary = newest resolved SHA d22bc6a0 per convention (the batch-45 prompt named 873d1130's tip, but both keepers were triaged+ported, so the boundary advances to the newer one; skill convention wins). Beta tip has since advanced to 85ff0a87 — af42492f/b8f82ffd/808f7f74/85ff0a87 landed post-triage and remain UNTRIAGED for the next run. PRIOR: efd8988b 2026-07-20 MeshClaw backend triaged/ported tip (batch-44, branch sync/frontend-ux-2026-07-19). Window 53214b50..efd8988b triaged by content: f2661c8a KEEP (sandbox transient backend-probe cache policy + prewarm_backend boot hook; port stops before userns_available), efd8988b KEEP (mcp-gateway ping-gated wedge detection + notifications/cancelled receiver; IS_POSIX gate on the stdio-loop worker interleave). Both the newest resolved SHAs; efd8988b is the beta tip. See batch-44 note above. PRIOR: 53214b50 2026-07-19 MeshClaw backend triaged/ported tip (batch-43, branch sync/frontend-ux-2026-07-19). Window 3f83fa15..53214b50 triaged by content: 32225cc2 PARTIAL (config to_dict knowledge/heartbeat/watchdog; snapshot_dir hunk already present), c28619b5 KEEP (auto-research near-miss cycle filenames + recovery prompt), c3732ac0 KEEP (immutable /assets/ cache only on 200/206/304), 2b3c722d KEEP (seccomp kill(-1) guard + host-PID identity hardening; partially mitigates inherited 56fbb774), 53214b50 KEEP (session_host_preexec NOFILE raise for ACP session hosts). f2661c8a (backend-probe cache, current beta tip) left untriaged for the next run. See batch-43 note above. PRIOR: 3f83fa15 2026-07-19 MeshClaw backend triaged/ported tip (batch-42, branch sync/frontend-ux-2026-07-19). Window cd1cc045..3f83fa15 triaged by content: 3afa65a5 PARTIAL (transient-flag raise sites + tests; cron-cancel test hunk already present), 337b2fcc PARTIAL (test-only identity-topology archive, fork adaptations), 22fa7042 KEEP (pool-discard kill verification), 3f158792 KEEP (TasksMax 8192), 3f83fa15 KEEP (granular .kirocrew file-explorer policy, keystone-pinned) — plus the 475146ca defer rescue (comment MCP tools, folds a84eabc4/de64d07b) which predates the window. See batch-42 note above. PRIOR: cd1cc045 2026-07-19 MeshClaw backend triaged tip (batch-40c boundary correction — no new backend port this batch). cd1cc045 was triaged PARTIAL in batch-40b (only the generic TestAbortAckLogging AF_UNIX sun_path test hunk ported; the toolbox-bundler arch-gate headline is internal-only) and 1fd785bc ALREADY_PRESENT, but the boundary was left at 6f6d5bdf; corrected to the newest resolved SHA per convention. PRIOR: 6f6d5bdf 2026-07-19 MeshClaw backend triaged/ported tip (batch-40b addendum, branch sync/beta-2026-07-18). Window 3eaa7df1..6f6d5bdf: 6f6d5bdf ghost-cards backend PORTED, cd1cc045 PARTIAL (AF_UNIX test hunk only), 1fd785bc ALREADY_PRESENT — see batch-40b note above. PRIOR: 3eaa7df1 2026-07-18 backend triaged/ported tip (batch-40, branch sync/beta-2026-07-18). Window 4075a006..3eaa7df1 triaged by content: 11 backend PORTED (incl. the 14fb9442 revert of 24c320f6 pid-ns and two PARTIALs 076a7a43/3eaa7df1) + test-only PARTIAL f93e2f07 + 1 SKIP_NONKIROACP (d80b1430 Bedrock history trim) + 2 ALREADY_PRESENT (4fe1de2b session_key binding, ce65b67a blue-green revert no-op) — see batch-40 note above. Boundary = the current beta-braveheart tip. PRIOR: 4075a006 2026-07-18 backend triaged/ported tip (batch-39 follow-up, branch sync/beta-2026-07-18). Window 670922ac..4075a006 triaged: 4 backend PORTED (4075a006 AWS-CLI deny, 9d4360d7 knowledge sqlite, 24c320f6 PID-ns, ed2f65b0 cron-cancel) + 1 SKIP_INTERNAL (40965431 Midway blue-green cutover — Midway stubbed in fork) — see batch-39 note above. Boundary advances PAST 40965431 to the newest PORTED SHA 4075a006 (the batch-38 beta tip). Still-untriaged batch-38 DEFER remainder (1d8f9a4c toolbox-shim, 25cfdce3 CRS-test-mypy, 8c8d085d system-monitor, 081c7b47 docs-sync) predate 4075a006 in the batch-38 window and were verdicted SKIP/N-A there; the next run re-scans 4075a006..beta. PRIOR: 670922ac 2026-07-18 backend triaged/ported tip (batch-38, branch sync/beta-2026-07-18). Window 5d1961ef..670922ac: 19 backend PORTED — see batch-38 note above. PRIOR: 5d1961ef 2026-07-17 backend triaged tip (batch-37, branch sync/beta-2026-07-17). Window f9b37d8c..5d1961ef triaged by content; 11 backend PORTED (+1 prereq 90f5c3f1) — see batch-37 note above. PRIOR: f9b37d8c 2026-07-16 backend triaged tip (batch-36 addendum, folded into PR #92; advanced from batch-35 3cae0aad). Window 3cae0aad..f9b37d8c (9 commits): 7 PORTED + 2 SKIP_INTERNAL — see batch-36 note above. Window 84d8abf3..d757fd9d triaged by content; 5 backend KEEP ported (36527cd3 Heimdall pentest security.py hardening, 7cc8217d Talos redaction/SSRF/XPIA follow-ups, 82560fb7 exfil-URL path/raw-IP + bash-egress gate, 56fbb774 mcp-gateway claim-push caller identity, 0917c9aa queue chat during sub-agent runs). 791c0cb5 (Board removal / Channels hide) ALREADY_PRESENT (merged fork PR #78). SKIP: cdb2d135 (CC deny union-merge — SKIP_NONKIROACP, confined to deleted cc_agent.py), d757fd9d (brazil ws-create timeout — SKIP_INTERNAL, Brazil installer path absent from fork). DEFER: ed984a05 (Talos legacy-HTML-dashboard removal — KEEP-worthy generic-core XSS-surface reduction, but a 16-file behavior-changing removal that landed after the batch verdicts were verified; held for the next verified batch — see left-out.md).
frontend-beta cfa63fc9eba3ce4d35469fca8cb01aff9e430778  # 2026-07-20 MeshClawWebsite triaged/ported tip (batch-49, branch sync/beta-2026-07-20-batch49, = the beta-braveheart tip). Window f20f5fada..cfa63fc (all 3 commits ported, zero SKIP): 614aa3c KEEP (hint install-on-first-publish — FE half of backend b9120339; optional PublishProviderDescriptor.available? + share-panel hint, standalone-inert), 311afea KEEP (chat-specific terminal tabs + tab/panel persistence — 16 files, 2 deleted; usePanelTabs module store + openTerminal, terminalRegistry persistent WS, thin CliPanel, chatSlice per-chat activityOpen, ChatPage run-in-terminal + cold-tab hydration, App.tsx retires floating terminal; pairs backend 84359f45; fork 'artifacts' ViewKind kept), cfa63fc KEEP (IME composition Enter guard in channel MentionInput — useImeGuard; Channels hide untouched, KEEP not SKIP_FORKUX). See batch-49 note above. PRIOR: f20f5fada28347415f611c964f312919d6d5457f 2026-07-20 MeshClawWebsite triaged/ported tip (batch-47, = the beta-braveheart tip). Window 4e520bc..f20f5fa: 5b93d06 KEEP (always-on in-process embeddings dashboard — pairs backend 85ff0a87, USER-DIRECTED adoption) PORTED; f20f5fa KEEP (cancel errors shown on the Logs tab — JobLogsView cancelError prop) PORTED. See batch-47 note above. PRIOR: 4e520bce2b57cde30cf795deee1d169c4a399599 2026-07-20 MeshClawWebsite triaged/ported tip (batch-46). Window b6561a90..4e520bc: 4e520bc KEEP (reasoning-effort dropdown for GPT models — frontend pair of backend b8f82ffd, same CR) PORTED; fork keeps REASONING_EFFORT_PROVIDERS acp-only. 5b93d06 (always-on in-process embeddings dashboard, pairs backend 85ff0a87) remains UNTRIAGED for the next run. See batch-46 note above. PRIOR: b6561a90 2026-07-20 MeshClawWebsite triaged/ported tip (batch-44, beta-braveheart tip). Window 2e67ddb0..b6561a90: 109b0d3e KEEP (allowlist HTML sanitize + per-message MessageErrorBoundary, React #290) PORTED, b6561a90 KEEP (Mesh-2908 composer wrong-session fix: createSlot switched-away guard + uiSlot send routing + composerSlotRef persist keying) PORTED. See batch-44 note above. PRIOR: 2e67ddb0 2026-07-19 MeshClawWebsite triaged/ported tip (batch-43, beta-braveheart tip). Window b1ffd248..2e67ddb0: 2e67ddb0 KEEP (sw.js shell-only cache + /apps/ skip + stable build-time cache version via swVersionPlugin) PORTED. See batch-43 note above. PRIOR: b1ffd248 2026-07-19 MeshClawWebsite triaged/ported tip (batch-42, beta-braveheart tip). Window 082f45d5..b1ffd248: b1ffd248 KEEP (AcpAdapter static model-registry fallback) PORTED; plus the user-authorized frontend-UX defer rescues ported this batch — 08e28b8a Electron shell redesign PARTIAL (folds rider 23f5a314; mwinit/Claude-cost/Artifactory-Share hunks dropped), 082f45d5 KEEP (instance-tab-bar traffic-light inset, ported LAST on the new inset stack — supersedes batch-41's no-anchor N/A), cd6730f ALREADY_PRESENT (masonry — fork ahead via PR #14, batch-18 DEFER closed), 0000f561/f746d60/86315dc PARTIAL (prod via PR #14; missing tests ported). See batch-42 note above. PRIOR: 082f45d5 2026-07-19 MeshClawWebsite triaged/ported tip (batch-41, beta-braveheart tip). Window 507715f0..082f45d5: c64e203a still-streaming switchback fix PORTED, 23f5a31 activity-panel shrink-0 ALREADY_PRESENT (by architecture, DetailPanel already shrink-0), 082f45d5 macOS instance-tab-bar traffic-light inset N/A (no anchor — fork lacks the traffic-light inset stack; rides with the DEFERred Electron shell redesign 08e28b8a). See batch-41 note above. PRIOR: 507715f0 2026-07-19 MeshClawWebsite triaged/ported tip (batch-40c). Window 7851f2af..507715f0 (parent chain 4aec096f <- 2a33062f <- 507715f0): all 3 PORTED — 4aec096f split-button send-now pills, 2a33062f progress-track removal, 507715f0 a11y keyboard session rows + focus-reveal. Boundary = newest resolved SHA 507715f0 (NOT 4aec096f, which is the oldest of the three); only 23f5a31 (pin activity panel width) remains untriaged for the next run — see batch-40c note above. PRIOR: 7851f2af 2026-07-19 MeshClawWebsite triaged/ported tip (batch-40b addendum). Window 5a51c71c..7851f2af: 7851f2af ghost-cards frontend PORTED (pairs backend 6f6d5bdf) — see batch-40b note above. PRIOR: 5a51c71c 2026-07-18 MeshClawWebsite triaged/ported tip (batch-40). Window ec0c619..5a51c71c + the two batch-39 stragglers triaged by content: 7 frontend PORTED (38dfc2e9 WS notification event, 964d92c9 smooth-stream cursor snap, 52a6c57b batched-frame marker guard, 7ddd1b5e steer-echo reconcile, 5147f023 SourcesList namespace, 5a51c71c stop-button escalation, 9f685806 Skill browser modal straggler) + 08e28b8a Electron shell redesign DEFER (blocked on open fork PR #14 — see left-out.md human-decision table) — see batch-40 note above. PRIOR: ec0c619 2026-07-18 MeshClawWebsite triaged/ported tip (batch-39 follow-up). Window fdd8734..ec0c619 triaged by content: ec0c619 (cron-cancel UI — pairs with backend ed2f65b0) PORTED — see batch-39 note above. Boundary advances to ec0c619. Still-untriaged: 9f68580 (Skill browser modal — FE half of 31b8a10b) predates ec0c619 in the batch-38 DEFER set; 08e28b8 (Electron shell redesign) postdates it — both re-scanned by content next run. PRIOR: fdd8734 2026-07-18 MeshClawWebsite triaged/ported tip (batch-38). Window 7665cc8e..fdd8734: 12 frontend PORTED — see batch-38 note above. PRIOR: 7665cc8e 2026-07-17 MeshClawWebsite triaged tip (batch-37). Window e7c1de9..7665cc8e triaged by content; 4 frontend PORTED (7665cc8e throttle-retry, fe9137b5 steer split-send, abfe9692 Schedule running badge, 08e6d08b ChatSidebar deflake) — see batch-37 note above. PRIOR: e7c1de9 2026-07-16 (batch-36 addendum; advanced from batch-35 340cfa9). Window 340cfa9..e7c1de9 (3 commits): 273cb0e playwright load-stability PORTED, c0fb568 Slack panel + conversation-map ALREADY_PRESENT (fork PR #65/#84), e7c1de9 registry regen no-op. Window 4b9d6255..340cfa9 (4 commits) all PORTED (store cache-evict/notif-cap, semantic-memory render cap, sidebar folder drag-move, command-palette Settings provider). See batch-35 note above. Window ab0d8d2c..4b9d6255 triaged by content; 1 frontend KEEP ported (4b9d6255 reflect sub-agent runs in composer busy state — selectComposerBusy/selectSlotSubagentsActive). c64af69 (Board removal frontend) ALREADY_PRESENT (merged fork PR #78). No frontend SKIP/DEFER this batch.
mainline 45dcb603f117886648d79c7dde05e82d2d8cda2d  # 2026-07-15 MeshClaw mainline tip (upstream), unchanged since batch-27 (batch-33 re-confirmed; batch-40 re-confirmed unchanged). No NEW mainline-only content to port (all in-window candidates reached the fork via beta-braveheart; every mainline-only delta is a release-merge or already triaged via beta).
# NOTE: this 'mainline' SHA is the UPSTREAM MeshClaw mainline tip (for the mainline-only
# candidate check), NOT the fork's mainline — batch-12 mistakenly recorded the fork SHA
# (796e21c5) here, which broke the `$MAIN..origin/mainline` scan. Corrected in batch-13.

# --- Batch-30 (dual-repo sync 2026-07-14) ---
# Branch sync/beta-2026-07-14 off origin/main 6df6dad. Backend window 3b679515..5a62fdd7,
# frontend window 2a70682..2bf7ede, triaged by content.
# Tally: 19 keepers PORTED (12 backend + 7 frontend, incl. 3 PARTIAL: ebad8b6c credit-usage,
#   41dec169 bulk-model, dd1d652b SafeChildWatcher) + 7 left out.
# LEFT OUT: 4 SKIP_INTERNAL = 21e296f1 (secretary self-alert — secretary absent), 819ffc16 +
#   3b4858bd (ADC Ops Center + kratos app-registry rows — registry is [] by design, internal
#   Brazil pkgs), 9471ba1b (setup.cfg->setup.py console_scripts — Brazil CPython310 workaround,
#   fork uses setuptools>=64). 2 SKIP_NONKIROACP = c143b659 (cc agent-skills sync — mirror.py
#   absent, claude_code-gated), 2e976a72 (claude knowledge-worker approval_mode — CCWorker path).
# 1 DEFER = 4964a3e (credit-usage popup FE half of ebad8b6c): reads credits_overage/percentage
#   the fork's text-scrape backend does not yet emit; porting alone regresses the pill. Port as a
#   pair once the backend text path emits the canonical shape. Flagged for human review.
#   >> OBSOLETE (2026-07-18 governance-seam re-triage): the fork backend now emits the canonical
#   >> shape and the same FE fix was independently merged as fork PR #20 (382c9bd5). Recorded
#   >> ALREADY_PRESENT via PR #20; govseam item P4 dropped as superseded. See the re-triage note
#   >> at the top of this file + left-out.md.

# --- Batch-28 (dual-repo sync 2026-07-13) ---
# Branch sync/beta-2026-07-13 off origin/main 4f942f3. 58 candidates triaged (31 backend in
# 5dbb7778..e870fed4 + 20 frontend in 3858e573..20f18813 + 7 batch-27 DEFER re-audits) via a
# 116-agent adversarial workflow (analyzer + skeptic per candidate, decide by content).
# Tally: 41 keepers PORTED (22 backend + 19 frontend, incl. 9 PARTIAL) + 17 left out.
# LEFT OUT: 10 SKIP_INTERNAL = 0e7322ce (MarkBin publish provider — publish subsystem absent),
#   0fe90c30 (mimir SIM assignee — mimir absent), d87672ea (file-backed publications — publish
#   absent), 4a41b8a3 (taskkeeper slack-user-content strip — taskkeeper absent), 5cdfb619
#   (secretary keyword-negation regex — secretary absent), a5979e42+6055397 (Chorus comment-edit
#   BE+FE — Chorus publish absent), d0a78b07 (defusedxml dead dep — no fork consumer), 7cfeed18
#   (Work Summary app registry row — app-registry.json is [] by design), 1984ccc (publish UI
#   supports_shared gate — publish surface absent).
# 3 ALREADY_PRESENT = 70898ca5+d5819090 (AcpRuntime teardown/recycle — fork PR #43 did it
#   natively), e4fc0ce (follow-up-bar scroll arrows — via fork PR #45).
# 4 DEFER = 3e5d7132 (workflows-engine script vetting — PR #57 carries all three fixes by
#   content), f8383887 (hero SVGs — needs the fd633154 builtin-ui materialization substrate),
#   0000f561+f746d60 (anchored/durable artifact-comment subsystem absent — port when the
#   affffcff base lands).
# BATCH-27 DEFER RESCUES ported this batch: 3858e573 CommandPalette tab-strip scroll (KEEP),
#   5360aa4d split-view live tool streaming (KEEP — the per-slot substrate now exists),
#   3acce399 RL-v2 UI (PARTIAL — engine chooser dropped pending PR #57). d2240c48 resolved as
#   PARTIAL (generic _dm_owner redacting DM exit point ported; tunnel hunks remain absent).
# Verify (independent cumulative pass): backend pytest 11834 pass / 53 skip, ONLY fail is the
#   KNOWN pre-existing test_skills mtime flake (test_mutators_invalidate_iter_cache) — confirmed
#   failing identically on origin/main in a clean worktree, untouched by this batch; flake8
#   CLEAN on all 52 touched .py files. Frontend tsc -b CLEAN + 3303 vitest pass (285 files,
#   1 skip). Step-6 de-Amazon: 0 LIVE couplings (single grep hit = "incognito" substring-matching
#   "cognito" in a comment). KiroACP invariant HELD (provider enum ["acp"], ACP_BACKEND_CLAUDE
#   seam 23 refs untouched, no provider selector/cc_*/bedrock_* re-added). Inclusive-language
#   gate on added lines: 0 hits.
# 42 commits on branch (41 ports + 1 review-nits chore c646ab9 folded onto fb9d6f9). Boundaries:
#   beta 5dbb7778->e870fed4, frontend-beta 3858e573->20f18813, mainline unchanged 45dcb603.

# --- Batch-27 follow-up (2026-07-10): folder icon picker (46c7a01) ---
# Ported the un-triaged-tail item "folder icon picker" (MeshClawWebsite 46c7a01,
# 2026-06-13, pre-window) as a batch-27 follow-up per Zezhen's ask. Adapted the
# upstream fixed-portal picker to the fork's Radix DropdownMenu idiom (6d8008a migration);
# curated FOLDER_EMOJIS grid + isSingleEmoji free-input + reset-to-auto, wired into both
# list-view and board-view hover action groups. Emoji support intentional per Zezhen; AGENTS.md
# + AUTOSDE exception documented. No boundary advance (commit is within the pre-window tail).

# --- Batch-27 (dual-repo sync 2026-07-10, ultracode comprehensive, FIRST on the GitHub fork) ---
# RUN IN THE PUBLIC GITHUB CHECKOUT /workplace/bolichen/KiroCrewGitHub (origin github.com/
# kirodotdev/KiroCrew), which ships via PULL REQUEST — NOT the internal Brazil workspace/CRUX
# path of batches 1-25. Branch sync/beta-2026-07-10 off origin/main 8355fc6 (safety tag batch27-base).
# KEY CONTEXT: the boundary file was STALE (recorded batch-25 beta 6cf3aae0) but the fork had since
# done batch-26 (PR #18) + a large GitHub-NATIVE porting wave (PRs #10-#38) that did NOT cite the
# upstream SHA — so the fork is BIDIRECTIONAL with upstream, NOT strictly downstream. Every candidate
# was therefore triaged BY CONTENT against the fork file, never by SHA reachability.
#   Window: backend beta 6cf3aae0..5dbb7778 (88 candidates) ; frontend 9b512ca..3858e573 (47) = 135.
#   mainline advanced 199d7853..45dcb603 (no new mainline-only content).
# 135 candidates triaged via a 270-agent adversarial Workflow (analyzer + skeptic per commit, decide
# by content; 2 skeptics hit an API error and were hand-confirmed: 2dce8f8a/8e58cadf both KEEP). Tally:
# 73 PORTED (58 KEEP + 15 PARTIAL: 47 backend + 26 frontend) + 62 left out (22 SKIP_INTERNAL, 4
# SKIP_NONKIROACP, 29 ALREADY_PRESENT, 7 DEFER).
# HEADLINE backend: d759d3b3 SEL-audit off-loop (PARTIAL — bulk ALREADY_PRESENT, fork-origin CR-286485989),
#   255ef0b8 opt-in lazy-load skills (new skill_usage.py + skill_search), 9daac30f auto-research
#   recursive exploration, 2e960fe2 mid-turn steer (PARTIAL — ACP layer already present, ported the
#   dashboard wiring), 647cc057 deny widget-origin orchestrator auto-run, 2dce8f8a instances SO_REUSEADDR
#   reconnect, 0a22f1c7 restart-bot-from-Slack, 41ad420c strip PYTHONPATH/PYTHONHOME from foreign MCP,
#   8a11ca98 preserve data/ on app install, c4b62689 forward-Slack-message shortcut, 8b9dfc18
#   server-authoritative workspace theme, 52deca0/b182453 Claude Fable 5 (model-registry, PARTIAL).
# HEADLINE frontend: a7f25225 native recursive split-pane grid (biggest FE port), 12bfe897 Command
#   Palette / Search Everywhere (PARTIAL), 26ce1d57+033951d Steer button + visible ack, b66599c unified
#   store-connected SessionActionsMenu, 07e9f27 Pinned filter, folder-visibility + search-by-folder.
# LEFT OUT (highlights): SKIP_INTERNAL = WritingReview (7608b99f/321f9fbc/eed2773d), mcp_gateway
#   (7bb86a44), secretary (8b5351f1), code-reviewer FE (3a182608), artifact/Chorus publish (9a696e65/
#   5e13d8b7), AEA-tunnel (92104fcc/a179b9e0/b944c74d), toolbox/Brazil/mailmap (9bbcb048/cb6f8f19/
#   c637016b/7f86241d), midway (4f753ed0), builder-mcp security-assistance (cec38579), app-registry
#   (1f6b7982/81d19cef), version bumps (0e9c9ffc/d53b497b). SKIP_NONKIROACP = claude_code usage/cost
#   tracking (3997c2ae/7b74179b/c6bc2744/f6e38b3b — the fork has no per-provider spend surface).
#   DEFER (7, human review): 3e5d7132 (workflows engine absent), f8383887 (deploy_web/workflows hero
#   assets), d2240c48 (tunnel-off warning follow-up), 0000f561/3acce399/5360aa4d (FE: comment-anchor
#   subsystem / RL-v2 UI / split-view rich tool-stream — absent substrates), 3858e573 (CommandPalette
#   tab-strip scroll — pairs the ported 12bfe897, tiny follow-up worth picking up next batch).
# Verify (independent cumulative pass): backend flake8 CLEAN (51 touched .py) + pytest 11260 pass/52
#   skip/1 fail (ONLY the pre-existing test_skills mtime flake — confirmed failing identically on
#   origin/main, untouched by this batch). frontend tsc -b CLEAN + 3156 vitest pass (273 files, 1 skip).
#   Step-6 de-Amazon: 0 LIVE couplings (all inert comments/labels/test-strings). KiroACP invariant HELD
#   (provider enum ["acp"], ACP_BACKEND_CLAUDE seam intact, no provider selector/cc_*/bedrock_* re-added).
# 73 commits on branch. DMG rebuild OWED (macOS-only; this ran on Linux — mode:port ceiling). PR against
#   main authorized by the user for this manual run.

# --- Batch-25 (dual-repo sync 2026-07-03, ultracode comprehensive) -----------
# STACKED on the batch-22+23+24 worktree branch sync/beta-2026-06-27 AFTER rebasing all 141 commits
# onto the advanced origin/mainline (55d0ccb = fork-native "Pentest round 3": fail-closed SEL audit,
# REST input validation on /api/lessons+/api/crons, legacy dashboard.js XSS escaping). Rebase resolved
# 2 conflict hunks, both in dashboard/handlers/cron.py (validation-import union + drop the redundant
# unvalidated agent_id re-assignment, keep project_path); 233 collision-regression tests green. One
# coherent CR for 22+23+24+25. Window since batch-24:
#   backend  beta-braveheart bfa7b4e8..6cf3aae0  (13 candidates ; mainline unchanged 199d7853)
#   frontend MeshClawWebsite  0f2c062..9b512ca    (2 candidates)
# 15 candidates triaged via a 30-agent adversarial Workflow (analyzer + skeptic per commit + synthesis;
# 1 frontend analyzer 0d9797e hit the StructuredOutput retry cap -> triaged by hand). Tally: 9 PORTED
# (6 KEEP + 3 PARTIAL: 8 backend + 1 frontend) + 6 left out. ZERO skeptic verdict-flips (all confirmed
# on the merits against post-rebase fork content).
# KEEP (6): 4aba97e9 app-MCP registration health-gate (dead HTTP mcp url no longer poisons EVERY kiro-cli
#   session via ~/.kiro/settings/mcp.json — the fork's SOLE provider path; new _live_port_for +
#   _gate_mcp_registration + boot reconcile, applied ON TOP of fork-native governance re-vet); 284af3ea
#   cookie-Secure via is_https_request (X-Forwarded-Proto:https from a loopback peer — any TLS-terminating
#   proxy/tunnel; 4 cookie sites; de-Amazoned the tunnels.lab.aws.dev docstring); b0140b0c Permissions-Policy
#   clipboard header at the no_cache_middleware chokepoint (Chrome 143+; extracted _apply_security_headers,
#   DROPPED upstream's tunnels/cognito/RUM CSP endpoints); f4acf37e ACP tool-stall watchdog folds
#   _last_activity (session-keepalive) into the clock (client.py:2375, mirrors the stale-turn check);
#   210baed2 pytest-xdist worker cap min(cpu,32) (conftest hook; MESHCLAW_->KIROCREW_MAX_TEST_WORKERS);
#   0d9797e (FE) per-tab remote-host config keyed by port + per-tab token refresh (new electron/host-config.js
#   + validation REMOTE_PORT_RE/REMOTE_PATH_RE + main.js per-port refactor; RESTORED the fork's OSS
#   DEFAULT_REMOTE_BIN=~/.local/bin/kirocrew vs upstream's ~/.toolbox default; kept toolbox as candidate).
# PARTIAL (3): 99041753 (bulk pentest-r2+bg-reconnect ALREADY_PRESENT via fork-native 1c8f980+aef7a86;
#   residual ported = the 2 edit-mode Slack preview sites still using the truncating throwaway
#   StreamRedactor().feed(accumulated) -> redact(accumulated) at handler.py:2798/2893 + 1 regression test;
#   DROPPED all ALREADY_PRESENT security.py/hooks.py/chat_runner.py/server.py hunks + the _SENSITIVE_STAGING_RE
#   widening that collides with the fork's governance _EXTRACT_INTO_TRUST_ROOT_RE divergence); bf9663df
#   (workspace_root() realpath-normalize for symlinked-home cold resume — new _resolve_workspace_root helper
#   routes all 3 branches through os.path.realpath; DROPPED the _encode_cc_project_dir slug-parity test — the
#   CC provider is deleted; MESHCLAW_->KIROCREW_WORKSPACE); 76cfc2e0 (the 3900-line ACP session-multiplexing
#   rewrite is DEFERRED for human review — it deletes _turn_lock + re-adds a claude_code|bedrock get_bg_session
#   selector on the absent _resolve_agent_provider = SKIP_NONKIROACP; rescued ONLY the 2 generic subagent-path
#   chokepoint fixes: redact sub-agent title+text via the existing redactors + read nested content.text via
#   _extract_text_chunk at client.py:~2660-2688 + 1 test).
# LEFT-OUT (6): SKIP_INTERNAL x5 = 6cf3aae0 (docs for the ABSENT mcp_gateway/ subsystem), 66c022fc + 9b512ca
#   (AUTOSDE.yaml — absent Brazil/AutoSDE build tooling), 21087188 (MeshClawApp-Planning row — app-registry.json
#   is [] by design), a4a35008 (Slack auth-window-expiry warning GATED on the fork-removed challenge_window
#   subsystem). DEFER x1 = 2bbf2e0b (2231-line `meshclaw pod` test-harness — brazil-build/toolbox/~/.aim +
#   Bedrock-shaped env-scrub couplings need re-architecture not rename; legitimate DEFER per SKILL cron rule).
# Verify: backend flake8 clean (18 touched files) + FULL pytest suite [see CR]; frontend tsc -b clean +
#   168 electron node --test pass (host-config/remote-token/validate). Step-6 de-Amazon: 0 live couplings
#   (toolbox refs in electron remote-token are pre-existing fork convention, .local/bin default restored).
#   KiroACP invariant HELD (enum ["acp"], no provider selector re-added, 76cfc2e0 provider-dispatch DEFERRED,
#   ACP_BACKEND_CLAUDE seam untouched, CONTRACT_VERSION unchanged). Governance keystone intact; the app-MCP +
#   cookie + ACP + slack ports re-ran test_governance_boot/self_protection green.
# 151 commits on branch (141 rebased from 22+23+24 + 9 batch-25 + 1 boundary chore). DMG rebuild OWED (macOS-only).
#   Safety refs: batch25-backup-prerebase (0cdc53a), batch25-postrebase, batch25-postport, batch25-postreview.
#   Boundaries: beta bfa7b4e8->6cf3aae0 (live tip, stragglers cleared), frontend-beta 0f2c062->9b512ca (live tip),
#   mainline unchanged 199d7853.
# CODE-REVIEW (/code-review, 10-angle recall Workflow, 36 agents): 51 raw -> 23 deduped -> 21 surviving
#   (16 CONFIRMED + 5 PLAUSIBLE). Disposition: the ONLY fork-introduced regressions were in the hand-written
#   subagent bridge I wrote when DEFERRING 76cfc2e0's rewrite (client.py:2671-2688 + _extract_text_chunk):
#   (1) empty nested content.text shadowed populated flat text -> use `or` fallback; (2) reasoning/thinking
#   chunk surfaced as user-visible subagent output -> gate on `not _su_thinking`; (3) non-dict content raised
#   AttributeError -> isinstance guard in _extract_text_chunk. All 3 FIXED + a regression test added, folded
#   into 61b83b6 via fixup/autosquash; 394 acp+governance tests green. The other 18 findings all reproduce
#   UPSTREAM VERBATIM (verified against 4aba97e9/f4acf37e/284af3ea/99041753/0d9797e post-images): the app-MCP
#   over-broad `app:`-prefix scrub + disabled-only boot-reconcile (4aba97e9), the watchdog max(last_data_ts,
#   _last_activity) clock (f4acf37e), is_https_request loopback-trust (284af3ea, documented threat model),
#   redact(accumulated) edit-preview (99041753 — per-chunk redaction already runs), and the new URL().port==""
#   coalescing (0d9797e). These are design observations for UPSTREAM to address; fork-diverging on an actively-
#   maintained subsystem mid-sync would guarantee next-batch conflicts, so left as-ported (noted here + in CR).

# --- Batch-24 (dual-repo sync 2026-07-02, ultracode comprehensive) -----------
# STACKED on the batch-22+23 worktree branch sync/beta-2026-06-27 AFTER rebasing 22+23 onto the
# advanced origin/mainline (aef7a86 = 9 fork-native commits: pentest auth incl. CWE-613 cookie
# re-mint, CPP wave-3, slack challenge-gate REMOVED, instances auto-reconnect, default port 5476).
# The rebase excised the orphaned send_channel_challenge port (182d101) + resolved ~7 conflicts;
# see the batch-24 rebase note in the kirocrew-meshclaw-sync memory. One coherent CR for 22+23+24.
# Window since batch-23:
#   backend  beta-braveheart 1926f16f..bfa7b4e8  (47 candidates ; live tip advanced past post-scan)
#   frontend MeshClawWebsite  a6b1fc7..0f2c062    (31 candidates ; live tip 0d9797e post-scan)
#   mainline fcee75b9 (v3.1.1 slack hotfix == beta 7ae12831 content, SKIP).
# 78 candidates triaged via a 156-agent adversarial Workflow (analyzer + skeptic per commit +
# synthesis) + 1 manual (8a6bfbc0 API-flake). Tally: 53 PORTED (36 KEEP + 17 PARTIAL: 32 backend +
# 21 frontend) + 25 left out.
# FORK-NATIVE-OVERLAP (the key win this batch — fork base already did these NATIVELY, so ALREADY_PRESENT):
#   ed2f53b6/ef92034/d331022 (instances auto-reconnect, native c033f4a+aef7a86+8603ed7),
#   2e3e7ed4 (pentest auth hardening, native 5700059+24e94b9). 8b1d5eb1 KEPT (only NEW tunables).
# HEADLINE backend: 8b05b3e4 macOS event-loop wedge fix (new executors.subprocess_executor pool +
#   SafeChildWatcher); 8a6bfbc0 keep ACP session alive on model-substitution advisory (adopt "Using X
#   instead" + re-issue session/new; DROPPED absent new_conversation glue, rides dormant _is_claude
#   seam); 829e96d8 gateway singleton flock (new gateway_lock.py); 68fd4f6c folder-PDF ingest +
#   dedup better-recall (pdfplumber, builds on batch-23 dedup.py); e659ed45 auto-ingest artifacts
#   into KB (new artifact_ingest.py); b9b5164a KB span/citations; a62133bc/7c50c24b/702ecac1 redaction;
#   47c06ec7 queue-message edit (BE, pairs FE fcb3309); be1df3cf model-registry context window.
# HEADLINE frontend: 9b3179e revert drag-reorder queue (dnd-kit->framer-motion); 6d8008a-adjacent
#   Radix already present; c17a009 resizable browse mirror; fcb3309 queue-edit FE; 4ab933e storage
#   Phase-0 GC (safeSetItem across ~28 files); 4af6c3b comment-highlight (self-contained, NOT the
#   absent batch-23 comment-bridge); 0f2c062 sortable Name/Date file browser (pairs BE bfa7b4e8).
# 17 PARTIALs dropped internal/absent hunks: mcp_gateway (e5210223, 9586ad6 MCP-Pool tab, 3da3f45-SKIP),
#   .meshclaw-dev app manifests+hero assets (fd633154/0951476), tui/ (dd27186 Message.tsx), Brazil
#   Config (b1edc01e), Java/Brazil doctor hints (372329d1), absent-file storage sites (4ab933e).
# SKIP_INTERNAL (16): app-registry adds (247aa7d8 pensieve, ffee2086 standup-zen), mcp_gateway
#   (da953198, 37aa5567, 3da3f45), code-review-sage (79868424, e8a1010b), secretary (328d704e, 2b91d0a),
#   taskkeeper (e3641e2), tui (7c75dec), mwinit (744f5ad), slack session-ceiling (7ae12831), Midway/
#   Taskei client (16c5375 challenge-handoff), toolbox docs (ca927a74), v3.1.1 bump (1f4ae9f7), 0cf627ea test.
# SKIP_NONKIROACP (5): 3d466e6c+42ebd21 Auto approval mode (VERIFIED hard-gated on claude_code backend
#   + native afk classifier), 79868424 code-review-sage selectable model (provider surface), a8224f18+
#   056aa42 Miami Vice 2080 theme (cosmetic).
# ALREADY_PRESENT (4): ed2f53b6, 2e3e7ed4, ef92034, d331022 (all fork-native overlaps above).
# WORKFLOW-STALL: the backend port workflow stalled TWICE at wave-6 start (model-inference hang, not a
#   shell hang — agent .jsonl flat, no python process). FIX: TaskStop (tree clean, waves 1-5 committed) +
#   ported the last 8 backend keepers via a single general-purpose Agent subagent. Frontend workflow ran
#   clean (5 waves). LESSON recorded in memory: stop+delegate-to-one-Agent beats fighting a stuck workflow.
# Verify: backend flake8 clean (37 files) + pytest 10364 pass/34 skip (only the pre-existing test_skills
#   mtime flake); frontend tsc -b clean + 2940 vitest pass (248 files). Step-6 de-Amazon: 0 live couplings
#   (7 inert comment/docstring hits). KiroACP invariant HELD (enum ["acp"], ACP_BACKEND_CLAUDE seam 22 refs,
#   no provider selector re-added, CONTRACT_VERSION=1 untouched == mainline base). Governance keystone intact.
#   config-baseline.json regenerated (187 entries) for e659ed45 knowledge descriptors + instances tunables.
# 140 commits on branch (87 from 22+23 + 32 backend + 21 frontend). DMG rebuild OWED (macOS-only).
# pdfplumber + boto3 pip-installed into the verify venv. Boundaries: beta 1926f16f->bfa7b4e8 (last
#   triaged), frontend-beta a6b1fc7->0f2c062, mainline 199d7853.

# --- Batch-23 (dual-repo sync 2026-06-27, small-batch read-each-diff) --------
# STACKED on the batch-22 worktree branch sync/beta-2026-06-27 (batch-22 not yet
# merged; origin/mainline still dfa9559). Same branch, one coherent CR for 22+23.
# Window since batch-22:
#   backend  beta-braveheart 58e0abb0..1926f16f  (4 candidates ; mainline unchanged 199d7853)
#   frontend MeshClawWebsite  0e1f442..a6b1fc7    (2 candidates)
# 6 candidates triaged by reading each diff (small-batch path, no Workflow).
# Tally: 4 PORTED (3 backend + 1 frontend) ; 2 SKIP_INTERNAL.
# PORTED:
#   eb32b8a2 -> e2d58b2  fix(dashboard): raise recent-projects cap 10->100 (_MAX_RECENT_PROJECTS).
#   6eaab8cb -> cfadb4a  feat(prompt): require a self-contained final message — 4th _CRITICAL_RULES
#     rule in context.py (decision context must live in the final message; UI collapses steps).
#     Kept the fork's dormant ACP_BACKEND_CLAUDE test_cc_provider parity test (both assertions ported).
#   c6fa6489 -> 72ef8df  feat(knowledge): auto-dedup documents across sources — new knowledge/dedup.py
#     (524 lines, regenerated from post-image) + content_hash schema/index/add_item + ingest+folder
#     triggers + knowledge_dedup MCP tool + `kirocrew knowledge dedup [--apply]` CLI + spec doc + test.
#     Inert "quip" PERSISTENT_SOURCE_TYPES literal kept verbatim (no quip source in OSS).
#   384a5d1  -> c2eb9f6  feat(picker): ProjectPicker searchable Recent tab + browse drill-on-slash
#     (pairs backend eb32b8a2). Regenerated ProjectPicker.tsx + test from post-image (byte-identical
#     pre-image, no brand tokens). +9 tests (30 total pass).
# SKIP_INTERNAL (2):
#   1926f16f  fix(browser): detect Playwright MCP via aim JSON not substring — the fork's
#     is_playwright_installed()/ensure_playwright_installed() are de-Amazoned OSS NO-OP STUBS
#     (the `aim mcp` package manager doesn't exist in OSS — exactly the coupling the fork removed);
#     the fix targets the `aim mcp list` substring check with NO fork anchor.
#   a6b1fc7   fix: anchor widget comment to selected occurrence — the anchored-comment bridge
#     (COMMENT_BRIDGE_BODY/selectionContext/FileArtifactComments) is ABSENT in the fork's diverged
#     widgetSrcdoc.ts (332 vs 560 lines); same absent subsystem as the batch-22 DEFER 4685d34.
#     ANTI-MISS LESSON: the file widgetSrcdoc.ts exists, but the CHANGED CODE does not — verify the
#     hunk's anchor, not just the filename.
# Verify: backend flake8 clean + pytest 9935 pass / 33 skip (only the pre-existing test_skills mtime
#   flake fails, untouched). frontend tsc -b clean + ProjectPicker 30 vitest + context 56 backend pass.
#   de-Amazon audit: 0 live couplings (2 inert quip literals). KiroACP invariant held; governance untouched.
# Boundaries advanced: beta 58e0abb0->1926f16f, frontend-beta 0e1f442->a6b1fc7, mainline unchanged 199d7853.

# --- Batch-22 (dual-repo sync 2026-06-27, ultracode comprehensive) -----------
# Worktree branch sync/beta-2026-06-27 off origin/mainline (dfa9559 = governance CR-284272012
# merged; tracks origin/mainline from creation). Built in a git worktree (/workplace/bolichen/
# kirocrew-wt/batch22) because another session was mid-work on feat/governance in the main checkout.
# Window since batch-21:
#   backend  beta-braveheart 59ec6e1d..58e0abb0  (68 candidates ; mainline unchanged 199d7853)
#   frontend MeshClawWebsite  ca99bb4..0e1f442    (37 candidates)
# 105 candidates triaged via a 211-agent adversarial Workflow (analyzer + skeptic per commit +
# synthesis critic; build/meshclaw-triage-batch22.js, all 105 verdicts, 0 flaked). Ported via 2
# wave-workflows (backend 10 waves, frontend 7 waves; <=5 commits/wave, sequential on one branch).
# Tally: 80 keepers (68 KEEP + 12 PARTIAL: 49 backend + 31 frontend) + 25 left out.
#   Left out: 16 SKIP_INTERNAL, 4 SKIP_NONKIROACP, 1 ALREADY_PRESENT, 4 DEFER. (full list: left-out.md)
# DEFER handling: 8e24a0fc FOLDED into 4425bd52 ; 64599424 FOLDED into f8e5b36f (one commit each);
#   cca1191 (FE project-agent UI) UNBLOCKED by BE 7519a5d9 and ported in the same batch;
#   4685d34 truly HELD (prereq affffcff artifact-comments base absent in fork).
# Cross-repo PAIRS ported BE-before-FE: e0d9577c<->cfdc19f (hide_in_chat), ae522641<->843cc81
#   (tool_search), 7519a5d9<->cca1191 (project agents), b05944e4<->47169ba (KB rename).
# HEADLINE backend: b40982aa OpenAI-compatible /v1/chat/completions (new openai_compat.py);
#   a636b1ba set_project MCP tool; ce4f6c49 replace child-kill allowlist with per-spawn basename
#   tracking (the inert b'builder'/b'aim'/b'arcc' tokens intentionally REMOVED — upstream replaced
#   the mechanism); 38173673 natural-language search recall (FTS5 stopword+OR+weighted RRF);
#   ca9d9bea event-loop wedge containment (new loop_watchdog.py); 4425bd52 background lesson
#   contradiction resolution; 9787ead5 autonudge_stop user-token handshake.
# HEADLINE frontend: 6d8008a Radix sidebar-menu migration (new ui/dropdown-menu+context-menu,
#   npm deps); 8b6807c electron wedged-backend auto-recover (new gateway-liveness.js/pyspy-dump.js
#   — ADDED to electron/package.json build.files[]); aeecc0b dnd-kit Apps-nav reorder; e66986c
#   silent expired-session recovery; cca1191 project-agent discovery UI.
# 11 PARTIALs (generic kept, internal/absent dropped): 5f782326 (drop usage_upload+_patch),
#   4d260c01 (drop entire jail subsystem), f63dfe53 (drop the app, keep manifest/conftest bits),
#   0e139747 (keep SecretaryConfig.auto_dismiss field only), a48502a7 (keep 4 generic guard fixes,
#   drop the whole workflows subsystem), 92fa8ece (drop _patch hunk), ca9d9bea (drop rewrite_agents
#   overlay-offload), 9a6e2a87 (docs: keep 5 generic backfills, drop all internal-subsystem prose),
#   a59e892c (doc coupled to FE aeecc0b), ce4f6c49 (drop providers/claude_code.py hunk), edb11ea +
#   e64b151 (frontend: drop absent-file hunks).
# SKIP_INTERNAL (16): 54f01fc0+bc92b8b5 (writing-review), 25feb233 (artifact sync/Harmony),
#   6ecb54d2 (code-review-sage), 3a1ea4d5+e08f3e4b+fc6a8c12+0d978546 (mcp_gateway), 16fa88d2 (jail),
#   71ac5fcc (Taskei/Midway), 803af9de (Midway preflight), 938a147d (taskkeeper), 20ae2b81+cf093d08
#   (internal app-registry), 48f9627e (toolbox), 9dca095 (workflows UI — generic fixes ride a48502a7).
# SKIP_NONKIROACP (4): cdd68ecb (claude_code 1M-context), d5dc40e+186aeaa (Artifactory publish/sync UI),
#   823ec07 (secretary UI — generic config rides BE 0e139747).
# ALREADY_PRESENT (1): 4ff05ccf (revert of f63dfe53's app — fork already at post-revert state).
# Verify: backend flake8 CLEAN (29 .py) + pytest 9915 pass/33 skip (3 non-batch fails: 2 botocore-
#   absent, fixed by pip install boto3; 1 test_skills mtime flake in UNTOUCHED code, fails same at
#   origin/mainline). frontend tsc -b CLEAN + 2867 vitest pass (245 files; +4 new SourcesList.rename).
#   Step-6 de-Amazon audit: 0 LIVE couplings (7 inert hits: builder-mcp allowlist read in aim_agents
#   _extract_skills, comments/docstrings, secretary config field). KiroACP invariant HELD: provider
#   enum ["acp"], ACP_BACKEND_CLAUDE/_is_claude seam (19 refs) untouched, no provider selector/cc_*/
#   bedrock_* re-added; model-id literals (claude_code map key, claude-opus-4-8[1m]) kept verbatim.
#   Governance keystone intact (security_policy.json/profiles/admission_policy.json in
#   _SENSITIVE_HOME_DIRS); CONTRACT_VERSION=2 not lowered; governance suites green where implicated.
# config-baseline.json regenerated (186 entries) for new fields: agent.sandbox_allow_no_isolation,
#   agent.tool_search, secretary.auto_dismiss_on_reply, slack_channels.*.project_path.
# 81 commits on branch (49 backend incl baseline-regen + 32 frontend). DMG rebuild OWED (macOS-only;
#   this ran on Linux). Artifacts in build/batch22-* (gitignored). CR not yet raised (stop-before-push
#   per user directive — manual run, not the cron standing authorization).
# Boundaries advanced: beta 59ec6e1d->58e0abb0, frontend-beta ca99bb4->0e1f442, mainline unchanged 199d7853.

# --- Batch-21 (dual-repo sync 2026-06-22) ------------------------------------
# Fresh branch sync/beta-2026-06-22 off origin/mainline (b10b070 = batch-20 MERGED).
# Branch tracks origin/mainline from creation (batch-20's CR-base bug fix — see memory).
# Window:
#   backend  beta-braveheart b35c496b..59ec6e1d  (mainline unchanged 199d7853)
#   frontend MeshClawWebsite  fdfe158b..ca99bb4
# 5 candidates triaged (4 backend + 1 frontend), small-batch read-each-diff (no Workflow).
# Tally: 3 PORTED (2 backend + 1 frontend) ; 2 SKIP_INTERNAL.
# PORTED:
#   59ec6e1d -> 2092347  fix(dashboard): same-origin loopback WS for embedded instance
#     iframe (Mesh-1864). Generic CSRF fix in dashboard/origin.py check_origin(): trust a
#     loopback Origin only when it == request Host. Pairs the SEC-016 work taken in batch-20.
#     +5 regression tests (port 8765/kirocrew.localhost adapted). docs/security.md note.
#   d10750e2 -> 5891deb  fix(apps): gracefully handle missing app config.json. Seed empty
#     {} on GET when config absent (data dir wiped by update) so app UI doesn't hang on
#     "Initializing". Generic apps/routes.py handle_app_config; new test_app_config_self_heal.py.
#   ca99bb4  -> d6856a2  fix(voice): pre-warm mic for instant recording (the batch-20 DEFER
#     straggler). useVoiceInput.ts regenerated from post-image (byte-identical pre-image);
#     ChatInput/ChatPage prop-wired surgically. +6 prewarm vitest.
# SKIP_INTERNAL (2):
#   5d99a8d4  feat(security): MCS-Jail Midway AgentContext enforcement (Mesh-1517) — Midway/MCS
#     coupling; jail.py absent, MCS-Jail Brazil dep. Its only generic hunk (sandbox.py
#     userns_available() public alias) has NO fork consumer — only the absent jail.py calls
#     it (anti-miss (b): same precedent as batch-16 useVisibilityInterval). cli.py --no-jail,
#     config agent.jail enum, cli_doctor jail status, config-baseline all confined to the jail.
#   afed9312  fix(mcp-gateway): raise pooled-backend cap 20->64 — 100% confined to mcp_gateway/
#     (manager.py GatewaySpec + McpGatewayConfig in loader.py). Fork has NO McpGatewayConfig
#     (grep empty) — no anchor.
# Verify: backend test_dashboard_origin 54 pass + test_app_config_self_heal 3 pass, flake8
#   clean; frontend tsc -b clean + 14 voice vitest (6 new prewarm) + ChatInput 111 pass.
# de-Amazon audit (Step 6): 0 live couplings; KiroACP invariant held (no provider glue).
# Boundaries advanced: beta b35c496b->59ec6e1d, frontend-beta fdfe158b->ca99bb4 (== live tip,
#   no straggler), mainline unchanged 199d7853.

# --- Batch-20 (dual-repo sync 2026-06-20) ------------------------------------
# Fresh branch sync/beta-2026-06-20 off origin/mainline (batch-19 boundary).
# Spans the post-batch-19 window on BOTH repos:
#   backend  beta-braveheart 36c205e4..b35c496b  (mainline unchanged 199d7853)
#   frontend MeshClawWebsite  08716b28..fdfe158b  (ca99bb4 landed mid-finalize → DEFER)
# 39 commits ported BY CONTENT (no cherry-pick; repos share content lineage only),
# path/symbol-mapped, in chronological order, one fix per commit. Verified per
# commit: backend pytest(affected files) + flake8; frontend tsc -b + vitest.
# Tally: 39 PORTED (17 backend + 22 frontend) ; 19 SKIP/ALREADY_PRESENT ; 1 DEFER.
# No NEW mainline-only delta — the upstream mainline tip is still the v3.1.0 release
# merge; every in-window candidate reached us via beta-braveheart (already triaged).
#
# HEADLINE backend: 465268b CSE must-fix hardening (SEC-009/016/006/012, d2ebac3);
#   2b2eaeb OAuth-style refresh tokens for dashboard auth (ec9c76b); a270bd1 auto-retry
#   transient backend 5xx on the chat_runner path (c2713ac); 33dc71b block session when
#   lessons exceed size cap (463feca); 2bc48022 SEC-016 instance tunnel port-mirror
#   (c76a75e); e401fda deploy-web built-in app + publish-provider registry (c2d404e);
#   d6e48920 resolve KRB5CCNAME at spawn for Kerberos-gated MCP servers (a7c4ba1).
# HEADLINE frontend: 2d6a3e46 desktop auto-update + fail-fast gateway + boot splash
#   (4b3a7e5); 9b6568c docked chat-search results pane (b7feb70); a8861d80 quota-safe
#   localStorage writes (ebae724); 1603580 keyboard nav for dropdowns/pickers (e32e603);
#   fdfe158 visible offline state + click-block + self-recovery (b1077f3); a1d2cd6
#   Serengeti Watering Hole scene (f3dee15).
#
# 2 cross-candidate OVERRIDES of the per-commit triage (analyzers couldn't see each
# other): 1e8f71cb (safeStorage reclaim escalate, e702543) DEFER->KEEP — its prereq
#   a8861d80 (quota-safe localStorage, ebae724) is a keeper ported earlier this batch;
#   2dc76572 (electron scrollable error UI + port recovery, d1fcbad) DEFER->KEEP — its
#   prereq 2d6a3e46 (electron auto-update/splash, 4b3a7e5) is a keeper this batch.
#
# SKIP / ALREADY_PRESENT (19) — backend:
#   754ef420 SKIP_INTERNAL — one-line Brazil versionSet edit (OncallWatchTower/development
#     ->release) in apps/app-registry.json; Brazil registry data, no public-build effect.
#   1464a0a7 SKIP_INTERNAL — appends one curated oncall-watchtower row to app-registry.json
#     (repo:OncallWatchTower); App Store data only.
#   08ae6866 ALREADY_PRESENT — resolution-aware MCP merge fallback (agent.py
#     rebuild_agent_config + test); the production behavior is already live in the fork.
#   35e6ca74 SKIP_INTERNAL — writing-review scanner-sync auth swaps aiohttp->curl; all
#     logic confined to the absent writing-review subsystem.
#   5f3c6555 ALREADY_PRESENT — doc-only Raycast PATH fix in REMOTE_DESKTOP_SETUP.md
#     (toolbox/bin hand-built PATH); fork doc already correct.
#   6b51f2eb SKIP_INTERNAL — adds "meetnote" (repo:KiroCrewApp-MeetNote) to
#     app-registry.json; App Store data only.
#   6aba983d SKIP_INTERNAL — silent self-install Artifactory MCP on first publish; confined
#     to the absent Harmony-Artifactory publish/install subsystem (PublishProvider.ensure_ready).
#   439ae3b6 SKIP_INTERNAL — configurable mwinit FIDO2 flags; entirely in the Midway/mwinit
#     auth subsystem the fork stubs.
#   15ebbb04 SKIP_INTERNAL — mcp-gateway rewriter isolation/correctness; confined to the
#     absent mcp_gateway/rewriter.py + its test.
# SKIP / ALREADY_PRESENT (19) — frontend:
#   7fdd2398 SKIP_INTERNAL — move Secretary settings into the Secretary app page; 4/5 files
#     have no fork counterpart (SecretaryPage/SecretaryPanel — Secretary absent).
#   eefae42a SKIP_INTERNAL — tidy TaskKeeper settings panel (settings/TaskKeeperPanel.tsx);
#     TaskKeeper built-in app absent.
#   6ab1d283 ALREADY_PRESENT — theme mascot positioning / cross-instance bleed / dynamic
#     wordmark; the only generic core hunk (App.tsx dynamic wordmark) is already present.
#   9287a3ac SKIP_INTERNAL — move TaskKeeper settings into the TaskKeeper app page; all 4
#     files confined to the deleted TaskKeeper app.
#   ed11a9ac SKIP_INTERNAL — writing-review "Download annotated copy" button; wholly in the
#     absent writing-review subsystem (no WritingReviewPage.tsx).
#   90e9478  ALREADY_PRESENT — one-line Tailwind w-full on TrustDropdown itemClass rows;
#     already present in the fork's TrustDropdown.tsx.
#   8e3ed110 SKIP_INTERNAL — Web Deploy console + unified publish-provider registry
#     (Route B); every hunk gated on the absent Artifactory/publish-console subsystem.
#   967849fb ALREADY_PRESENT — voice recording feedback + mic picker; the real feat port
#     is fork commit c58b018 (cites 967849f) — verified by content, not title.
#   aad8db00 ALREADY_PRESENT — topbar notifications bell popover; all 7 touched paths carry
#     this behavior in the fork (3 new files present, confirmed by post-image content).
#
# DEFER (1, picked up next batch): frontend ca99bb4 (fix(voice): Pre-warm mic for instant
#   recording) — landed on origin/beta-braveheart at 15:07Z mid-finalize, AFTER the batch's
#   scan; never triaged. The frontend-beta boundary is deliberately LEFT at fdfe158b (the
#   last triaged commit), NOT advanced onto ca99bb4 — advancing would make the next
#   `fdfe158b..origin/beta-braveheart` scan exclude it (silent skip). It is the first
#   candidate of batch-21.
#
# De-Amazon audit (Step 6) clean over LIVE added lines: 0 live couplings on either repo.
# Expected inert hits only — the acp/client.py Bedrock-path comment + allowlist literals,
# and the d6e48920 KRB5CCNAME helper (generic MIT Kerberos env passthrough, NOT a coupling;
# its comments may inertly name amazon-quick-mcp). KIROACP-ONLY invariant held: no
# ClaudeCode/Bedrock provider glue re-introduced; the dormant ACP_BACKEND_CLAUDE /
# _is_claude seam in acp/client.py left untouched.
# Boundaries advanced: beta 36c205e4->b35c496b, frontend-beta 08716b28->fdfe158b
# (NOT ca99bb4 — that straggler landed post-scan, DEFER to batch-21), mainline unchanged 199d7853.

# --- Batch-19 (manual dual-repo sync 2026-06-17, content-port) -----------------
# Fresh branch sync/beta-2026-06-17 off origin/mainline (batch-18 boundary).
# Spans the post-batch-18 window up to the upstream v3.1.0 release on BOTH repos:
#   backend  beta-braveheart 570a9ccf..36c205e4 (mainline merge 199d7853)
#   frontend MeshClawWebsite  ecc6e5a..08716b28 (mainline merge e6cd760)
# 48 commits ported BY CONTENT (no cherry-pick; repos share content lineage only),
# path/symbol-mapped, in chronological order, one fix per commit. Verified per
# commit: backend pytest(affected files) + flake8; frontend tsc -b + vitest.
# Tally: ~36 KEEP, ~12 PARTIAL (internal/absent hunks dropped), 0 DEFER.
# No NEW mainline-only delta — the v3.1.0 release is a straight beta-braveheart→
# mainline merge on both repos; every commit landed via beta-braveheart (triaged).
#
# HEADLINE backend: 3aa01c0 agentic chat-history MCP search tools (Phase 1);
#   f1f9914+1f65ff4 knowledge embedding correctness (embed chunk content, sig-gated
#   async rebuild, watcher self-heal); c3e7241 cron credential-exfil block;
#   2186d95 Slack tool-approval prompts for linked dashboard sessions; e10ce35 5xx
#   classification in _format_acp_error; 8f1d32e cpu_pct from /proc/stat delta.
# HEADLINE frontend: 5a3feab smooth text streaming; cd614c0 collapsible thinking
#   block; 2c687ac Voice tab (TTS+STT); d0eb0ca topbar notifications bell;
#   36c1451 chat-nav merged into activity sidebar; f4930b9 Kiro credit usage pill.
#
# PARTIAL drops (generic hunks ported, internal/absent dropped): d1091de knowledge
#   sandbox KEPT fork CCWorker / dropped claude-via-ACP; llm_pool same; mcp_cron
#   cred-vet ported without internal sub-tools; 2c687ac/36c1451/122f318 dropped the
#   absent secretarySlice + SlackTab; 39cb03d dropped secretary hunks; f4930b9
#   dropped the SkillsTab(PromptFarm) wait-budget hunk + re-anchored the pill beside
#   system-metrics (the Midway TTL pill it anchored on is stripped from the fork).
# SKIP_INTERNAL: backend 36c205e4 (version bump 3.0.2→3.1.0 + config baseline —
#   version/changelog only; fork is 0.1.0) and the v3.1.0 release-merge commits
#   (199d7853 / e6cd760 — no content delta beyond beta-braveheart). KIROACP-ONLY
#   invariant held throughout: no ClaudeCode/Bedrock provider glue re-introduced;
#   the inert ACP_BACKEND_CLAUDE seam in acp/client.py left untouched.
#

# --- Batch-18 (manual dual-repo sync 2026-06-14, ultracode comprehensive) ----
# Fresh branch sync/beta-2026-06-14 off origin/mainline (3fc5d1d = batch-16 boundary).
# Batch-17 (sync/beta-2026-06-12, 15 commits, CR-281529650) was ABANDONED by the user
# (CR draft lost / discarded) and NOT merged — so this batch re-spanned the batch-16
# boundary, re-porting batch-17's verified fixes from scratch plus all newer commits.
# 56 candidates triaged (35 backend + 21 frontend) via a 113-agent analyzer+skeptic
# Workflow. 45 commits: 42 keepers + 2 mid-batch frontend fold-ins + 1 baseline regen.
# Tally: 30 KEEP, 11 PARTIAL, 2 DEFER, 13 SKIP_INTERNAL. No NEW mainline-only delta
# (ebd78ac4 is a byte-identical dual-landing of 14ae0f50, ported).
#
# HEADLINE "KiroCrew styles" (user ask): 7661050 Kiro ghost BOOT-REVEAL splash (draws the
# KIROCREW wordmark in Kiro-purple + vendored anime.js v3.2.2) — ported KEEPING the fork's
# current header logo/avatar/favicon untouched (splash + persistent logo coexist). Plus
# fd88c429 Knight Rider theme persona, b674cd5 WCAG-AA contrast, 8b07305 disintegrate effect.
#
# 3 cross-candidate OVERRIDES of the per-commit triage (the analyzers couldn't see each
# other): 96c39b8 (MLX STT UI) DEFER->KEEP — its backend pair 7b66e2e3 is a keeper this
# batch (ported backend-first); d2325079 (unfreeze session switch) DEFER->KEEP — its prereq
# 24c317d (retrace Back/Forward) is a keeper this batch; cd6730f (artifacts masonry) PARTIAL->
# DEFER — +572-line rewrite on a hard-diverged 253-line page + new @virtuoso.dev/masonry dep,
# zero fix/security value (matches batch-17's human-review deferral).
#
# DEFER (2, noted in CR for human review): cd6730f (artifacts masonry, frontend),
#   96c39b8's masonry sibling N/A. (96c39b8 itself was RESCUED to KEEP — see overrides.)
# SKIP_INTERNAL (13): 2b95f6ac + fc06e9ab (AIM auto_update — absent), 6d07a290 + 45821ff
#   (writing-review — absent), 24745b38 (oncall-radar App Store entry — app-registry.json is
#   [] by design), 231cb2dc (code-approvers — Brazil infra), a6a7c2db (AppSenseAIUsage
#   telemetry — RUM stub), 24f23968 (Knight Rider *world* — parody theme like LCARS, absent
#   substrate), 7c9c140f + 92a1d3bb + 94196d1a + fedf6e4d (changelog/version bumps to 3.x —
#   fork is 0.1.0), 6181474a (SharePoint/Loop redaction carve-out — Amazon-corp M365 hosts
#   only, e62422ae precedent; flagged for human review).
# MID-BATCH FOLD-IN (cron rule): 2 frontend commits landed during the ~2h port (ecc6e5a
#   in-input trigger menu + friendly-placeholder hint — follow-up to the $skill picker;
#   fab2c55 root ErrorBoundary crash-guard) — folded onto the same branch, both clean KEEPs.
# Verify: backend 1538 pass / 24 skip + flake8 clean; frontend tsc -b clean + 2596 vitest;
# de-Amazon audit clean (0 live couplings; 8 inert comment hits + 4 acp Bedrock-path literals).
# Boundaries advanced: beta ebee95e7->570a9ccf, frontend-beta 1ae3c85->ecc6e5a, mainline
# 49633d05->ebd78ac4. CR-281902310.

# --- Batch-16 (cron dual-repo sync 2026-06-11) -------------------------------
# Fresh branch sync/beta-2026-06-11c off origin/mainline (batch-15 CR-281319232 merged, tip 7824113).
# 12 candidates (6 backend + 6 frontend); 9 PORTED, 3 SKIP. No mainline-only delta.
# PORTED (backend): dd59c148 (ACP stale-turn: fold stderr _last_activity into the
#   thinking-path staleness check via max(last_data_ts,_last_activity) — stops false
#   stale-turn reaping long subagent runs), c38b72e8 (test isolation: autouse fixture
#   stubs subagent.read_learned_cost so subagent-sizing tests use cfg fallback not the
#   real ~/.kirocrew store), 7a22e34d (tag compaction notices kind="compaction" via a
#   shared _append_compaction_notice chokepoint so follow-up [OPTIONS:] survive; state.py
#   auto-compact notice tagged inline to avoid an import cycle), bfcea59a (Unlink from
#   Slack: SessionMap.clear_slack_link + slack-unlink endpoint, clears both raw and
#   dashboard:-prefixed keys; 3 test files).
# PORTED (frontend): ffa68cd (Unlink from Slack menu action — pairs bfcea59a),
#   f1910f8 (deriveFollowUpOptions util skips kind=="compaction" — pairs 7a22e34d),
#   599cff3 (prompt-undo reseed/optimize hardening, follow-up to batch-13 b7d3039 —
#   valueFromUserRef discriminates DOM edit vs draft restore; completion-effect single
#   undo boundary), 74e880b (in-progress session filter: showUnreadOnly bool -> activeFilters
#   Set + SESSION_FILTERS table; ADAPTED — dropped upstream slot.app guard, fork Slot has
#   no app field), b1b1211 (board-view "new chat in folder" + button + column-aware
#   createChatInFolder; ADAPTED to the fork's diverged renderColumnFolder button cluster).
# SKIP: ebee95e7 (macOS gateway SERVICE installer — scripts/darwin-gateway-service/ absent,
#   Midway-gateway LaunchAgent + FDA Swift helper, 100% confined, NOT the fork's
#   packaging/build-desktop.sh DMG flow), b9036ac4 (standup test mock — team_manager/standup
#   absent), 1ae3c85 (useVisibilityInterval hook — generic, but its ONLY consumer is the
#   absent midway-ttl topbar countdown; no other fork caller, so SKIP per anti-miss check (b)).
# Both DMGs arch-verified (arm64 + x86_64). CR-281392951.

# --- Batch-15 (cron dual-repo sync 2026-06-11) -------------------------------
# Fresh branch sync/beta-2026-06-11b off origin/mainline (batch-14 CR-281228622 merged, tip 7b1530a).
# 8 candidates (7 backend + 1 frontend); 8 PORTED, 0 SKIP — the biggest real-port batch in a while.
# PORTED (backend): cbd32c91 (test isolation: subagent + stage-loop autouse fixtures),
#   80ba942f (send_message cron Slack delivery + delivered_to reporting),
#   82cf9907 (remove Slack Home Tab plan-usage line — also dropped a fork-only
#   _warm_usage_cache fixture in test_slack_home_tab.py to match upstream HEAD),
#   632b5731 (slack tool-timer elapsed -> title, not details, fixes accumulation),
#   2745c65b (multi-session gateway freeze: per-client _turn_lock in _prompt_loop +
#   aclosing() in send_message_stream; session.py get_or_create claim-under-lock /
#   acquire-after-release on BOTH fast path + cold-start won-race, bounded retry;
#   event-loop heartbeat + consolidation timing logs. ~600 LOC, riskiest port —
#   486 session/acp tests green. ClaudeCodeProvider guards ported VERBATIM as the
#   dormant KiroACP seam, inert in OSS),
#   499818c7 (completion_keep hot-reload setter on SubagentManager + config-patch wiring),
#   202268a3 (stop re-queue zombie-retry guard — _should_suppress_requeue on all 3 paths).
# PORTED (frontend): d12eb8c (completion_keep Chat-panel UI — matched PAIR with 499818c7,
#   committed together as one feature: Subagents settings section, mode select + chars input).
# Symbol map applied throughout (mesh_claw->kiro_crew, MeshClawConfig->KiroCrewConfig,
# api_meshclaw_config_patch->api_kirocrew_config_patch, meshclawConfig->kirocrewConfig,
# /api/config/meshclaw->/api/config/kirocrew, MESHCLAW_SESSION_KEY env already KIROCREW_).
# Verify: backend 939 tests + flake8 clean; frontend tsc -b clean + 2521 vitest (+4);
# de-Amazon audit clean (only the inert ClaudeCodeProvider seam in session.py). CR-281319232.
# GOTCHA: chat_runner re-exports PromptBusyExhaustedError (from llm_helpers) so the upstream
# test import path works verbatim. test_acp_turn_lock.py regenerated-from-post-image.

# --- Batch-14 (cron dual-repo sync 2026-06-11) -------------------------------
# Fresh branch sync/beta-2026-06-11 off origin/mainline (prior CR-281120970 merged, tip dc42d0d).
# 4 candidates (3 backend + 1 frontend); 0 PORTED, 0 PARTIAL, 4 dropped. SKIP-only boundary
# advance (no code change, no DMG rebuild — nothing ships).
# SKIP_INTERNAL: 80c564f5 (team-manager standup-cron leak fix — apps/builtins/team_manager/
#   ABSENT in fork; fork has only auto_research + file_explorer; all 3 files 100% confined,
#   reconcile_schedule_crons has no fork analogue). Anti-miss: grep'd the shared-helper — confined.
# SKIP: 7c25d7ef (v2.7 docs + AGENTS, 33 files — bulk upstream doc snapshot for absent
#   subsystems mcp-gateway/writing-review/promptfarm/research-lab; fork keeps diverged specs
#   per-change). Anti-miss on its bundled test/test_cli.py hunk: RAN the fork's 4
#   TestDoctorOllamaDocker tests → 4 passed (the validate_enterprise patch is redundant here).
# ALREADY_PRESENT: 78f8f60c (patch validate_enterprise in TestDoctorOllamaDocker, Mesh-2072 —
#   fixes a fail-CLOSED Enterprise-Grid gate; the fork's validate_enterprise is DEFAULT-OPEN
#   (slack/enterprise.py:90, fails open with no allowlist), so the fork tests are green by a
#   more fundamental divergence. Porting the patch = pointless churn. Verified by running them.)
# SKIP (frontend): 9099180 (frontend AGENTS.md v2.7 — diverged convention doc mixing present
#   Monaco/themes with absent LCARS/bikini-bottom + Artifactory ArtifactSharePanel/RemoteArtifactCard;
#   fork's website/AGENTS.md is its own de-Amazoned voice).

# --- Batch-13 (cron dual-repo sync 2026-06-10) -------------------------------
# Fresh branch sync/beta-2026-06-10d off origin/mainline (prior CR-281070110 merged).
# 9 candidates (4 backend + 5 frontend); 7 PORTED/PARTIAL, 2 SKIP.
# PORTED: f42ef2d9 (MCP slash-key alias — new mcp_utils.mcp_server_alias; kiro tool
# resolution, fixes the fork's npm:@playwright/mcp server), fac5fcbb (secretary
# retention config-roundtrip — ANTI-MISS save: titled fix(secretary) but the fork
# ships SecretaryConfig with these fields and load() dropped them, a real fork bug),
# 3838bcc (sidebar rename caret), 50ea94e (browse toggle per-session, Mesh-2055),
# b7d3039 (Ctrl+Z prompt undo/redo, Mesh-2029), fa3c5ed (Cmd/Ctrl+Click new tab),
# 977aa9e (flatter chrome + IconButton primitives + tool-name chip; PARTIAL —
# dropped the absent AimPackageManager.tsx hunk). 977aa9e+3838bcc committed together
# (shared a ChatSidebar diff hunk; splitting risked corruption).
# PARTIAL: 3a017786 (python-docx 1.x floor for py3.10 — applied to setup.cfg, NOT the
# Brazil Config which the public build ignores).
# SKIP: 39d22f8a (mcp_gateway hardening — dir ABSENT, 100% confined), 3a017786's
# Brazil Config hunk (setuptools build).
# 2fee03d/50ea94e-style tests that imported the absent secretarySlice were re-authored
# fork-correct. CR-281120970. Both DMGs arch-verified (arm64 + x86_64).

# --- Batch-12 (dual-repo sync 2026-06-10, after CR-281050885 merged) ---------
# Fresh branch sync/beta-2026-06-10c off origin/mainline (prior CR merged, no
# longer stacking). 6 candidates (3 backend + 3 frontend); 4 PORTED, 2 SKIP.
# PORTED: d00e6ac6 (backend terminal scrollback ring buffer + reconnect replay —
# RE-ANCHORED redaction: imports kiro_crew.security NOT mesh_claw.redaction, and
# tuple-unpacks `text,_ = redact_*()` since the fork's redactors return
# tuple[str,list[str]] while upstream's redaction module returns str — a verbatim
# copy would have crashed; added _redact_terminal helper + 6 regression tests),
# fbdf391 (frontend terminal session persistence/rename/reconnect — LABELS_KEY
# symbol-mapped to kirocrew-terminal-labels; renameSession already existed),
# 2fee03d (input interactive while stopping, Mesh-2004 — test re-authored fork-
# correct: dropped the absent secretarySlice the upstream test imported, matched
# the fork's flat dashboard-slot shape), 76a7fa3 (force-kill arming debounce +
# consequence-stating tooltip — new utils/stopDebounce.ts, per-slot Map ref).
# SKIP: 527459a0 (config-baseline regen — adds ONLY promptfarm + auto_update_aim
# (AIM) schema, both absent/internal; fork baseline correctly has neither),
# 641a6f0c (per-agent provider override — SKIP_NONKIROACP: re-introduces the
# deleted multi-provider dispatch factory with bedrock/claude_code branches;
# meaningless under the enum ["acp"], no ungated generic hunk to salvage).

# --- Batch-11 (cron dual-repo sync 2026-06-10) -------------------------------
# 6h cron run. 8 candidates (3 backend + 5 frontend); 3 ported, 4 SKIP, 1 ALREADY_PRESENT.
# Stacked on batch-10 (CR-280980741 still pending — origin/mainline lacks it + the
# KiroACP-only refactor, so the existing sync branch is the correct base, NOT a fresh
# branch from origin/mainline). PORTED: 03e2ba43 (slack tool-card elapsed time),
# 0dc8ff1a (kirocrew cron preview), 964b7e5 (board card layoutId per column). SKIP:
# 599d6f64 + ed7c87f (promptfarm publish — absent), 7342c6e (secretary — absent),
# 7eb7d6c (Cloud Sync SyncPanel — absent). ALREADY_PRESENT: a673571 (MCP OAuth banner
# — fork ChatPage already routes m.role==='mcp_oauth').

# --- Batch-10 (dual-repo sync 2026-06-10) ------------------------------------
# First sync to cover BOTH repos. Backend window e62422ae..29ad9776; frontend window =
# MeshClawWebsite beta since the 2026-06-02 fork snapshot through 94e92af. 97 commits
# triaged. Ported 67: 11 backend (9 KEEP + instances feat 38864fd9/d17306e1 + Projects/
# Autopilot apps 29ad9776) + 56 frontend (54 wave-ported + instances UI e341b727/8406d8da
# + 94e92af). The 6h auto-sync cron fired mid-batch and surfaced 29ad9776 + 94e92af (a
# matched pair, CR-280786172) — folded into this batch rather than run as a competing
# pipeline (the tree was mid-port). The rest were SKIP_INTERNAL (absent subsystems),
# SKIP_NONKIROACP, or ALREADY_PRESENT — see the CR.
#
# instances/ feature: previously DEFERRED, now owner-cleared for external release.
# Ported as a PARTIAL de-Amazon port (backend 38864fd9 + d17306e1, frontend e341b727 +
# 8406d8d): kept the generic multi-instance registry / port-allocator / SSH tunnel
# manager (plain OpenSSH, not coupled to the stubbed outbound tunnel/manager.py) + UI;
# DROPPED the Midway SSH-cert watchdog (instances/midway.py) and its security.py
# carve-out — porting it would re-open ~/.ssh, forbidden by MIGRATION_PLAN.md L383.
#
# Backend SKIPs in-window: 437262df (Midway connectivity docs), 5c7b2c93 +
# 1408ca73 (real tunnel-probe machinery the fork omits), 33658c12 (GitFarm app
# registry). Frontend SKIPs: writing-review, secretary, code-reviewer, auto-research,
# mcp_gateway UI, GitFarm sync, RUM telemetry, AIM, Harmony Artifactory, lcars +
# bikini-bottom themes (absent subsystems); Bedrock image-limits + Claude-Code agent
# picker (SKIP_NONKIROACP). The fork's website/ provider selector was de-Amazoned to
# KiroACP-only in this batch (f1883d2e — Bedrock removed from the UI to match the
# backend enum ["acp"]).
