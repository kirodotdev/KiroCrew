# Last-synced upstream tips (one SHA per tracked branch/repo).
# Updated at the END of every sync run (Step 1/7). The meshclaw-sync skill bounds
# the candidate set by these SHAs — see skills/meshclaw-sync/SKILL.md Step 1.
#
# This file records the sync BOUNDARY (what has been triaged). For the per-commit
# record of what was deliberately NOT ported and why — including the open
# human-decision (DEFER/flagged) items — see skills/meshclaw-sync/left-out.md.
# Format: "<branch-or-repo> <sha>  # <date> <note>"
#
# TWO upstream repos are tracked (the fork bundles both into one package):
#   - backend  = MeshClaw          (package mesh_claw  → src/kiro_crew/)
#   - frontend = MeshClawWebsite    (the React/Vite SPA → website/src/)
# Boundary semantics differ per repo (see SKILL.md Step 1):
#   - backend  : SHA-range incremental (the fork shares the backend's content lineage).
#   - frontend : the fork's website/ is a DIVERGED partial content-snapshot (~2026-06-02),
#                so frontend sync is by CONTENT within the post-snapshot window, not a
#                clean SHA range. This SHA records the upstream tip last triaged.

# --- Batch-36 addendum (folded into PR #92 / branch sync/beta-2026-07-15-batch35) ---
# After batch-35's ports landed, a third upstream wave arrived (backend
# 3cae0aad..f9b37d8c = 9 commits; frontend 340cfa9..e7c1de9 = 3). Folded into the
# SAME PR. Tally: 7 backend PORTED + 2 backend SKIP_INTERNAL; 1 frontend PORTED +
# 2 frontend ALREADY_PRESENT/no-op.
# BACKEND PORTED (7):
#   ff921945 isolate per-app spawn failures at gateway boot (try/except so one
#     app's fail-closed sandbox error doesn't crash boot); ca356e53 fold playwright
#     e2e suite (de-Amazoned harness, dropped @amzn/chromium/brazil machinery);
#     a6c1680e [SECURITY] sandbox agent-influenced spawns (Talos 92e24570) —
#     sandboxed_spawn_argv chokepoint (wrap_argv + scrub_env) routing
#     mcp_discovery.probe_server/git_coord/task_executor; 8993ac32 grill skill
#     facts-vs-decisions (dropped _GRILL_CHALLENGE_PROMPT, absent); 689fe139
#     orphan-sweep spawn-grace + dead-runtime respawn (dropped doc-links-gated
#     chat_handlers/knowledge hunks); f9b37d8c macOS-26 sandbox-exec — REMOVES the
#     >=26 version gate again (empirical probe trusted), superseding batch-35's
#     interim ef4f05b gate re-add.
#   549fd384 (zero-CLI Slack settings API + subagent approval-slot routing) was
#     FULLY ALREADY_PRESENT (fork PRs #65/#68) — nothing ported, boundary advances.
# BACKEND SKIP_INTERNAL (2):
#   88e703ab (register Dev Fleet in app-registry) — internal MeshClawApp-DevFleet
#     GitFarm app; fork registry is []. 38b4fb97 (auto-ingest Chorus/Pippin/Quip
#     doc links) — entirely internal doc sources (chorus.aws.dev/pippin.amazon.dev/
#     quip-amazon.com) via the stubbed ReadInternalWebsites bridge.
# FRONTEND: 273cb0e playwright load-stability PORTED (re-anchored to fork IA);
#   c0fb568 (Slack panel + conversation-map close-on-click-away) ALREADY_PRESENT
#   (fork PR #65/#84); e7c1de9 registry regen a no-op.
# Codex findings from this + batch-35 remain tracked as inherited fix-in-both below;
# a6c1680e/ff921945/f9b37d8c RESOLVE several of them (wrap_argv-raises boot crash,
# macOS-26 hard-fail, unscrubbed agent-spawn env).
#
# --- Batch-35 (dual-repo beta sync 2026-07-15, SECURITY-FIRST round) ---
# Branch sync/beta-2026-07-15-batch35 off origin/main. Backend window
# a7736388..3cae0aad (5 commits), frontend window 4b9d6255..340cfa9 (4 commits).
# User directive this round: make sure ALL security commits are synced; two
# mid-round upstream waves were folded into the SAME PR. Tally: 9 PORTED =
# 5 backend + 4 frontend. ZERO SKIP — every candidate landed. SECURITY
# COVERAGE: no hardening silently dropped.
# BACKEND 5th commit (folded after the first 4):
#   3cae0aad -> "fix(sandbox): restore macOS-26 sandbox-exec gate + macOS test
#     fixes" — upstream fixup to THIS batch's platform_compat/Talos-sandbox work:
#     re-adds the platform.mac_ver() macOS-26 (Darwin 25/Tahoe) short-circuit that
#     383eae45 had dropped (+ restores module-level `import platform` and the
#     test_sandbox_probe macOS-26 assertions), plus macOS-only test hardening
#     (tmp_path->/tmp, passthrough fixtures, PEERCRED mocks, numpy skipif).
#     session_pid.get_ppid / acp isort were already at parity. Upstream-CR
#     CR-289598418.
# FRONTEND 4 (window 4b9d6255..340cfa9, all KEEP):
#   3c290bb command-palette Settings provider (codegen registry + deep-link +
#     useSettingHighlight; adapted to the fork's KiroACP-only tab set, dropped
#     absent Provider/Secretary/Sync/TaskKeeper panels); 8345970 sidebar
#     folder drag-move (collectFolderSubtreeIds cycle-safe reparent; dropped the
#     Radix submenu affordance the fork's inline-toolbar sidebar lacks); d29d798
#     Semantic Memory table render cap (100 rows + filter); 340cfa9 store
#     per-slot cache eviction on sseSlots + 200-notification ring cap.
# PORTED (4 backend, chronological / parent-first):
#   eaf62582 -> "feat: native Windows support via platform_compat shim" — the
#     fork ADOPTS Windows support (user reversed the prior macOS+Linux-only stance).
#     NEW src/kiro_crew/platform_compat.py (cross-platform file-lock/process/signal/
#     chmod/resource/strftime/console shim) + routing ~50 live modules through it;
#     replaces the dangerous os.kill(pid,0) liveness probe with pid_exists. PARTIAL:
#     dropped hunks for fork-absent modules (providers/claude_code.py, cc_agent.py,
#     aim_cli.py, secretary.py, knowledge/connectors/quip.py). Docs flipped to
#     "macOS, Linux, and Windows" (CLAUDE.md/AGENTS.md/README/INSTALL/FEATURES +
#     cli/session/acp-client specs) + new docs/WINDOWS_INSTALL.md. macOS/Linux
#     behavior byte-for-byte unchanged (Windows branches added, not substituted).
#     Task Mesh-2329.
#   383eae45 -> "fix(security): remediate 10 Talos AppSec pentest findings" —
#     [SECURITY] sandbox launcher hardening (CAP_SYS_ADMIN drop + NO_NEW_PRIVS,
#     seccomp-BPF, hardlink inode scan), fail-closed unsandboxed-exec gate
#     (agent.sandbox_allow_unsandboxed_exec opt-in), quote-evasion-resistant
#     git-publish detection, IMDS/env-credential access detection, command-path
#     normalization, apply_resource_limits, SEL 0o600, snapshot sel_hmac.key
#     exclusion, orphaned-session-root sweep. Kept the fork's STRICTER fail-closed
#     git-push gate (did not graft upstream's looser quoted-feature-branch allow).
#     Keystone floor preserved.
#   e99840c3 -> "fix(vector-memory): guard FAISS dedup against writes without
#     embeddings" — correctness guard (emoji stripped from subject).
#   1b0585bb -> "fix(security): port Talos OSR AppSec (app admission, HMAC, host)"
#     — [SECURITY] NEW apps/admission.py App-Kit admission gate (banned/approved,
#     optional-HMAC signature, fail-closed), backend.entryPoint path-containment,
#     third-party load gate (apps_allow_third_party opt-in), body-bound App-Kit
#     HMAC, added ~/.kirocrew/app_admission.json to the sensitive floor. DISTINCT
#     from + does NOT weaken the CPP platform/admission.py plugin gate
#     (CONTRACT_VERSION stays 1). Dropped the publish_sync.py hunk (internal
#     Artifactory engine, absent from fork).
# INHERITED-UPSTREAM findings from PR #88 remain fix-in-both (see left-out.md);
# NOT re-addressed here. KiroACP invariant HELD; de-Amazon audit clean.
#
# --- Batch-34 addendum (folded into PR #88 / branch sync/beta-2026-07-15-batch33) ---
# After the batch-33 ports landed, three more backend commits appeared upstream
# (d757fd9d..a7736388) plus the batch-33 DEFER (ed984a05) was resolved. Triaged
# by content and folded into the SAME PR (not a new one), advancing beta to the
# new tip a7736388. Frontend tip unchanged (no new MeshClawWebsite commits).
# PORTED (2 backend, generic-core KEEP):
#   ed984a05 -> "fix(security): remove legacy HTML dashboard entirely" — was the
#     batch-33 DEFER; the fork still carried static/dashboard.html + js/dashboard.js
#     + purify.min.js + css/dashboard.css + cli-mode.css + _HTML_PATH wiring + the
#     legacy-XSS test, so this is a genuine generic-core XSS-surface reduction.
#     index() now serves dist/index.html only (guidance page on missing bundle);
#     /static route, theme assets, kirocrew-logo.png, and _BASE_CSP preserved.
#     Upstream-CR CR-289374220 / Talos V2285871874.
#   a7736388 -> "feat(context): scale injected-memory budget to model context
#     window" — the fork had the fixed _CONTEXT_BUDGET_BASE=165k this refactors to
#     scale linearly with the active model's window (1M reference = factor 1.0,
#     default deployment byte-for-byte unchanged, pinned by test). Generic core.
# SKIP:
#   889f87b0 (register 6 chapmang apps in app-registry) — SKIP_INTERNAL: all six
#     are internal MeshClawApp-* GitFarm gateway apps (incl. taskei-agent); the
#     fork's src/kiro_crew/apps/app-registry.json is deliberately [] and these
#     point at nonexistent internal repos. Upstream-CR CR-288078910.
#
# --- Batch-33 (dual-repo beta sync 2026-07-15) ---
# Branch sync/beta-2026-07-15-batch33 off origin/main 327be9d. Backend window
# 84d8abf3..d757fd9d, frontend window ab0d8d2c..4b9d6255, triaged by content.
# 6 PORTED = 5 backend + 1 frontend, one fix per commit, in chronological order.
# BACKEND (5, all generic-core KEEP): 36527cd3 Heimdall pentest security.py hardening
#   (CWD-bypass absolutize in _path_in_home_dirs, _CRED_CLASS quote/query delimiters,
#   _PEM_HOLD_RE streaming hold-back, empty-user DB-URI, relative-traversal verb-agnostic
#   block); 7cc8217d consolidated Talos redaction/SSRF/XPIA follow-ups (PEM round-3,
#   JWT/JWE+JSON-Bearer union + StreamRedactor fail-closed, entropy glued-secret, SSRF
#   trailing-dot, XPIA round-2) — built on 36527cd3, ported first; 82560fb7 exfil-URL
#   path/raw-IP scan (_HARD_CREDENTIAL_RE full-path + _URL_RE raw-IPv4/IPv6) + bash-egress
#   DENY gate (_BASH_EXFIL_PATTERNS/audit_bash_exfiltration wired into hooks.on_tool_call
#   above the governance block + mcp_cron governance-routed OR chain); 56fbb774 mcp-gateway
#   claim-push caller identity (new claim.py, _StubConn/_CONN_INDEX/_apply_claim in gatewayd,
#   ancestor_pids + backoff in stub, schedule_claim on both rekey paths; AcpClient socket
#   read defensively via getattr); 0917c9aa queue chat during sub-agent runs (is_system_injection
#   + _dequeue_next_system_message drain filter, serialize_slots shared serializer +
#   subagents_running, api_chat idle-case hold, chat_runner drain honors hold, debounced
#   slots push on subagent_spawn/done). FRONTEND (1): 4b9d6255 reflect sub-agent runs in
#   composer busy state (selectComposerBusy/selectSlotSubagentsActive + subagents_running
#   type, both ChatPage + ChatPane gated on the shared selector).
# ALREADY_PRESENT: 791c0cb5 (backend Board removal / Channels hide) + c64af69 (frontend Board
#   removal) — the removal is fully done on the fork via merged PR #78 (Board dir/tests absent,
#   Channels hidden flag + AppsPage filter live). SKIP: cdb2d135 (CC deny union-merge —
#   SKIP_NONKIROACP, confined to the deleted cc_agent.py / _CC_DENY_PATTERNS), d757fd9d
#   (brazil ws-create timeout — SKIP_INTERNAL, patches _brazil_build_app_locked which is
#   absent from the fork; the fork's timeout=30 is an unrelated public git-pull step).
# DEFER: ed984a05 (Talos V2285871874 remove legacy HTML dashboard — KEEP-worthy generic-core
#   XSS-surface reduction that applies to the fork, but a 16-file −2409-line behavior-changing
#   removal that landed in the beta window AFTER the batch verdicts were adversarially verified;
#   held for the next verified batch, recorded in left-out.md "Still open").
# KiroACP invariant HELD (no provider glue re-added; _is_claude seam intact, 19 refs).
#   De-Amazon audit: 0 LIVE couplings on added backend/doc/frontend lines (only inert Talos
#   IDs + Mesh-NNNN/CR-NNNNNN provenance refs). Codex-flagged StreamRedactor.feed cross-chunk
#   credential-drop gap in 7cc8217d is inherited VERBATIM from upstream (not a regression) —
#   joint fork+upstream follow-up, NOT fixed here. Verify: per-commit pytest (touched files)
#   + flake8 + isort + mypy all green (test_security, test_hooks, test_mcp_cron, test_mcp_gateway_claim,
#   test_queue_during_subagents, test_slack_gateway); frontend tsc -b clean + chatSlice vitest green.

# --- Batch-31 (dual-repo beta sync 2026-07-15) ---
# Branch sync/beta-2026-07-15 off origin/main 2567734. Backend window 5a62fdd7..84d8abf3,
# frontend window 2bf7ede..ab0d8d2c, triaged by content. 21 commits ported IN CHRONOLOGICAL
# ORDER (oldest first), one fix per commit, ZERO left out (no SKIP / DEFER this batch).
# Tally: 21 PORTED = 15 backend + 6 frontend, incl. 3 PARTIAL (cec9758e electron single-
#   instance-lock rescue, a5e914cb slash-desc fallback drop /workflows, 834470ac backend
#   slash-desc drop /workflows assertion).
# BACKEND (15, security-heavy — all generic-core KEEP per the "SSRF/redaction/XPIA/SEL =
#   generic core" rubric): af5306f5 conftest AIM-skills isolation; 1d9968b0 episodic-memory
#   injection scan (XPIA persistence); 30e126ca JWT/Bearer redaction; 6068f9db bare 40-char
#   AWS-secret redaction; 4e5ad699 SSRF base guard on remote embedding URL; 1ebb2b20 remove
#   flaky test_discovery_runs_off_event_loop; 2159a709 SEL HMAC-key + audit-log sensitive-path;
#   834470ac backend slash-command descriptions; 975cc548 full PEM private-key body redaction;
#   220fad51 SSRF alternate-IPv4-encoding block (follow-up to 4e5ad699, ported same wave);
#   b19e6e1b Slack thread-parent/meta XPIA screen (moves _INJECTION_PATTERNS ->
#   vector_memory_constants, re-exported); c035795f taskrunner TOOL_AUTO_APPROVE (re-applied on
#   top of the fork governance kwargs); 0a010ec2 bound background MCP probe fan-out; 58be644c
#   folder-watcher build-output-dir prune; 84d8abf3 webbrowser.open offload to subprocess_executor.
# FRONTEND (6): cec9758e electron single-instance lock (PARTIAL — the ONLY generic hunk of an
#   unsupported-Windows commit; all Windows scaffolding dropped, electron stays ^33); a5e914cb
#   slash-command autocomplete descriptions (PARTIAL — dropped /workflows, absent in the fork);
#   291660b0 pin/unpin current-session palette action; 77eaf255 changelog viewer in Settings >
#   About (kept fork's GitHub tooltips, NOT code.amazon.com); 71a5d059 findBestOccurrence empty-
#   anchor guard + V-shaped early-exit; ab0d8d2c inline blinking streaming caret.
# KiroACP invariant HELD (no provider glue re-added; the 834470ac claude_code branch is the kept
#   dormant seam, filled via the shared map). De-Amazon audit: 0 LIVE couplings (inert Talos IDs
#   + builder-mcp test-fixture string only). Verify: per-commit pytest (touched files) + flake8 +
#   isort all green; frontend tsc -b clean + touched vitest green.

beta f9b37d8c20fe75ce268c9eb356ce5bfd2a909e72      # 2026-07-16 MeshClaw backend triaged tip (batch-36 addendum, folded into PR #92; advanced from batch-35 3cae0aad). Window 3cae0aad..f9b37d8c (9 commits): 7 PORTED + 2 SKIP_INTERNAL — see batch-36 note above. Window 84d8abf3..d757fd9d triaged by content; 5 backend KEEP ported (36527cd3 Heimdall pentest security.py hardening, 7cc8217d Talos redaction/SSRF/XPIA follow-ups, 82560fb7 exfil-URL path/raw-IP + bash-egress gate, 56fbb774 mcp-gateway claim-push caller identity, 0917c9aa queue chat during sub-agent runs). 791c0cb5 (Board removal / Channels hide) ALREADY_PRESENT (merged fork PR #78). SKIP: cdb2d135 (CC deny union-merge — SKIP_NONKIROACP, confined to deleted cc_agent.py), d757fd9d (brazil ws-create timeout — SKIP_INTERNAL, Brazil installer path absent from fork). DEFER: ed984a05 (Talos legacy-HTML-dashboard removal — KEEP-worthy generic-core XSS-surface reduction, but a 16-file behavior-changing removal that landed after the batch verdicts were verified; held for the next verified batch — see left-out.md).
frontend-beta e7c1de9ca2535a198ca1a3057a83271fb6998664  # 2026-07-16 MeshClawWebsite triaged tip (batch-36 addendum; advanced from batch-35 340cfa9). Window 340cfa9..e7c1de9 (3 commits): 273cb0e playwright load-stability PORTED, c0fb568 Slack panel + conversation-map ALREADY_PRESENT (fork PR #65/#84), e7c1de9 registry regen no-op. Window 4b9d6255..340cfa9 (4 commits) all PORTED (store cache-evict/notif-cap, semantic-memory render cap, sidebar folder drag-move, command-palette Settings provider). See batch-35 note above. Window ab0d8d2c..4b9d6255 triaged by content; 1 frontend KEEP ported (4b9d6255 reflect sub-agent runs in composer busy state — selectComposerBusy/selectSlotSubagentsActive). c64af69 (Board removal frontend) ALREADY_PRESENT (merged fork PR #78). No frontend SKIP/DEFER this batch.
mainline 45dcb603f117886648d79c7dde05e82d2d8cda2d  # 2026-07-15 MeshClaw mainline tip (upstream), unchanged since batch-27 (batch-33 re-confirmed). No NEW mainline-only content to port (all in-window candidates reached the fork via beta-braveheart; every mainline-only delta is a release-merge or already triaged via beta).
# NOTE: this 'mainline' SHA is the UPSTREAM MeshClaw mainline tip (for the mainline-only
# candidate check), NOT the fork's mainline — batch-12 mistakenly recorded the fork SHA
# (796e21c5) here, which broke the `$MAIN..origin/mainline` scan. Corrected in batch-13.

# --- Batch-30 (dual-repo sync 2026-07-14) ---
# Branch sync/beta-2026-07-14 off origin/main 6df6dad. Backend window 3b679515..5a62fdd7,
# frontend window 2a70682..2bf7ede, triaged by content.
# Tally: 19 keepers PORTED (12 backend + 7 frontend, incl. 3 PARTIAL: ebad8b6c credit-usage,
#   41dec169 bulk-model, dd1d652b SafeChildWatcher) + 7 left out.
# LEFT OUT: 4 SKIP_INTERNAL = 21e296f1 (secretary self-alert — secretary absent), 819ffc16 +
#   3b4858bd (ADC Ops Center + kratos app-registry rows — registry is [] by design, internal
#   Brazil pkgs), 9471ba1b (setup.cfg->setup.py console_scripts — Brazil CPython310 workaround,
#   fork uses setuptools>=64). 2 SKIP_NONKIROACP = c143b659 (cc agent-skills sync — mirror.py
#   absent, claude_code-gated), 2e976a72 (claude knowledge-worker approval_mode — CCWorker path).
# 1 DEFER = 4964a3e (credit-usage popup FE half of ebad8b6c): reads credits_overage/percentage
#   the fork's text-scrape backend does not yet emit; porting alone regresses the pill. Port as a
#   pair once the backend text path emits the canonical shape. Flagged for human review.

# --- Batch-28 (dual-repo sync 2026-07-13) ---
# Branch sync/beta-2026-07-13 off origin/main 4f942f3. 58 candidates triaged (31 backend in
# 5dbb7778..e870fed4 + 20 frontend in 3858e573..20f18813 + 7 batch-27 DEFER re-audits) via a
# 116-agent adversarial workflow (analyzer + skeptic per candidate, decide by content).
# Tally: 41 keepers PORTED (22 backend + 19 frontend, incl. 9 PARTIAL) + 17 left out.
# LEFT OUT: 10 SKIP_INTERNAL = 0e7322ce (MarkBin publish provider — publish subsystem absent),
#   0fe90c30 (mimir SIM assignee — mimir absent), d87672ea (file-backed publications — publish
#   absent), 4a41b8a3 (taskkeeper slack-user-content strip — taskkeeper absent), 5cdfb619
#   (secretary keyword-negation regex — secretary absent), a5979e42+6055397 (Chorus comment-edit
#   BE+FE — Chorus publish absent), d0a78b07 (defusedxml dead dep — no fork consumer), 7cfeed18
#   (Work Summary app registry row — app-registry.json is [] by design), 1984ccc (publish UI
#   supports_shared gate — publish surface absent).
# 3 ALREADY_PRESENT = 70898ca5+d5819090 (AcpRuntime teardown/recycle — fork PR #43 did it
#   natively), e4fc0ce (follow-up-bar scroll arrows — via fork PR #45).
# 4 DEFER = 3e5d7132 (workflows-engine script vetting — PR #57 carries all three fixes by
#   content), f8383887 (hero SVGs — needs the fd633154 builtin-ui materialization substrate),
#   0000f561+f746d60 (anchored/durable artifact-comment subsystem absent — port when the
#   affffcff base lands).
# BATCH-27 DEFER RESCUES ported this batch: 3858e573 CommandPalette tab-strip scroll (KEEP),
#   5360aa4d split-view live tool streaming (KEEP — the per-slot substrate now exists),
#   3acce399 RL-v2 UI (PARTIAL — engine chooser dropped pending PR #57). d2240c48 resolved as
#   PARTIAL (generic _dm_owner redacting DM exit point ported; tunnel hunks remain absent).
# Verify (independent cumulative pass): backend pytest 11834 pass / 53 skip, ONLY fail is the
#   KNOWN pre-existing test_skills mtime flake (test_mutators_invalidate_iter_cache) — confirmed
#   failing identically on origin/main in a clean worktree, untouched by this batch; flake8
#   CLEAN on all 52 touched .py files. Frontend tsc -b CLEAN + 3303 vitest pass (285 files,
#   1 skip). Step-6 de-Amazon: 0 LIVE couplings (single grep hit = "incognito" substring-matching
#   "cognito" in a comment). KiroACP invariant HELD (provider enum ["acp"], ACP_BACKEND_CLAUDE
#   seam 23 refs untouched, no provider selector/cc_*/bedrock_* re-added). Inclusive-language
#   gate on added lines: 0 hits.
# 42 commits on branch (41 ports + 1 review-nits chore c646ab9 folded onto fb9d6f9). Boundaries:
#   beta 5dbb7778->e870fed4, frontend-beta 3858e573->20f18813, mainline unchanged 45dcb603.

# --- Batch-27 follow-up (2026-07-10): folder icon picker (46c7a01) ---
# Ported the un-triaged-tail item "folder icon picker" (MeshClawWebsite 46c7a01,
# 2026-06-13, pre-window) as a batch-27 follow-up per Zezhen's ask. Adapted the
# upstream fixed-portal picker to the fork's Radix DropdownMenu idiom (6d8008a migration);
# curated FOLDER_EMOJIS grid + isSingleEmoji free-input + reset-to-auto, wired into both
# list-view and board-view hover action groups. Emoji support intentional per Zezhen; AGENTS.md
# + AUTOSDE exception documented. No boundary advance (commit is within the pre-window tail).

# --- Batch-27 (dual-repo sync 2026-07-10, ultracode comprehensive, FIRST on the GitHub fork) ---
# RUN IN THE PUBLIC GITHUB CHECKOUT /workplace/bolichen/KiroCrewGitHub (origin github.com/
# kirodotdev/KiroCrew), which ships via PULL REQUEST — NOT the internal Brazil workspace/CRUX
# path of batches 1-25. Branch sync/beta-2026-07-10 off origin/main 8355fc6 (safety tag batch27-base).
# KEY CONTEXT: the boundary file was STALE (recorded batch-25 beta 6cf3aae0) but the fork had since
# done batch-26 (PR #18) + a large GitHub-NATIVE porting wave (PRs #10-#38) that did NOT cite the
# upstream SHA — so the fork is BIDIRECTIONAL with upstream, NOT strictly downstream. Every candidate
# was therefore triaged BY CONTENT against the fork file, never by SHA reachability.
#   Window: backend beta 6cf3aae0..5dbb7778 (88 candidates) ; frontend 9b512ca..3858e573 (47) = 135.
#   mainline advanced 199d7853..45dcb603 (no new mainline-only content).
# 135 candidates triaged via a 270-agent adversarial Workflow (analyzer + skeptic per commit, decide
# by content; 2 skeptics hit an API error and were hand-confirmed: 2dce8f8a/8e58cadf both KEEP). Tally:
# 73 PORTED (58 KEEP + 15 PARTIAL: 47 backend + 26 frontend) + 62 left out (22 SKIP_INTERNAL, 4
# SKIP_NONKIROACP, 29 ALREADY_PRESENT, 7 DEFER).
# HEADLINE backend: d759d3b3 SEL-audit off-loop (PARTIAL — bulk ALREADY_PRESENT, fork-origin CR-286485989),
#   255ef0b8 opt-in lazy-load skills (new skill_usage.py + skill_search), 9daac30f auto-research
#   recursive exploration, 2e960fe2 mid-turn steer (PARTIAL — ACP layer already present, ported the
#   dashboard wiring), 647cc057 deny widget-origin orchestrator auto-run, 2dce8f8a instances SO_REUSEADDR
#   reconnect, 0a22f1c7 restart-bot-from-Slack, 41ad420c strip PYTHONPATH/PYTHONHOME from foreign MCP,
#   8a11ca98 preserve data/ on app install, c4b62689 forward-Slack-message shortcut, 8b9dfc18
#   server-authoritative workspace theme, 52deca0/b182453 Claude Fable 5 (model-registry, PARTIAL).
# HEADLINE frontend: a7f25225 native recursive split-pane grid (biggest FE port), 12bfe897 Command
#   Palette / Search Everywhere (PARTIAL), 26ce1d57+033951d Steer button + visible ack, b66599c unified
#   store-connected SessionActionsMenu, 07e9f27 Pinned filter, folder-visibility + search-by-folder.
# LEFT OUT (highlights): SKIP_INTERNAL = WritingReview (7608b99f/321f9fbc/eed2773d), mcp_gateway
#   (7bb86a44), secretary (8b5351f1), code-reviewer FE (3a182608), artifact/Chorus publish (9a696e65/
#   5e13d8b7), AEA-tunnel (92104fcc/a179b9e0/b944c74d), toolbox/Brazil/mailmap (9bbcb048/cb6f8f19/
#   c637016b/7f86241d), midway (4f753ed0), builder-mcp security-assistance (cec38579), app-registry
#   (1f6b7982/81d19cef), version bumps (0e9c9ffc/d53b497b). SKIP_NONKIROACP = claude_code usage/cost
#   tracking (3997c2ae/7b74179b/c6bc2744/f6e38b3b — the fork has no per-provider spend surface).
#   DEFER (7, human review): 3e5d7132 (workflows engine absent), f8383887 (deploy_web/workflows hero
#   assets), d2240c48 (tunnel-off warning follow-up), 0000f561/3acce399/5360aa4d (FE: comment-anchor
#   subsystem / RL-v2 UI / split-view rich tool-stream — absent substrates), 3858e573 (CommandPalette
#   tab-strip scroll — pairs the ported 12bfe897, tiny follow-up worth picking up next batch).
# Verify (independent cumulative pass): backend flake8 CLEAN (51 touched .py) + pytest 11260 pass/52
#   skip/1 fail (ONLY the pre-existing test_skills mtime flake — confirmed failing identically on
#   origin/main, untouched by this batch). frontend tsc -b CLEAN + 3156 vitest pass (273 files, 1 skip).
#   Step-6 de-Amazon: 0 LIVE couplings (all inert comments/labels/test-strings). KiroACP invariant HELD
#   (provider enum ["acp"], ACP_BACKEND_CLAUDE seam intact, no provider selector/cc_*/bedrock_* re-added).
# 73 commits on branch. DMG rebuild OWED (macOS-only; this ran on Linux — mode:port ceiling). PR against
#   main authorized by the user for this manual run.

# --- Batch-25 (dual-repo sync 2026-07-03, ultracode comprehensive) -----------
# STACKED on the batch-22+23+24 worktree branch sync/beta-2026-06-27 AFTER rebasing all 141 commits
# onto the advanced origin/mainline (55d0ccb = fork-native "Pentest round 3": fail-closed SEL audit,
# REST input validation on /api/lessons+/api/crons, legacy dashboard.js XSS escaping). Rebase resolved
# 2 conflict hunks, both in dashboard/handlers/cron.py (validation-import union + drop the redundant
# unvalidated agent_id re-assignment, keep project_path); 233 collision-regression tests green. One
# coherent CR for 22+23+24+25. Window since batch-24:
#   backend  beta-braveheart bfa7b4e8..6cf3aae0  (13 candidates ; mainline unchanged 199d7853)
#   frontend MeshClawWebsite  0f2c062..9b512ca    (2 candidates)
# 15 candidates triaged via a 30-agent adversarial Workflow (analyzer + skeptic per commit + synthesis;
# 1 frontend analyzer 0d9797e hit the StructuredOutput retry cap -> triaged by hand). Tally: 9 PORTED
# (6 KEEP + 3 PARTIAL: 8 backend + 1 frontend) + 6 left out. ZERO skeptic verdict-flips (all confirmed
# on the merits against post-rebase fork content).
# KEEP (6): 4aba97e9 app-MCP registration health-gate (dead HTTP mcp url no longer poisons EVERY kiro-cli
#   session via ~/.kiro/settings/mcp.json — the fork's SOLE provider path; new _live_port_for +
#   _gate_mcp_registration + boot reconcile, applied ON TOP of fork-native governance re-vet); 284af3ea
#   cookie-Secure via is_https_request (X-Forwarded-Proto:https from a loopback peer — any TLS-terminating
#   proxy/tunnel; 4 cookie sites; de-Amazoned the tunnels.lab.aws.dev docstring); b0140b0c Permissions-Policy
#   clipboard header at the no_cache_middleware chokepoint (Chrome 143+; extracted _apply_security_headers,
#   DROPPED upstream's tunnels/cognito/RUM CSP endpoints); f4acf37e ACP tool-stall watchdog folds
#   _last_activity (session-keepalive) into the clock (client.py:2375, mirrors the stale-turn check);
#   210baed2 pytest-xdist worker cap min(cpu,32) (conftest hook; MESHCLAW_->KIROCREW_MAX_TEST_WORKERS);
#   0d9797e (FE) per-tab remote-host config keyed by port + per-tab token refresh (new electron/host-config.js
#   + validation REMOTE_PORT_RE/REMOTE_PATH_RE + main.js per-port refactor; RESTORED the fork's OSS
#   DEFAULT_REMOTE_BIN=~/.local/bin/kirocrew vs upstream's ~/.toolbox default; kept toolbox as candidate).
# PARTIAL (3): 99041753 (bulk pentest-r2+bg-reconnect ALREADY_PRESENT via fork-native 1c8f980+aef7a86;
#   residual ported = the 2 edit-mode Slack preview sites still using the truncating throwaway
#   StreamRedactor().feed(accumulated) -> redact(accumulated) at handler.py:2798/2893 + 1 regression test;
#   DROPPED all ALREADY_PRESENT security.py/hooks.py/chat_runner.py/server.py hunks + the _SENSITIVE_STAGING_RE
#   widening that collides with the fork's governance _EXTRACT_INTO_TRUST_ROOT_RE divergence); bf9663df
#   (workspace_root() realpath-normalize for symlinked-home cold resume — new _resolve_workspace_root helper
#   routes all 3 branches through os.path.realpath; DROPPED the _encode_cc_project_dir slug-parity test — the
#   CC provider is deleted; MESHCLAW_->KIROCREW_WORKSPACE); 76cfc2e0 (the 3900-line ACP session-multiplexing
#   rewrite is DEFERRED for human review — it deletes _turn_lock + re-adds a claude_code|bedrock get_bg_session
#   selector on the absent _resolve_agent_provider = SKIP_NONKIROACP; rescued ONLY the 2 generic subagent-path
#   chokepoint fixes: redact sub-agent title+text via the existing redactors + read nested content.text via
#   _extract_text_chunk at client.py:~2660-2688 + 1 test).
# LEFT-OUT (6): SKIP_INTERNAL x5 = 6cf3aae0 (docs for the ABSENT mcp_gateway/ subsystem), 66c022fc + 9b512ca
#   (AUTOSDE.yaml — absent Brazil/AutoSDE build tooling), 21087188 (MeshClawApp-Planning row — app-registry.json
#   is [] by design), a4a35008 (Slack auth-window-expiry warning GATED on the fork-removed challenge_window
#   subsystem). DEFER x1 = 2bbf2e0b (2231-line `meshclaw pod` test-harness — brazil-build/toolbox/~/.aim +
#   Bedrock-shaped env-scrub couplings need re-architecture not rename; legitimate DEFER per SKILL cron rule).
# Verify: backend flake8 clean (18 touched files) + FULL pytest suite [see CR]; frontend tsc -b clean +
#   168 electron node --test pass (host-config/remote-token/validate). Step-6 de-Amazon: 0 live couplings
#   (toolbox refs in electron remote-token are pre-existing fork convention, .local/bin default restored).
#   KiroACP invariant HELD (enum ["acp"], no provider selector re-added, 76cfc2e0 provider-dispatch DEFERRED,
#   ACP_BACKEND_CLAUDE seam untouched, CONTRACT_VERSION unchanged). Governance keystone intact; the app-MCP +
#   cookie + ACP + slack ports re-ran test_governance_boot/self_protection green.
# 151 commits on branch (141 rebased from 22+23+24 + 9 batch-25 + 1 boundary chore). DMG rebuild OWED (macOS-only).
#   Safety refs: batch25-backup-prerebase (0cdc53a), batch25-postrebase, batch25-postport, batch25-postreview.
#   Boundaries: beta bfa7b4e8->6cf3aae0 (live tip, stragglers cleared), frontend-beta 0f2c062->9b512ca (live tip),
#   mainline unchanged 199d7853.
# CODE-REVIEW (/code-review, 10-angle recall Workflow, 36 agents): 51 raw -> 23 deduped -> 21 surviving
#   (16 CONFIRMED + 5 PLAUSIBLE). Disposition: the ONLY fork-introduced regressions were in the hand-written
#   subagent bridge I wrote when DEFERRING 76cfc2e0's rewrite (client.py:2671-2688 + _extract_text_chunk):
#   (1) empty nested content.text shadowed populated flat text -> use `or` fallback; (2) reasoning/thinking
#   chunk surfaced as user-visible subagent output -> gate on `not _su_thinking`; (3) non-dict content raised
#   AttributeError -> isinstance guard in _extract_text_chunk. All 3 FIXED + a regression test added, folded
#   into 61b83b6 via fixup/autosquash; 394 acp+governance tests green. The other 18 findings all reproduce
#   UPSTREAM VERBATIM (verified against 4aba97e9/f4acf37e/284af3ea/99041753/0d9797e post-images): the app-MCP
#   over-broad `app:`-prefix scrub + disabled-only boot-reconcile (4aba97e9), the watchdog max(last_data_ts,
#   _last_activity) clock (f4acf37e), is_https_request loopback-trust (284af3ea, documented threat model),
#   redact(accumulated) edit-preview (99041753 — per-chunk redaction already runs), and the new URL().port==""
#   coalescing (0d9797e). These are design observations for UPSTREAM to address; fork-diverging on an actively-
#   maintained subsystem mid-sync would guarantee next-batch conflicts, so left as-ported (noted here + in CR).

# --- Batch-24 (dual-repo sync 2026-07-02, ultracode comprehensive) -----------
# STACKED on the batch-22+23 worktree branch sync/beta-2026-06-27 AFTER rebasing 22+23 onto the
# advanced origin/mainline (aef7a86 = 9 fork-native commits: pentest auth incl. CWE-613 cookie
# re-mint, CPP wave-3, slack challenge-gate REMOVED, instances auto-reconnect, default port 5476).
# The rebase excised the orphaned send_channel_challenge port (182d101) + resolved ~7 conflicts;
# see the batch-24 rebase note in the kirocrew-meshclaw-sync memory. One coherent CR for 22+23+24.
# Window since batch-23:
#   backend  beta-braveheart 1926f16f..bfa7b4e8  (47 candidates ; live tip advanced past post-scan)
#   frontend MeshClawWebsite  a6b1fc7..0f2c062    (31 candidates ; live tip 0d9797e post-scan)
#   mainline fcee75b9 (v3.1.1 slack hotfix == beta 7ae12831 content, SKIP).
# 78 candidates triaged via a 156-agent adversarial Workflow (analyzer + skeptic per commit +
# synthesis) + 1 manual (8a6bfbc0 API-flake). Tally: 53 PORTED (36 KEEP + 17 PARTIAL: 32 backend +
# 21 frontend) + 25 left out.
# FORK-NATIVE-OVERLAP (the key win this batch — fork base already did these NATIVELY, so ALREADY_PRESENT):
#   ed2f53b6/ef92034/d331022 (instances auto-reconnect, native c033f4a+aef7a86+8603ed7),
#   2e3e7ed4 (pentest auth hardening, native 5700059+24e94b9). 8b1d5eb1 KEPT (only NEW tunables).
# HEADLINE backend: 8b05b3e4 macOS event-loop wedge fix (new executors.subprocess_executor pool +
#   SafeChildWatcher); 8a6bfbc0 keep ACP session alive on model-substitution advisory (adopt "Using X
#   instead" + re-issue session/new; DROPPED absent new_conversation glue, rides dormant _is_claude
#   seam); 829e96d8 gateway singleton flock (new gateway_lock.py); 68fd4f6c folder-PDF ingest +
#   dedup better-recall (pdfplumber, builds on batch-23 dedup.py); e659ed45 auto-ingest artifacts
#   into KB (new artifact_ingest.py); b9b5164a KB span/citations; a62133bc/7c50c24b/702ecac1 redaction;
#   47c06ec7 queue-message edit (BE, pairs FE fcb3309); be1df3cf model-registry context window.
# HEADLINE frontend: 9b3179e revert drag-reorder queue (dnd-kit->framer-motion); 6d8008a-adjacent
#   Radix already present; c17a009 resizable browse mirror; fcb3309 queue-edit FE; 4ab933e storage
#   Phase-0 GC (safeSetItem across ~28 files); 4af6c3b comment-highlight (self-contained, NOT the
#   absent batch-23 comment-bridge); 0f2c062 sortable Name/Date file browser (pairs BE bfa7b4e8).
# 17 PARTIALs dropped internal/absent hunks: mcp_gateway (e5210223, 9586ad6 MCP-Pool tab, 3da3f45-SKIP),
#   .meshclaw-dev app manifests+hero assets (fd633154/0951476), tui/ (dd27186 Message.tsx), Brazil
#   Config (b1edc01e), Java/Brazil doctor hints (372329d1), absent-file storage sites (4ab933e).
# SKIP_INTERNAL (16): app-registry adds (247aa7d8 pensieve, ffee2086 standup-zen), mcp_gateway
#   (da953198, 37aa5567, 3da3f45), code-review-sage (79868424, e8a1010b), secretary (328d704e, 2b91d0a),
#   taskkeeper (e3641e2), tui (7c75dec), mwinit (744f5ad), slack session-ceiling (7ae12831), Midway/
#   Taskei client (16c5375 challenge-handoff), toolbox docs (ca927a74), v3.1.1 bump (1f4ae9f7), 0cf627ea test.
# SKIP_NONKIROACP (5): 3d466e6c+42ebd21 Auto approval mode (VERIFIED hard-gated on claude_code backend
#   + native afk classifier), 79868424 code-review-sage selectable model (provider surface), a8224f18+
#   056aa42 Miami Vice 2080 theme (cosmetic).
# ALREADY_PRESENT (4): ed2f53b6, 2e3e7ed4, ef92034, d331022 (all fork-native overlaps above).
# WORKFLOW-STALL: the backend port workflow stalled TWICE at wave-6 start (model-inference hang, not a
#   shell hang — agent .jsonl flat, no python process). FIX: TaskStop (tree clean, waves 1-5 committed) +
#   ported the last 8 backend keepers via a single general-purpose Agent subagent. Frontend workflow ran
#   clean (5 waves). LESSON recorded in memory: stop+delegate-to-one-Agent beats fighting a stuck workflow.
# Verify: backend flake8 clean (37 files) + pytest 10364 pass/34 skip (only the pre-existing test_skills
#   mtime flake); frontend tsc -b clean + 2940 vitest pass (248 files). Step-6 de-Amazon: 0 live couplings
#   (7 inert comment/docstring hits). KiroACP invariant HELD (enum ["acp"], ACP_BACKEND_CLAUDE seam 22 refs,
#   no provider selector re-added, CONTRACT_VERSION=1 untouched == mainline base). Governance keystone intact.
#   config-baseline.json regenerated (187 entries) for e659ed45 knowledge descriptors + instances tunables.
# 140 commits on branch (87 from 22+23 + 32 backend + 21 frontend). DMG rebuild OWED (macOS-only).
# pdfplumber + boto3 pip-installed into the verify venv. Boundaries: beta 1926f16f->bfa7b4e8 (last
#   triaged), frontend-beta a6b1fc7->0f2c062, mainline 199d7853.

# --- Batch-23 (dual-repo sync 2026-06-27, small-batch read-each-diff) --------
# STACKED on the batch-22 worktree branch sync/beta-2026-06-27 (batch-22 not yet
# merged; origin/mainline still dfa9559). Same branch, one coherent CR for 22+23.
# Window since batch-22:
#   backend  beta-braveheart 58e0abb0..1926f16f  (4 candidates ; mainline unchanged 199d7853)
#   frontend MeshClawWebsite  0e1f442..a6b1fc7    (2 candidates)
# 6 candidates triaged by reading each diff (small-batch path, no Workflow).
# Tally: 4 PORTED (3 backend + 1 frontend) ; 2 SKIP_INTERNAL.
# PORTED:
#   eb32b8a2 -> e2d58b2  fix(dashboard): raise recent-projects cap 10->100 (_MAX_RECENT_PROJECTS).
#   6eaab8cb -> cfadb4a  feat(prompt): require a self-contained final message — 4th _CRITICAL_RULES
#     rule in context.py (decision context must live in the final message; UI collapses steps).
#     Kept the fork's dormant ACP_BACKEND_CLAUDE test_cc_provider parity test (both assertions ported).
#   c6fa6489 -> 72ef8df  feat(knowledge): auto-dedup documents across sources — new knowledge/dedup.py
#     (524 lines, regenerated from post-image) + content_hash schema/index/add_item + ingest+folder
#     triggers + knowledge_dedup MCP tool + `kirocrew knowledge dedup [--apply]` CLI + spec doc + test.
#     Inert "quip" PERSISTENT_SOURCE_TYPES literal kept verbatim (no quip source in OSS).
#   384a5d1  -> c2eb9f6  feat(picker): ProjectPicker searchable Recent tab + browse drill-on-slash
#     (pairs backend eb32b8a2). Regenerated ProjectPicker.tsx + test from post-image (byte-identical
#     pre-image, no brand tokens). +9 tests (30 total pass).
# SKIP_INTERNAL (2):
#   1926f16f  fix(browser): detect Playwright MCP via aim JSON not substring — the fork's
#     is_playwright_installed()/ensure_playwright_installed() are de-Amazoned OSS NO-OP STUBS
#     (the `aim mcp` package manager doesn't exist in OSS — exactly the coupling the fork removed);
#     the fix targets the `aim mcp list` substring check with NO fork anchor.
#   a6b1fc7   fix: anchor widget comment to selected occurrence — the anchored-comment bridge
#     (COMMENT_BRIDGE_BODY/selectionContext/FileArtifactComments) is ABSENT in the fork's diverged
#     widgetSrcdoc.ts (332 vs 560 lines); same absent subsystem as the batch-22 DEFER 4685d34.
#     ANTI-MISS LESSON: the file widgetSrcdoc.ts exists, but the CHANGED CODE does not — verify the
#     hunk's anchor, not just the filename.
# Verify: backend flake8 clean + pytest 9935 pass / 33 skip (only the pre-existing test_skills mtime
#   flake fails, untouched). frontend tsc -b clean + ProjectPicker 30 vitest + context 56 backend pass.
#   de-Amazon audit: 0 live couplings (2 inert quip literals). KiroACP invariant held; governance untouched.
# Boundaries advanced: beta 58e0abb0->1926f16f, frontend-beta 0e1f442->a6b1fc7, mainline unchanged 199d7853.

# --- Batch-22 (dual-repo sync 2026-06-27, ultracode comprehensive) -----------
# Worktree branch sync/beta-2026-06-27 off origin/mainline (dfa9559 = governance CR-284272012
# merged; tracks origin/mainline from creation). Built in a git worktree (/workplace/bolichen/
# kirocrew-wt/batch22) because another session was mid-work on feat/governance in the main checkout.
# Window since batch-21:
#   backend  beta-braveheart 59ec6e1d..58e0abb0  (68 candidates ; mainline unchanged 199d7853)
#   frontend MeshClawWebsite  ca99bb4..0e1f442    (37 candidates)
# 105 candidates triaged via a 211-agent adversarial Workflow (analyzer + skeptic per commit +
# synthesis critic; build/meshclaw-triage-batch22.js, all 105 verdicts, 0 flaked). Ported via 2
# wave-workflows (backend 10 waves, frontend 7 waves; <=5 commits/wave, sequential on one branch).
# Tally: 80 keepers (68 KEEP + 12 PARTIAL: 49 backend + 31 frontend) + 25 left out.
#   Left out: 16 SKIP_INTERNAL, 4 SKIP_NONKIROACP, 1 ALREADY_PRESENT, 4 DEFER. (full list: left-out.md)
# DEFER handling: 8e24a0fc FOLDED into 4425bd52 ; 64599424 FOLDED into f8e5b36f (one commit each);
#   cca1191 (FE project-agent UI) UNBLOCKED by BE 7519a5d9 and ported in the same batch;
#   4685d34 truly HELD (prereq affffcff artifact-comments base absent in fork).
# Cross-repo PAIRS ported BE-before-FE: e0d9577c<->cfdc19f (hide_in_chat), ae522641<->843cc81
#   (tool_search), 7519a5d9<->cca1191 (project agents), b05944e4<->47169ba (KB rename).
# HEADLINE backend: b40982aa OpenAI-compatible /v1/chat/completions (new openai_compat.py);
#   a636b1ba set_project MCP tool; ce4f6c49 replace child-kill allowlist with per-spawn basename
#   tracking (the inert b'builder'/b'aim'/b'arcc' tokens intentionally REMOVED — upstream replaced
#   the mechanism); 38173673 natural-language search recall (FTS5 stopword+OR+weighted RRF);
#   ca9d9bea event-loop wedge containment (new loop_watchdog.py); 4425bd52 background lesson
#   contradiction resolution; 9787ead5 autonudge_stop user-token handshake.
# HEADLINE frontend: 6d8008a Radix sidebar-menu migration (new ui/dropdown-menu+context-menu,
#   npm deps); 8b6807c electron wedged-backend auto-recover (new gateway-liveness.js/pyspy-dump.js
#   — ADDED to electron/package.json build.files[]); aeecc0b dnd-kit Apps-nav reorder; e66986c
#   silent expired-session recovery; cca1191 project-agent discovery UI.
# 11 PARTIALs (generic kept, internal/absent dropped): 5f782326 (drop usage_upload+_patch),
#   4d260c01 (drop entire jail subsystem), f63dfe53 (drop the app, keep manifest/conftest bits),
#   0e139747 (keep SecretaryConfig.auto_dismiss field only), a48502a7 (keep 4 generic guard fixes,
#   drop the whole workflows subsystem), 92fa8ece (drop _patch hunk), ca9d9bea (drop rewrite_agents
#   overlay-offload), 9a6e2a87 (docs: keep 5 generic backfills, drop all internal-subsystem prose),
#   a59e892c (doc coupled to FE aeecc0b), ce4f6c49 (drop providers/claude_code.py hunk), edb11ea +
#   e64b151 (frontend: drop absent-file hunks).
# SKIP_INTERNAL (16): 54f01fc0+bc92b8b5 (writing-review), 25feb233 (artifact sync/Harmony),
#   6ecb54d2 (code-review-sage), 3a1ea4d5+e08f3e4b+fc6a8c12+0d978546 (mcp_gateway), 16fa88d2 (jail),
#   71ac5fcc (Taskei/Midway), 803af9de (Midway preflight), 938a147d (taskkeeper), 20ae2b81+cf093d08
#   (internal app-registry), 48f9627e (toolbox), 9dca095 (workflows UI — generic fixes ride a48502a7).
# SKIP_NONKIROACP (4): cdd68ecb (claude_code 1M-context), d5dc40e+186aeaa (Artifactory publish/sync UI),
#   823ec07 (secretary UI — generic config rides BE 0e139747).
# ALREADY_PRESENT (1): 4ff05ccf (revert of f63dfe53's app — fork already at post-revert state).
# Verify: backend flake8 CLEAN (29 .py) + pytest 9915 pass/33 skip (3 non-batch fails: 2 botocore-
#   absent, fixed by pip install boto3; 1 test_skills mtime flake in UNTOUCHED code, fails same at
#   origin/mainline). frontend tsc -b CLEAN + 2867 vitest pass (245 files; +4 new SourcesList.rename).
#   Step-6 de-Amazon audit: 0 LIVE couplings (7 inert hits: builder-mcp allowlist read in aim_agents
#   _extract_skills, comments/docstrings, secretary config field). KiroACP invariant HELD: provider
#   enum ["acp"], ACP_BACKEND_CLAUDE/_is_claude seam (19 refs) untouched, no provider selector/cc_*/
#   bedrock_* re-added; model-id literals (claude_code map key, claude-opus-4-8[1m]) kept verbatim.
#   Governance keystone intact (security_policy.json/profiles/admission_policy.json in
#   _SENSITIVE_HOME_DIRS); CONTRACT_VERSION=2 not lowered; governance suites green where implicated.
# config-baseline.json regenerated (186 entries) for new fields: agent.sandbox_allow_no_isolation,
#   agent.tool_search, secretary.auto_dismiss_on_reply, slack_channels.*.project_path.
# 81 commits on branch (49 backend incl baseline-regen + 32 frontend). DMG rebuild OWED (macOS-only;
#   this ran on Linux). Artifacts in build/batch22-* (gitignored). CR not yet raised (stop-before-push
#   per user directive — manual run, not the cron standing authorization).
# Boundaries advanced: beta 59ec6e1d->58e0abb0, frontend-beta ca99bb4->0e1f442, mainline unchanged 199d7853.

# --- Batch-21 (dual-repo sync 2026-06-22) ------------------------------------
# Fresh branch sync/beta-2026-06-22 off origin/mainline (b10b070 = batch-20 MERGED).
# Branch tracks origin/mainline from creation (batch-20's CR-base bug fix — see memory).
# Window:
#   backend  beta-braveheart b35c496b..59ec6e1d  (mainline unchanged 199d7853)
#   frontend MeshClawWebsite  fdfe158b..ca99bb4
# 5 candidates triaged (4 backend + 1 frontend), small-batch read-each-diff (no Workflow).
# Tally: 3 PORTED (2 backend + 1 frontend) ; 2 SKIP_INTERNAL.
# PORTED:
#   59ec6e1d -> 2092347  fix(dashboard): same-origin loopback WS for embedded instance
#     iframe (Mesh-1864). Generic CSRF fix in dashboard/origin.py check_origin(): trust a
#     loopback Origin only when it == request Host. Pairs the SEC-016 work taken in batch-20.
#     +5 regression tests (port 8765/kirocrew.localhost adapted). docs/security.md note.
#   d10750e2 -> 5891deb  fix(apps): gracefully handle missing app config.json. Seed empty
#     {} on GET when config absent (data dir wiped by update) so app UI doesn't hang on
#     "Initializing". Generic apps/routes.py handle_app_config; new test_app_config_self_heal.py.
#   ca99bb4  -> d6856a2  fix(voice): pre-warm mic for instant recording (the batch-20 DEFER
#     straggler). useVoiceInput.ts regenerated from post-image (byte-identical pre-image);
#     ChatInput/ChatPage prop-wired surgically. +6 prewarm vitest.
# SKIP_INTERNAL (2):
#   5d99a8d4  feat(security): MCS-Jail Midway AgentContext enforcement (Mesh-1517) — Midway/MCS
#     coupling; jail.py absent, MCS-Jail Brazil dep. Its only generic hunk (sandbox.py
#     userns_available() public alias) has NO fork consumer — only the absent jail.py calls
#     it (anti-miss (b): same precedent as batch-16 useVisibilityInterval). cli.py --no-jail,
#     config agent.jail enum, cli_doctor jail status, config-baseline all confined to the jail.
#   afed9312  fix(mcp-gateway): raise pooled-backend cap 20->64 — 100% confined to mcp_gateway/
#     (manager.py GatewaySpec + McpGatewayConfig in loader.py). Fork has NO McpGatewayConfig
#     (grep empty) — no anchor.
# Verify: backend test_dashboard_origin 54 pass + test_app_config_self_heal 3 pass, flake8
#   clean; frontend tsc -b clean + 14 voice vitest (6 new prewarm) + ChatInput 111 pass.
# de-Amazon audit (Step 6): 0 live couplings; KiroACP invariant held (no provider glue).
# Boundaries advanced: beta b35c496b->59ec6e1d, frontend-beta fdfe158b->ca99bb4 (== live tip,
#   no straggler), mainline unchanged 199d7853.

# --- Batch-20 (dual-repo sync 2026-06-20) ------------------------------------
# Fresh branch sync/beta-2026-06-20 off origin/mainline (batch-19 boundary).
# Spans the post-batch-19 window on BOTH repos:
#   backend  beta-braveheart 36c205e4..b35c496b  (mainline unchanged 199d7853)
#   frontend MeshClawWebsite  08716b28..fdfe158b  (ca99bb4 landed mid-finalize → DEFER)
# 39 commits ported BY CONTENT (no cherry-pick; repos share content lineage only),
# path/symbol-mapped, in chronological order, one fix per commit. Verified per
# commit: backend pytest(affected files) + flake8; frontend tsc -b + vitest.
# Tally: 39 PORTED (17 backend + 22 frontend) ; 19 SKIP/ALREADY_PRESENT ; 1 DEFER.
# No NEW mainline-only delta — the upstream mainline tip is still the v3.1.0 release
# merge; every in-window candidate reached us via beta-braveheart (already triaged).
#
# HEADLINE backend: 465268b CSE must-fix hardening (SEC-009/016/006/012, d2ebac3);
#   2b2eaeb OAuth-style refresh tokens for dashboard auth (ec9c76b); a270bd1 auto-retry
#   transient backend 5xx on the chat_runner path (c2713ac); 33dc71b block session when
#   lessons exceed size cap (463feca); 2bc48022 SEC-016 instance tunnel port-mirror
#   (c76a75e); e401fda deploy-web built-in app + publish-provider registry (c2d404e);
#   d6e48920 resolve KRB5CCNAME at spawn for Kerberos-gated MCP servers (a7c4ba1).
# HEADLINE frontend: 2d6a3e46 desktop auto-update + fail-fast gateway + boot splash
#   (4b3a7e5); 9b6568c docked chat-search results pane (b7feb70); a8861d80 quota-safe
#   localStorage writes (ebae724); 1603580 keyboard nav for dropdowns/pickers (e32e603);
#   fdfe158 visible offline state + click-block + self-recovery (b1077f3); a1d2cd6
#   Serengeti Watering Hole scene (f3dee15).
#
# 2 cross-candidate OVERRIDES of the per-commit triage (analyzers couldn't see each
# other): 1e8f71cb (safeStorage reclaim escalate, e702543) DEFER->KEEP — its prereq
#   a8861d80 (quota-safe localStorage, ebae724) is a keeper ported earlier this batch;
#   2dc76572 (electron scrollable error UI + port recovery, d1fcbad) DEFER->KEEP — its
#   prereq 2d6a3e46 (electron auto-update/splash, 4b3a7e5) is a keeper this batch.
#
# SKIP / ALREADY_PRESENT (19) — backend:
#   754ef420 SKIP_INTERNAL — one-line Brazil versionSet edit (OncallWatchTower/development
#     ->release) in apps/app-registry.json; Brazil registry data, no public-build effect.
#   1464a0a7 SKIP_INTERNAL — appends one curated oncall-watchtower row to app-registry.json
#     (repo:OncallWatchTower); App Store data only.
#   08ae6866 ALREADY_PRESENT — resolution-aware MCP merge fallback (agent.py
#     rebuild_agent_config + test); the production behavior is already live in the fork.
#   35e6ca74 SKIP_INTERNAL — writing-review scanner-sync auth swaps aiohttp->curl; all
#     logic confined to the absent writing-review subsystem.
#   5f3c6555 ALREADY_PRESENT — doc-only Raycast PATH fix in REMOTE_DESKTOP_SETUP.md
#     (toolbox/bin hand-built PATH); fork doc already correct.
#   6b51f2eb SKIP_INTERNAL — adds "meetnote" (repo:KiroCrewApp-MeetNote) to
#     app-registry.json; App Store data only.
#   6aba983d SKIP_INTERNAL — silent self-install Artifactory MCP on first publish; confined
#     to the absent Harmony-Artifactory publish/install subsystem (PublishProvider.ensure_ready).
#   439ae3b6 SKIP_INTERNAL — configurable mwinit FIDO2 flags; entirely in the Midway/mwinit
#     auth subsystem the fork stubs.
#   15ebbb04 SKIP_INTERNAL — mcp-gateway rewriter isolation/correctness; confined to the
#     absent mcp_gateway/rewriter.py + its test.
# SKIP / ALREADY_PRESENT (19) — frontend:
#   7fdd2398 SKIP_INTERNAL — move Secretary settings into the Secretary app page; 4/5 files
#     have no fork counterpart (SecretaryPage/SecretaryPanel — Secretary absent).
#   eefae42a SKIP_INTERNAL — tidy TaskKeeper settings panel (settings/TaskKeeperPanel.tsx);
#     TaskKeeper built-in app absent.
#   6ab1d283 ALREADY_PRESENT — theme mascot positioning / cross-instance bleed / dynamic
#     wordmark; the only generic core hunk (App.tsx dynamic wordmark) is already present.
#   9287a3ac SKIP_INTERNAL — move TaskKeeper settings into the TaskKeeper app page; all 4
#     files confined to the deleted TaskKeeper app.
#   ed11a9ac SKIP_INTERNAL — writing-review "Download annotated copy" button; wholly in the
#     absent writing-review subsystem (no WritingReviewPage.tsx).
#   90e9478  ALREADY_PRESENT — one-line Tailwind w-full on TrustDropdown itemClass rows;
#     already present in the fork's TrustDropdown.tsx.
#   8e3ed110 SKIP_INTERNAL — Web Deploy console + unified publish-provider registry
#     (Route B); every hunk gated on the absent Artifactory/publish-console subsystem.
#   967849fb ALREADY_PRESENT — voice recording feedback + mic picker; the real feat port
#     is fork commit c58b018 (cites 967849f) — verified by content, not title.
#   aad8db00 ALREADY_PRESENT — topbar notifications bell popover; all 7 touched paths carry
#     this behavior in the fork (3 new files present, confirmed by post-image content).
#
# DEFER (1, picked up next batch): frontend ca99bb4 (fix(voice): Pre-warm mic for instant
#   recording) — landed on origin/beta-braveheart at 15:07Z mid-finalize, AFTER the batch's
#   scan; never triaged. The frontend-beta boundary is deliberately LEFT at fdfe158b (the
#   last triaged commit), NOT advanced onto ca99bb4 — advancing would make the next
#   `fdfe158b..origin/beta-braveheart` scan exclude it (silent skip). It is the first
#   candidate of batch-21.
#
# De-Amazon audit (Step 6) clean over LIVE added lines: 0 live couplings on either repo.
# Expected inert hits only — the acp/client.py Bedrock-path comment + allowlist literals,
# and the d6e48920 KRB5CCNAME helper (generic MIT Kerberos env passthrough, NOT a coupling;
# its comments may inertly name amazon-quick-mcp). KIROACP-ONLY invariant held: no
# ClaudeCode/Bedrock provider glue re-introduced; the dormant ACP_BACKEND_CLAUDE /
# _is_claude seam in acp/client.py left untouched.
# Boundaries advanced: beta 36c205e4->b35c496b, frontend-beta 08716b28->fdfe158b
# (NOT ca99bb4 — that straggler landed post-scan, DEFER to batch-21), mainline unchanged 199d7853.

# --- Batch-19 (manual dual-repo sync 2026-06-17, content-port) -----------------
# Fresh branch sync/beta-2026-06-17 off origin/mainline (batch-18 boundary).
# Spans the post-batch-18 window up to the upstream v3.1.0 release on BOTH repos:
#   backend  beta-braveheart 570a9ccf..36c205e4 (mainline merge 199d7853)
#   frontend MeshClawWebsite  ecc6e5a..08716b28 (mainline merge e6cd760)
# 48 commits ported BY CONTENT (no cherry-pick; repos share content lineage only),
# path/symbol-mapped, in chronological order, one fix per commit. Verified per
# commit: backend pytest(affected files) + flake8; frontend tsc -b + vitest.
# Tally: ~36 KEEP, ~12 PARTIAL (internal/absent hunks dropped), 0 DEFER.
# No NEW mainline-only delta — the v3.1.0 release is a straight beta-braveheart→
# mainline merge on both repos; every commit landed via beta-braveheart (triaged).
#
# HEADLINE backend: 3aa01c0 agentic chat-history MCP search tools (Phase 1);
#   f1f9914+1f65ff4 knowledge embedding correctness (embed chunk content, sig-gated
#   async rebuild, watcher self-heal); c3e7241 cron credential-exfil block;
#   2186d95 Slack tool-approval prompts for linked dashboard sessions; e10ce35 5xx
#   classification in _format_acp_error; 8f1d32e cpu_pct from /proc/stat delta.
# HEADLINE frontend: 5a3feab smooth text streaming; cd614c0 collapsible thinking
#   block; 2c687ac Voice tab (TTS+STT); d0eb0ca topbar notifications bell;
#   36c1451 chat-nav merged into activity sidebar; f4930b9 Kiro credit usage pill.
#
# PARTIAL drops (generic hunks ported, internal/absent dropped): d1091de knowledge
#   sandbox KEPT fork CCWorker / dropped claude-via-ACP; llm_pool same; mcp_cron
#   cred-vet ported without internal sub-tools; 2c687ac/36c1451/122f318 dropped the
#   absent secretarySlice + SlackTab; 39cb03d dropped secretary hunks; f4930b9
#   dropped the SkillsTab(PromptFarm) wait-budget hunk + re-anchored the pill beside
#   system-metrics (the Midway TTL pill it anchored on is stripped from the fork).
# SKIP_INTERNAL: backend 36c205e4 (version bump 3.0.2→3.1.0 + config baseline —
#   version/changelog only; fork is 0.1.0) and the v3.1.0 release-merge commits
#   (199d7853 / e6cd760 — no content delta beyond beta-braveheart). KIROACP-ONLY
#   invariant held throughout: no ClaudeCode/Bedrock provider glue re-introduced;
#   the inert ACP_BACKEND_CLAUDE seam in acp/client.py left untouched.
#

# --- Batch-18 (manual dual-repo sync 2026-06-14, ultracode comprehensive) ----
# Fresh branch sync/beta-2026-06-14 off origin/mainline (3fc5d1d = batch-16 boundary).
# Batch-17 (sync/beta-2026-06-12, 15 commits, CR-281529650) was ABANDONED by the user
# (CR draft lost / discarded) and NOT merged — so this batch re-spanned the batch-16
# boundary, re-porting batch-17's verified fixes from scratch plus all newer commits.
# 56 candidates triaged (35 backend + 21 frontend) via a 113-agent analyzer+skeptic
# Workflow. 45 commits: 42 keepers + 2 mid-batch frontend fold-ins + 1 baseline regen.
# Tally: 30 KEEP, 11 PARTIAL, 2 DEFER, 13 SKIP_INTERNAL. No NEW mainline-only delta
# (ebd78ac4 is a byte-identical dual-landing of 14ae0f50, ported).
#
# HEADLINE "KiroCrew styles" (user ask): 7661050 Kiro ghost BOOT-REVEAL splash (draws the
# KIROCREW wordmark in Kiro-purple + vendored anime.js v3.2.2) — ported KEEPING the fork's
# current header logo/avatar/favicon untouched (splash + persistent logo coexist). Plus
# fd88c429 Knight Rider theme persona, b674cd5 WCAG-AA contrast, 8b07305 disintegrate effect.
#
# 3 cross-candidate OVERRIDES of the per-commit triage (the analyzers couldn't see each
# other): 96c39b8 (MLX STT UI) DEFER->KEEP — its backend pair 7b66e2e3 is a keeper this
# batch (ported backend-first); d2325079 (unfreeze session switch) DEFER->KEEP — its prereq
# 24c317d (retrace Back/Forward) is a keeper this batch; cd6730f (artifacts masonry) PARTIAL->
# DEFER — +572-line rewrite on a hard-diverged 253-line page + new @virtuoso.dev/masonry dep,
# zero fix/security value (matches batch-17's human-review deferral).
#
# DEFER (2, noted in CR for human review): cd6730f (artifacts masonry, frontend),
#   96c39b8's masonry sibling N/A. (96c39b8 itself was RESCUED to KEEP — see overrides.)
# SKIP_INTERNAL (13): 2b95f6ac + fc06e9ab (AIM auto_update — absent), 6d07a290 + 45821ff
#   (writing-review — absent), 24745b38 (oncall-radar App Store entry — app-registry.json is
#   [] by design), 231cb2dc (code-approvers — Brazil infra), a6a7c2db (AppSenseAIUsage
#   telemetry — RUM stub), 24f23968 (Knight Rider *world* — parody theme like LCARS, absent
#   substrate), 7c9c140f + 92a1d3bb + 94196d1a + fedf6e4d (changelog/version bumps to 3.x —
#   fork is 0.1.0), 6181474a (SharePoint/Loop redaction carve-out — Amazon-corp M365 hosts
#   only, e62422ae precedent; flagged for human review).
# MID-BATCH FOLD-IN (cron rule): 2 frontend commits landed during the ~2h port (ecc6e5a
#   in-input trigger menu + friendly-placeholder hint — follow-up to the $skill picker;
#   fab2c55 root ErrorBoundary crash-guard) — folded onto the same branch, both clean KEEPs.
# Verify: backend 1538 pass / 24 skip + flake8 clean; frontend tsc -b clean + 2596 vitest;
# de-Amazon audit clean (0 live couplings; 8 inert comment hits + 4 acp Bedrock-path literals).
# Boundaries advanced: beta ebee95e7->570a9ccf, frontend-beta 1ae3c85->ecc6e5a, mainline
# 49633d05->ebd78ac4. CR-281902310.

# --- Batch-16 (cron dual-repo sync 2026-06-11) -------------------------------
# Fresh branch sync/beta-2026-06-11c off origin/mainline (batch-15 CR-281319232 merged, tip 7824113).
# 12 candidates (6 backend + 6 frontend); 9 PORTED, 3 SKIP. No mainline-only delta.
# PORTED (backend): dd59c148 (ACP stale-turn: fold stderr _last_activity into the
#   thinking-path staleness check via max(last_data_ts,_last_activity) — stops false
#   stale-turn reaping long subagent runs), c38b72e8 (test isolation: autouse fixture
#   stubs subagent.read_learned_cost so subagent-sizing tests use cfg fallback not the
#   real ~/.kirocrew store), 7a22e34d (tag compaction notices kind="compaction" via a
#   shared _append_compaction_notice chokepoint so follow-up [OPTIONS:] survive; state.py
#   auto-compact notice tagged inline to avoid an import cycle), bfcea59a (Unlink from
#   Slack: SessionMap.clear_slack_link + slack-unlink endpoint, clears both raw and
#   dashboard:-prefixed keys; 3 test files).
# PORTED (frontend): ffa68cd (Unlink from Slack menu action — pairs bfcea59a),
#   f1910f8 (deriveFollowUpOptions util skips kind=="compaction" — pairs 7a22e34d),
#   599cff3 (prompt-undo reseed/optimize hardening, follow-up to batch-13 b7d3039 —
#   valueFromUserRef discriminates DOM edit vs draft restore; completion-effect single
#   undo boundary), 74e880b (in-progress session filter: showUnreadOnly bool -> activeFilters
#   Set + SESSION_FILTERS table; ADAPTED — dropped upstream slot.app guard, fork Slot has
#   no app field), b1b1211 (board-view "new chat in folder" + button + column-aware
#   createChatInFolder; ADAPTED to the fork's diverged renderColumnFolder button cluster).
# SKIP: ebee95e7 (macOS gateway SERVICE installer — scripts/darwin-gateway-service/ absent,
#   Midway-gateway LaunchAgent + FDA Swift helper, 100% confined, NOT the fork's
#   packaging/build-desktop.sh DMG flow), b9036ac4 (standup test mock — team_manager/standup
#   absent), 1ae3c85 (useVisibilityInterval hook — generic, but its ONLY consumer is the
#   absent midway-ttl topbar countdown; no other fork caller, so SKIP per anti-miss check (b)).
# Both DMGs arch-verified (arm64 + x86_64). CR-281392951.

# --- Batch-15 (cron dual-repo sync 2026-06-11) -------------------------------
# Fresh branch sync/beta-2026-06-11b off origin/mainline (batch-14 CR-281228622 merged, tip 7b1530a).
# 8 candidates (7 backend + 1 frontend); 8 PORTED, 0 SKIP — the biggest real-port batch in a while.
# PORTED (backend): cbd32c91 (test isolation: subagent + stage-loop autouse fixtures),
#   80ba942f (send_message cron Slack delivery + delivered_to reporting),
#   82cf9907 (remove Slack Home Tab plan-usage line — also dropped a fork-only
#   _warm_usage_cache fixture in test_slack_home_tab.py to match upstream HEAD),
#   632b5731 (slack tool-timer elapsed -> title, not details, fixes accumulation),
#   2745c65b (multi-session gateway freeze: per-client _turn_lock in _prompt_loop +
#   aclosing() in send_message_stream; session.py get_or_create claim-under-lock /
#   acquire-after-release on BOTH fast path + cold-start won-race, bounded retry;
#   event-loop heartbeat + consolidation timing logs. ~600 LOC, riskiest port —
#   486 session/acp tests green. ClaudeCodeProvider guards ported VERBATIM as the
#   dormant KiroACP seam, inert in OSS),
#   499818c7 (completion_keep hot-reload setter on SubagentManager + config-patch wiring),
#   202268a3 (stop re-queue zombie-retry guard — _should_suppress_requeue on all 3 paths).
# PORTED (frontend): d12eb8c (completion_keep Chat-panel UI — matched PAIR with 499818c7,
#   committed together as one feature: Subagents settings section, mode select + chars input).
# Symbol map applied throughout (mesh_claw->kiro_crew, MeshClawConfig->KiroCrewConfig,
# api_meshclaw_config_patch->api_kirocrew_config_patch, meshclawConfig->kirocrewConfig,
# /api/config/meshclaw->/api/config/kirocrew, MESHCLAW_SESSION_KEY env already KIROCREW_).
# Verify: backend 939 tests + flake8 clean; frontend tsc -b clean + 2521 vitest (+4);
# de-Amazon audit clean (only the inert ClaudeCodeProvider seam in session.py). CR-281319232.
# GOTCHA: chat_runner re-exports PromptBusyExhaustedError (from llm_helpers) so the upstream
# test import path works verbatim. test_acp_turn_lock.py regenerated-from-post-image.

# --- Batch-14 (cron dual-repo sync 2026-06-11) -------------------------------
# Fresh branch sync/beta-2026-06-11 off origin/mainline (prior CR-281120970 merged, tip dc42d0d).
# 4 candidates (3 backend + 1 frontend); 0 PORTED, 0 PARTIAL, 4 dropped. SKIP-only boundary
# advance (no code change, no DMG rebuild — nothing ships).
# SKIP_INTERNAL: 80c564f5 (team-manager standup-cron leak fix — apps/builtins/team_manager/
#   ABSENT in fork; fork has only auto_research + file_explorer; all 3 files 100% confined,
#   reconcile_schedule_crons has no fork analogue). Anti-miss: grep'd the shared-helper — confined.
# SKIP: 7c25d7ef (v2.7 docs + AGENTS, 33 files — bulk upstream doc snapshot for absent
#   subsystems mcp-gateway/writing-review/promptfarm/research-lab; fork keeps diverged specs
#   per-change). Anti-miss on its bundled test/test_cli.py hunk: RAN the fork's 4
#   TestDoctorOllamaDocker tests → 4 passed (the validate_enterprise patch is redundant here).
# ALREADY_PRESENT: 78f8f60c (patch validate_enterprise in TestDoctorOllamaDocker, Mesh-2072 —
#   fixes a fail-CLOSED Enterprise-Grid gate; the fork's validate_enterprise is DEFAULT-OPEN
#   (slack/enterprise.py:90, fails open with no allowlist), so the fork tests are green by a
#   more fundamental divergence. Porting the patch = pointless churn. Verified by running them.)
# SKIP (frontend): 9099180 (frontend AGENTS.md v2.7 — diverged convention doc mixing present
#   Monaco/themes with absent LCARS/bikini-bottom + Artifactory ArtifactSharePanel/RemoteArtifactCard;
#   fork's website/AGENTS.md is its own de-Amazoned voice).

# --- Batch-13 (cron dual-repo sync 2026-06-10) -------------------------------
# Fresh branch sync/beta-2026-06-10d off origin/mainline (prior CR-281070110 merged).
# 9 candidates (4 backend + 5 frontend); 7 PORTED/PARTIAL, 2 SKIP.
# PORTED: f42ef2d9 (MCP slash-key alias — new mcp_utils.mcp_server_alias; kiro tool
# resolution, fixes the fork's npm:@playwright/mcp server), fac5fcbb (secretary
# retention config-roundtrip — ANTI-MISS save: titled fix(secretary) but the fork
# ships SecretaryConfig with these fields and load() dropped them, a real fork bug),
# 3838bcc (sidebar rename caret), 50ea94e (browse toggle per-session, Mesh-2055),
# b7d3039 (Ctrl+Z prompt undo/redo, Mesh-2029), fa3c5ed (Cmd/Ctrl+Click new tab),
# 977aa9e (flatter chrome + IconButton primitives + tool-name chip; PARTIAL —
# dropped the absent AimPackageManager.tsx hunk). 977aa9e+3838bcc committed together
# (shared a ChatSidebar diff hunk; splitting risked corruption).
# PARTIAL: 3a017786 (python-docx 1.x floor for py3.10 — applied to setup.cfg, NOT the
# Brazil Config which the public build ignores).
# SKIP: 39d22f8a (mcp_gateway hardening — dir ABSENT, 100% confined), 3a017786's
# Brazil Config hunk (setuptools build).
# 2fee03d/50ea94e-style tests that imported the absent secretarySlice were re-authored
# fork-correct. CR-281120970. Both DMGs arch-verified (arm64 + x86_64).

# --- Batch-12 (dual-repo sync 2026-06-10, after CR-281050885 merged) ---------
# Fresh branch sync/beta-2026-06-10c off origin/mainline (prior CR merged, no
# longer stacking). 6 candidates (3 backend + 3 frontend); 4 PORTED, 2 SKIP.
# PORTED: d00e6ac6 (backend terminal scrollback ring buffer + reconnect replay —
# RE-ANCHORED redaction: imports kiro_crew.security NOT mesh_claw.redaction, and
# tuple-unpacks `text,_ = redact_*()` since the fork's redactors return
# tuple[str,list[str]] while upstream's redaction module returns str — a verbatim
# copy would have crashed; added _redact_terminal helper + 6 regression tests),
# fbdf391 (frontend terminal session persistence/rename/reconnect — LABELS_KEY
# symbol-mapped to kirocrew-terminal-labels; renameSession already existed),
# 2fee03d (input interactive while stopping, Mesh-2004 — test re-authored fork-
# correct: dropped the absent secretarySlice the upstream test imported, matched
# the fork's flat dashboard-slot shape), 76a7fa3 (force-kill arming debounce +
# consequence-stating tooltip — new utils/stopDebounce.ts, per-slot Map ref).
# SKIP: 527459a0 (config-baseline regen — adds ONLY promptfarm + auto_update_aim
# (AIM) schema, both absent/internal; fork baseline correctly has neither),
# 641a6f0c (per-agent provider override — SKIP_NONKIROACP: re-introduces the
# deleted multi-provider dispatch factory with bedrock/claude_code branches;
# meaningless under the enum ["acp"], no ungated generic hunk to salvage).

# --- Batch-11 (cron dual-repo sync 2026-06-10) -------------------------------
# 6h cron run. 8 candidates (3 backend + 5 frontend); 3 ported, 4 SKIP, 1 ALREADY_PRESENT.
# Stacked on batch-10 (CR-280980741 still pending — origin/mainline lacks it + the
# KiroACP-only refactor, so the existing sync branch is the correct base, NOT a fresh
# branch from origin/mainline). PORTED: 03e2ba43 (slack tool-card elapsed time),
# 0dc8ff1a (kirocrew cron preview), 964b7e5 (board card layoutId per column). SKIP:
# 599d6f64 + ed7c87f (promptfarm publish — absent), 7342c6e (secretary — absent),
# 7eb7d6c (Cloud Sync SyncPanel — absent). ALREADY_PRESENT: a673571 (MCP OAuth banner
# — fork ChatPage already routes m.role==='mcp_oauth').

# --- Batch-10 (dual-repo sync 2026-06-10) ------------------------------------
# First sync to cover BOTH repos. Backend window e62422ae..29ad9776; frontend window =
# MeshClawWebsite beta since the 2026-06-02 fork snapshot through 94e92af. 97 commits
# triaged. Ported 67: 11 backend (9 KEEP + instances feat 38864fd9/d17306e1 + Projects/
# Autopilot apps 29ad9776) + 56 frontend (54 wave-ported + instances UI e341b727/8406d8da
# + 94e92af). The 6h auto-sync cron fired mid-batch and surfaced 29ad9776 + 94e92af (a
# matched pair, CR-280786172) — folded into this batch rather than run as a competing
# pipeline (the tree was mid-port). The rest were SKIP_INTERNAL (absent subsystems),
# SKIP_NONKIROACP, or ALREADY_PRESENT — see the CR.
#
# instances/ feature: previously DEFERRED, now owner-cleared for external release.
# Ported as a PARTIAL de-Amazon port (backend 38864fd9 + d17306e1, frontend e341b727 +
# 8406d8d): kept the generic multi-instance registry / port-allocator / SSH tunnel
# manager (plain OpenSSH, not coupled to the stubbed outbound tunnel/manager.py) + UI;
# DROPPED the Midway SSH-cert watchdog (instances/midway.py) and its security.py
# carve-out — porting it would re-open ~/.ssh, forbidden by MIGRATION_PLAN.md L383.
#
# Backend SKIPs in-window: 437262df (Midway connectivity docs), 5c7b2c93 +
# 1408ca73 (real tunnel-probe machinery the fork omits), 33658c12 (GitFarm app
# registry). Frontend SKIPs: writing-review, secretary, code-reviewer, auto-research,
# mcp_gateway UI, GitFarm sync, RUM telemetry, AIM, Harmony Artifactory, lcars +
# bikini-bottom themes (absent subsystems); Bedrock image-limits + Claude-Code agent
# picker (SKIP_NONKIROACP). The fork's website/ provider selector was de-Amazoned to
# KiroACP-only in this batch (f1883d2e — Bedrock removed from the UI to match the
# backend enum ["acp"]).
